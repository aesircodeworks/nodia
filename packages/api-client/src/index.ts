// Placeholder entry point. The typed fetch client wrapping the generated API
// types (see src/generated/) is implemented once the contract generation
// pipeline has produced its first output.
export {};
