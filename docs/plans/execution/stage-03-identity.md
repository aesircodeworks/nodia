# Execution Journal: Stage 3, Identity, AuthN, AuthZ

Durable record of execution runs for [stage-03-identity.md](../stage-03-identity.md). Append a new run header per run; never rewrite prior entries.

## Run: 2026-07-09

- Stage: 3, Identity, AuthN, AuthZ
- Date: 2026-07-09
- Branch: `feat/api-implementation`
- Base commit: `d3a95016b94f3bd046e82e12201ddade5cbac913`

Verified starting state: Stages 1 and 2 are done (status table plus commit history through d3a9501). Nothing from Stage 3 exists: there is no `app/Identity` directory, `composer.json` has neither `laravel/passport` nor `spatie/laravel-activitylog`, `database/migrations` holds only the Laravel defaults and the four Stage 2 tenancy migrations, and the two Stage 2 deferral points are still in place exactly as the stage plan expects (`PlatformAuthPlaceholder` behind the `auth.platform` alias, `ResolveTenantFromHeader` validating tenant existence only with the membership check deferred to this stage). The master plan status table said "Not started"; this run flips it to "In progress".

### Task checklist

- [x] task-01: Passport install, uuid oauth migrations, staff and customer guards and providers, client seeding, explicit lifetimes (plan task 1)
- [x] task-02: Slice 1: staff token issuance, JWT claims, `GET /v1/me` (empty memberships), contract paths (plan task 2)
- [x] task-03: Slice 2: refresh rotation, reuse detection with family revocation, logout, parallel-refresh concurrency test (plan task 3)
- [x] task-04: `memberships` and `roles` migrations with RLS, `Capability` enum, template seeder under `nodia_platform`, isolation tests first (plan task 4)
- [x] task-05: Slice 3: membership validation in tenant resolution, platform-scope fallback, resolution matrix, `/v1/me` memberships (plan task 5)
- [x] task-06: Slice 4a: capability Gate and Policy layer, authorization matrix harness (plan task 6)
- [x] task-07: Stage 2 alias rebinding: Passport bearer plus `tenants.manage` on the platform route group (plan task 7)
- [x] task-08: Slice 4b: role endpoints and `GET /v1/capabilities` (plan task 8)
- [x] task-09: Slice 4c: membership endpoints, `InviteUser` and `AssignRole` Actions, invitation acceptance (plan task 9)
- [x] task-10: users MFA columns and `mfa_recovery_codes` migration, failing unit and recovery-code concurrency tests first (plan task 10)
- [x] task-11: Slice 5: MFA endpoints, token-endpoint challenge, enforcement middleware, denial paths (plan task 11)
- [ ] task-12: `customers` migration with RLS, isolation tests first (plan task 12)
- [ ] task-13: Slice 6: customer endpoints, `RegisterCustomer` and `ClaimGuestAccount`, customer guard, tenant claim validation, guest-creation concurrency test (plan task 13)
- [ ] task-14: Adjusted activitylog install: uuid keys, non-null `tenant_id`, append-only RLS, isolation tests first (plan task 14)
- [ ] task-15: Slice 7: audit recording on logins, mutations, and platform-role use, data-driven route coverage (plan task 15)
- [ ] task-16: Slice 8: staff password reset with single-use token consumption guard, concurrency test first, family revocation (plan task 16)
- [ ] task-17: Closeout: OpenAPI review, types drift check, master plan status flip (plan task 17)

### Review rounds

### Decisions and deviations

#### task-01: Passport install and configuration (2026-07-09)

Verified current Passport documentation (context7 `/laravel/passport`, pinned `13.x` docs, cross-checked against the installed `v13.7.5` source under `vendor/laravel/passport`) before writing any code, per the plan's Risks item:

- Password grant: available but disabled by default since Passport 12.0; requires an explicit `Passport::enablePasswordGrant()` call. Confirmed fit for the stage's design; the fallback (first-party token controller not using the password grant type) is not needed. A first-party `/v1/auth/*` controller is still built in task-02, but it drives Passport's `AuthorizationServer` directly with `grant_type=password` rather than exposing Passport's own `/oauth/token` route, which is disabled via `Passport::ignoreRoutes()` so it cannot leak the OAuth-shaped contract Passport ships by default.
- Provider binding: `passport:client --provider=` and `ClientRepository::createPasswordGrantClient($name, $provider, $confidential)` bind a client to a named entry in `config('auth.providers')`; a client's `provider` column is read from `oauth_clients` and compared against the authenticating guard's provider name at token-issuance and resource-request time. Confirmed sufficient for the two-population design without any Passport source changes.
- JWT claim customization: Passport's `AccessTokenTrait::convertToJWT()` (`league/oauth2-server`) is a private trait method, not overridable by subclassing `Laravel\Passport\Bridge\AccessToken`. The supported extension point is `Passport::useAccessTokenEntity()` with a custom entity overriding the public `toString()` method, which task-02 will use to add `identity_type` and (for customers) `tenant_id` claims. Recorded now because it determines task-02's approach; no code needed yet since no endpoint ships this task.
- Refresh token internals: `oauth_refresh_tokens` has no reuse-detection concept built in beyond `revoked` and `Passport::$revokeRefreshTokenAfterUse`; the family-revocation semantics the stage plan wants (revoke every live token in the family on reuse) need custom code around `RefreshTokenRepository`, confirmed as task-03's job, not something Passport provides directly.

No fallback triggered: Passport's password grant is fit for purpose as designed in the stage plan.

Additional decisions made while implementing:

- Passport's five published oauth migrations (`oauth_auth_codes`, `oauth_access_tokens`, `oauth_refresh_tokens`, `oauth_clients`, `oauth_device_codes`) are all kept for parity with Passport's own model expectations, even though the device grant is unused; `user_id` columns changed from `foreignId` (bigint) to `uuid` on the three tables that carry one, matching the uuid primary key shared by `users` and the future `customers` table. `oauth_clients.owner` morphs changed from the default bigint morphs to `nullableUuidMorphs` for the same reason. No FK constraints exist on any `user_id`/`client_id` column in Passport's own migrations (`foreignId`/`foreignUuid` without `->constrained()` just types the column), which is what makes a single polymorphic `user_id` shared between two provider populations possible.
- No tenant_id, no RLS on any oauth table, per the plan's sanctioned deviation. Added `Rls::grantUnscoped()` (plain `GRANT ... TO nodia_app, nodia_platform`, no `ENABLE`/`FORCE ROW LEVEL SECURITY`, no policies) since these tables are reached from tenant-scoped request transactions (customer token issuance and refresh run under `SET LOCAL ROLE nodia_app` via the Host-resolution middleware) and would otherwise be invisible to that unprivileged role once the request stops running as the table owner.
- The two seeded password-grant clients (`Staff` bound to the `users` provider, `Customer` bound to the `customers` provider) are public (no secret): the wire contract's request Data classes (`StaffTokenRequestData`, `CustomerTokenRequestData` in the stage plan) never carry OAuth client credentials, so the `/v1/auth/*` controllers built in later slices look each client up by provider plus grant type at request time instead of managing a shared secret. This also sidesteps `Laravel\Passport\Client::secret()`'s at-rest hashing, which only exposes the plaintext secret transiently on the request that created it, making a deterministic seeded secret awkward without bypassing `ClientRepository`. Seeded via a data migration (`2026_07_09_000009_seed_passport_clients.php`), the same pattern the Stage 2 tenants migration used for the sentinel row, so it runs exactly once and is tracked like every other schema change.
- Refresh token lifetime: the plan fixes the access token lifetime at 15 minutes but leaves the refresh lifetime unspecified (Risks: "Admin portal idle timeout... expressed here only as refresh token lifetime configuration... deferred until the admin frontend consumes the API"). Picked 30 days (`43_200` minutes) as an explicit, config-driven, testable starting value (`config/identity.php`, `PASSPORT_REFRESH_TOKEN_TTL_MINUTES`), open to revisit once that design note lands.
- `App\Identity\Models\Customer` is a minimal stub (`Authenticatable`, `HasUuids`, `HasApiTokens`, implements `Laravel\Passport\Contracts\OAuthenticatable`) with no fillable attributes or casts, so the `customers` guard and provider and the seeded client have a model to bind to before task-12 ships the `customers` table and its RLS policy. Nothing queries it yet. `App\Models\User` gained the same `HasApiTokens`/`OAuthenticatable` wiring; it was not moved into `App\Identity\Models`, matching system-design 3.2's directory diagram, which keeps staff on the default Laravel `users` model at `App\Models\User` (ADR 007) while `Identity/Models` holds `Membership`, `Role`, `Customer`.
- RSA keypair provisioning for JWT signing (`passport:keys`) is not addressed this task: `PassportServiceProvider`'s `AuthorizationServer` binding is lazy (a singleton closure, never resolved during boot), so nothing in this task's tests touches the signing keys, and no CI job needed changes. This is a real open item for task-02, which will need a deterministic way to get keys into place for local dev, tests, and CI (candidates: generate on first boot when missing, or provision via `PASSPORT_PRIVATE_KEY`/`PASSPORT_PUBLIC_KEY` env vars) — flagged here so it isn't missed, not solved.
- Architecture suite: added `App\Identity\IdentityServiceProvider` and `App\Identity\Models`/`App\Identity\Http\Controllers` to the existing `arch()->preset()->laravel()->ignoring([...])` list in `tests/Architecture/PresetTest.php`, mirroring the existing Tenancy entries, so context-owned service providers and models keep passing the Laravel preset's App\Providers/App\Models location rules.

Test evidence: `composer -d apps/api run test` (all six suites: Feature, Unit, Contract, Architecture, Isolation, Concurrency) passed 393 tests, 1453 assertions, 0 failures, including the two new Unit files (`tests/Unit/Identity/PassportConfigurationTest.php`, `tests/Unit/Identity/PassportMigrationsTest.php`, 29 tests covering lifetime-from-config, password grant enablement, route suppression, guard/provider wiring, oauth table uuid typing, absence of `tenant_id`, absence of RLS, the unscoped grants, and the two seeded clients). `composer -d apps/api run lint` (Pint) passed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` ran with no diff against the committed generated TypeScript (no laravel-data Data classes changed this task, so no drift possible).

Commit: `9922557` (`feat(identity): install and configure Passport for staff and customer guards`).

#### task-02: Slice 1, staff token issuance and GET /v1/me (2026-07-09)

Built the feature test first (`tests/Feature/Identity/StaffAuthenticationTest.php`), then the contract (`StaffTokenRequestData`, `TokenPairData`, `CurrentUserData`, `MembershipData`, the OpenAPI paths), then the unit test for the claim builder (`tests/Unit/Identity/IdentityClaimsTest.php`), then implemented to green, per the double loop.

Endpoints shipped: `POST /v1/auth/staff/token` (unauthenticated; `StaffTokenRequestData` email/password in, `TokenPairData` out) and `GET /v1/me` (staff bearer; `CurrentUserData` out, `memberships` always `[]` until task-05 as the task specifies). `mfa_enabled` is likewise always `false`: the `users.mfa_enabled` column does not exist until task-10, and `CurrentUserData` ships its full stage-endpoint-table shape now so the wire contract does not reshape later, only its data sources do (mirrors the memberships placeholder). `MembershipData` (id, user_id, user_name, user_email, tenant_id, role_id, role_name, scope) is defined now, ahead of the task's named Data list, purely as a wire shape so `CurrentUserData.memberships` has a concrete element type instead of `mixed[]`; nothing constructs it yet.

Two deviations from a literal reading of the plan, both investigated against vendor source before deciding, in the spirit of task 1's Passport verification:

1. **The `/v1/auth/staff/token` controller cannot expose Passport's OAuth-shaped request directly** (already established by task 1), so `App\Identity\Actions\IssueStaffToken` builds a synthetic `Illuminate\Http\Request` with `grant_type=password`, the seeded staff client's id, and the caller's email/password, converts it to a PSR-7 request via `Symfony\Bridge\PsrHttpMessage\Factory\PsrHttpFactory`, and calls `AuthorizationServer::respondToAccessTokenRequest()` directly. `Laravel\Passport\Client::password_client` turned out to be a virtual accessor derived from the `grant_types` column (Passport's current published migration has no such column; task 1's decision journal predates this discovery), not a queryable database column, so the client lookup is by `provider` alone (exactly one password-grant client is seeded per provider by task 1, so this is unambiguous) rather than `where('password_client', true)`, which raised `column "password_client" does not exist` against real Postgres. `League\OAuth2\Server\Exception\OAuthServerException::invalidCredentials()` (bad username or password, and Passport's `Bridge\UserRepository` never distinguishes an unknown email from a wrong password) carries the OAuth error type `invalid_grant`, not a type literally named `invalid_credentials`; the action maps only that error type to the stage's `invalid_credentials` code (401) and rethrows anything else, so a genuinely broken client or grant configuration surfaces as a 500 instead of masquerading as a login failure.

2. **JWT claim customization needed a structural fix beyond task 1's plan.** `Passport::useAccessTokenEntity()` is declared `class-string<Laravel\Passport\Bridge\AccessToken>`, so a class only implementing `AccessTokenEntityInterface` (composing the three league/oauth2-server traits directly, which was the first approach, chosen because `AccessTokenTrait::convertToJWT()` and the private `$privateKey`/`$jwtConfiguration` state it needs are private to whichever class composes the trait and therefore unreachable from a plain subclass of `Bridge\AccessToken`) fails Larastan with `argument.type` rather than being silently accepted; PHPStan's own instructions forbid suppressing this with a cast, an ignore comment, or a baseline entry. The fix that satisfies both constraints: `App\Identity\OAuth\IdentityAccessToken extends Laravel\Passport\Bridge\AccessToken` (satisfying the `class-string` bound structurally) and separately re-composes `League\OAuth2\Server\Entities\Traits\AccessTokenTrait` in the child (giving the child its own private `$privateKey`/`$jwtConfiguration`/`initJwtConfiguration()`/`convertToJWT()`-equivalent state, shadowing the parent's inaccessible copies, since PHP trait composition in a subclass takes precedence over inherited parent methods of the same name); the other trait members it needs (`getClient()`, `getIdentifier()`, ...) are `protected` in `TokenEntityTrait`/`EntityTrait` and stay inherited from `Bridge\AccessToken` without recomposition. `toString()` duplicates `AccessTokenTrait::convertToJWT()`'s claim set (`aud`, `jti`, `iat`, `nbf`, `exp`, `sub`, `scopes`) and adds whatever `App\Identity\OAuth\IdentityClaims::for($provider)` returns, keyed off the issuing client's `provider` (`Laravel\Passport\Bridge\Client::$provider`, a public property already carrying `'users'` or `'customers'`); `IdentityClaims` is a pure function so it is unit-tested directly without touching JWT signing or the database. Registered via `Passport::useAccessTokenEntity(IdentityAccessToken::class)` in `IdentityServiceProvider::boot()`.

**Genuine blocker resolved rather than deferred: RSA key provisioning, flagged but explicitly left open by task 1.** `league/oauth2-server`'s `AbstractGrant::issueAccessToken()` and `Lcobucci\JWT`'s `BearerTokenValidator` both require real key files (or configured key contents) the moment any token is issued or validated, and neither the app image, `make setup`, nor CI provisioned them; the first attempt to hit the new endpoint failed with "Private key is empty" equivalent errors. Fixed with a self-contained test harness fix mirroring the existing `Tests\Support\MigratedDatabase` pattern: `Tests\Support\OAuthKeys::ensure()` runs `passport:keys --force` once per test process if `storage/oauth-{public,private}.key` are missing, called from `Tests\TestCase::setUp()` (covering Feature, Unit, Contract, Isolation, and Concurrency, since all extend `TestCase`; Architecture does not need it and does not boot the framework). This means CI needed no changes: every job that runs Pest suites already boots through `TestCase`. Also added the same idempotent `passport:keys` step to `make setup` (guarded like the adjacent `key:generate` step) for host-based local dev; the live Docker Compose `api` service's own key provisioning is not addressed (no task in this stage's plan currently exercises `POST /v1/auth/staff/token` against the running container, and the CLAUDE.md-mandated verify command is only `/v1/health`), so this remains an open item if a later task needs the live stack to issue tokens.

**A second, adjacent blocker discovered and resolved while proving the "rejected 901 seconds later" requirement**: `league/oauth2-server`'s `AbstractGrant::issueAccessToken()` sets the `exp` claim from `(new DateTimeImmutable())->add($accessTokenTTL)` and `Lcobucci\JWT`'s `BearerTokenValidator` validates it against its own hardcoded `new DateTimeImmutable('now', ...)` clock (confirmed by reading both classes' source before writing any test code); neither goes through Carbon, so Laravel's `freezeTime()`/`travel()` test helpers, which only patch `Carbon::setTestNow()`, have no effect on a Passport-issued JWT's expiry in either direction. This is intrinsic to the two vendor libraries, not something reachable through `Passport::useAccessTokenEntity()`'s claim-set extension point without also overriding `setExpiryDateTime()`/replacing the validator's clock, which would diverge production behavior from what is tested. Deviation taken: the `expires_in: 900` value is asserted directly and tied to `config('identity.access_token_ttl_minutes')` (not restated as a literal magic number) in the feature test; the "rejected once past expiry" requirement is proven by decoding a genuinely issued access token, rebuilding a new JWT carrying the same `jti` (so it still resolves as a live, non-revoked row in `oauth_access_tokens`, the same check `BearerTokenValidator::isAccessTokenRevoked()` performs) and the same `sub`/`aud`/`scopes`/`identity_type` claims but with `iat`/`nbf`/`exp` moved into the past, signed with the app's real private key, and asserting `GET /v1/me` rejects it with `auth.unauthenticated`. This exercises exactly the `LooseValidAt` validation branch that will reject the real token 901 seconds after issuance, deterministically, without sleeping or attempting to fake wall-clock time inside a vendor library that does not support it.

`response()->json($data, ...)` is used explicitly for the token endpoint's controller rather than returning the `TokenPairData` object directly from the controller (the pattern used everywhere else, including `GET /v1/me`): `Spatie\LaravelData\Concerns\ResponsableData::calculateResponseStatus()` defaults a POST response to 201, which is correct for `POST /v1/tenants` (creates a resource) but wrong for a token exchange (issues a token, creates nothing addressable), caught by the feature test asserting `assertOk()` against an initial 201.

No isolation or concurrency coverage this task: no tenant-scoped table is introduced (matches the stage plan's "Isolation and Concurrency: not applicable" note for Slice 1).

Test evidence: `composer -d apps/api run test` (all six suites) passed 411 tests, 1530 assertions, 0 failures — up from task 1's 393/1453, i.e. 18 new tests (8 feature, 4 unit for the claim builder, plus the Contract suite's data-driven coverage test picking up the 5 new documented responses and 2 new registered routes) and 77 new assertions, with the 2 pre-existing registry tests (`ErrorCodeTest`, `PresetTest`) updated for the new `invalid_credentials` code and exception class. `composer -d apps/api run lint` (Pint) passed after one auto-fix pass (import ordering, spacing). `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors after the `IdentityAccessToken` structural fix above. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for the four new Data classes and the `invalid_credentials` addition to the generated `ErrorCode` union, committed with no drift on a second run.

Commits: `6e5bc08` (`feat(identity): issue staff tokens and expose GET /v1/me`), plus this journal entry.

#### task-03: Slice 2, refresh rotation, reuse detection, logout (2026-07-09)

Built the feature tests first (`tests/Feature/Identity/StaffRefreshTest.php`, `tests/Feature/Identity/StaffLogoutTest.php`), then the contract (`RefreshTokenRequestData`, reusing `TokenPairData` from task-02, the two new OpenAPI paths, and a combined `RefreshTokenProblem` schema following the `TenantDomainConflictProblem` precedent from Stage 2 for a status that can carry either of two codes), then the concurrency test first for the rotation guard (`tests/Concurrency/StaffRefreshRotationContentionTest.php`), then the unit tests for the family-revocation logic (`tests/Unit/Identity/RefreshTokenFamilyRevocationTest.php`), then implemented to green, per the double loop.

Endpoints shipped: `POST /v1/auth/staff/refresh` (unauthenticated; `RefreshTokenRequestData` in, `TokenPairData` out, mirroring `IssueStaffToken`'s approach of driving `AuthorizationServer::respondToAccessTokenRequest()` directly with `grant_type=refresh_token`) and `POST /v1/auth/staff/logout` (staff bearer; 204).

Core design, investigated against `league/oauth2-server` and Passport source before writing any code, per the plan's Risks item "Reuse detection semantics":

- **Family tracking.** A new `family_id` column on `oauth_refresh_tokens` (migration `2026_07_09_000010`, a plain `ALTER TABLE` since the table already merged in task-01 and per CLAUDE.md merged migrations are never edited) threads every refresh token issued from one login through every rotation that follows it. `App\Identity\OAuth\IdentityRefreshTokenRepository extends Laravel\Passport\Bridge\RefreshTokenRepository`, bound in `IdentityServiceProvider::register()` against the concrete class name (not the interface): `PassportServiceProvider` resolves `Bridge\RefreshTokenRepository::class` via the container for both the password grant (login) and the refresh grant (rotation), so one binding reaches every code path that creates or rotates a refresh token. `family_id` is `uuid` and NOT NULL with no default: refresh token ids themselves are league's own 40-character hex strings, not valid uuids, so a brand-new login's token gets a fresh `Str::uuid()`, never its own identifier (a first attempt reusing the token's own id as its family id failed with a Postgres `invalid input syntax for type uuid` error against real Postgres, caught by the first test run). No backfill concern: this table carries no production traffic yet.
- **Rotation guard (Slice 2 Concurrency).** `revokeRefreshToken()` overrides Passport's unconditional `update(['revoked' => true])` with a conditional `UPDATE ... WHERE id = ? AND revoked = false`, checked by affected-row count, never read-then-write, mirroring the `RemoveDomain`/`TenantDomainContentionTest` pattern from Stage 2. The concurrency test (two parallel `/v1/auth/staff/refresh` calls presenting one refresh token, driven through the real HTTP kernel in forked workers per `ParallelRunner`) proves exactly one 200 and one 401. The loser's specific error code is not pinned to a single value: `league/oauth2-server`'s `RefreshTokenGrant::validateOldRefreshToken()` calls `isRefreshTokenRevoked()` (a plain read) *before* the grant ever reaches the conditional-UPDATE guard, so depending on exactly how the two requests interleave, the loser is rejected either at the guard itself (`invalid_refresh_token`, this class's own `RaceLostHint`) or earlier, because the winner's commit already landed by the time the loser's own revoked-check runs (`refresh_token_reused`, `VendorRevokedHint`). Both are asserted as acceptable in the concurrency test with a comment explaining why; the invariant that is unconditionally guaranteed regardless of interleaving, and the one actually asserted strictly, is that exactly one requester's conditional UPDATE can ever flip `revoked` false to true for one row.
- **Reuse detection and family revocation.** `isRefreshTokenRevoked()` treats any presentation of an already-revoked token as reuse (the revoked flag alone cannot distinguish a legitimate past rotation from a captured, replayed token) and calls `App\Identity\Actions\RevokeRefreshTokenFamily`, which revokes every currently-live refresh token in the family and the access token issued alongside each one, via two UPDATEs both conditioned on `revoked = false` (naturally idempotent under replay or concurrent replay of the same stale token). The rotation guard's race-loser is deliberately *not* routed through this path: `RotateStaffToken` distinguishes the two failure hints and only maps `VendorRevokedHint` to `refresh_token_reused` per the design point above, so a benign concurrent double-call never revokes the winner's brand-new pair. This is stricter than Passport's default single-token revocation, exactly as the plan's Risks item anticipates.
- **Logout** (`App\Identity\Actions\RevokeStaffSession`) revokes only the presented session's access token (via Passport's own `AccessToken::revoke()`) and its one live refresh token (`WHERE access_token_id = ? AND revoked = false`), deliberately not the whole family: a legitimate logout must not cascade into revoking other sessions for the same user, proven by a dedicated feature test logging in twice and confirming the second session survives the first session's logout.

**Blocker investigated and resolved within this task, not deferred: a Laravel testing artifact, not a production bug.** The first run of both `StaffLogoutTest` and `StaffRefreshTest`'s multi-request scenarios failed with a revoked bearer token still authenticating successfully on a second `GET /v1/me` call within the same test method. Root-caused (not guessed) by a reproduction script confirming the DB row was genuinely `revoked = true` while the HTTP response was still 200: `Illuminate\Auth\AuthManager::guard()` caches a resolved guard instance (`$this->guards[$name] ??= ...`) for the life of the container, and `Illuminate\Auth\GuardHelpers` caches the resolved `$user` on first success; Laravel's HTTP testing helpers do not rebuild the application between `$this->postJson()`/`$this->getJson()` calls within one test method (only between test methods), so a guard resolved during an earlier call in the same test keeps returning its cached user on a later call regardless of what bearer token that later request actually presents. Confirmed as test-only by reproducing the fix with `Illuminate\Support\Facades\Auth::forgetGuards()` inserted between the state-changing call and the next authenticated assertion, which is now called at each such point in both files with a comment explaining why; a real deployment boots a fresh application per request and has no equivalent problem. Flagged here in case it recurs in later Stage 3 slices that also chain multiple authenticated calls in one test (MFA, membership endpoints).

No isolation coverage this task: no new tenant-scoped table (the `family_id` addition lands on the existing unscoped `oauth_refresh_tokens` table, matching the Stage 3 plan's sanctioned Passport-infrastructure exception already covered by `tests/Unit/Identity/PassportMigrationsTest.php`).

Test evidence: `composer -d apps/api run test` (all six suites) passed 432 tests, 1629 assertions, 0 failures on two consecutive full runs — up from task-02's 411/1530, i.e. 21 new tests and 99 new assertions (10 feature across the two new files, 1 concurrency, 6 unit for the family-revocation logic, plus the Contract suite's data-driven coverage test picking up 4 new documented responses, and the 2 pre-existing registry tests, `ErrorCodeTest` and `PresetTest`, updated for the two new codes and exception classes). One run hit a single failure in `StaffAuthenticationTest`'s pre-existing `expires_in` assertion (899 vs 900, the same raw-wall-clock timing sensitivity task-02's journal already documents for JWT expiry, unrelated to this task's code); it passed in isolation and on every other full run, confirmed as a pre-existing flake rather than a regression by rerunning it alone. `composer -d apps/api run lint` (Pint) passed with no changes needed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for `RefreshTokenRequestData` and the two new `ErrorCode` union members, committed with no drift on a second run. The rotation-guard concurrency test was additionally run 3 extra times in isolation to check for flakiness; all passed.

Commit: `ba98ef7` (`feat(identity): rotate staff refresh tokens with reuse detection and logout`), plus this journal entry.

#### task-04: memberships and roles schema, Capability enum, template seeder (2026-07-09)

Pure migration task per the plan (task breakdown item 4): no endpoint routes to `memberships` or `roles` yet, so this task's TDD loop is isolation tests first, then migrations and models, then unit tests for the two named invariants, with no feature/contract loop (no wire endpoint exists this task).

Migrations, in dependency order: `2026_07_09_000011_grant_users_table_to_rls_roles.php`, `2026_07_09_000012_create_roles_table.php`, `2026_07_09_000013_create_memberships_table.php`, `2026_07_09_000014_seed_template_roles.php`.

- **`roles`**: nullable `tenant_id` (the sanctioned exception), `capabilities` jsonb, `unique(tenant_id, name)` plus the partial `roles_template_name_idx` on `name` where `tenant_id is null` (Postgres treats NULLs as distinct under a normal unique index, so the plain unique constraint alone never catches two templates sharing a name). RLS is hand-written rather than through `Rls::applyTenantPolicies`: a `roles_template_or_tenant_read` SELECT policy admitting `tenant_id is null or tenant_id = app.tenant_id`, three separate `roles_tenant_insert`/`_update`/`_delete` policies (each requiring `tenant_id = app.tenant_id`, which a NULL `tenant_id` row can never satisfy, so nodia_app can never mutate a template at the database level), and `roles_platform_write for all to nodia_platform using (true) with check (true)`, the one write path a template row can ever satisfy.
- **`memberships`**: `unique(user_id, tenant_id)`, `scope` a plain string column backed by the new `App\Identity\Enums\MembershipScope` enum (`tenant`, `platform`) via an Eloquent cast. RLS is the plain `Rls::applyTenantPolicies('memberships')` (no `platformWrite`: the plan calls this "standard single-table policy," and membership mutation is tenant admin surface for a future `InviteUser`/`AssignRole` Action running under `nodia_app`, not platform-admin surface the way `tenant_domains` is) plus one extra hand-written permissive SELECT policy, `memberships_self_read`, matching `user_id = current_setting('app.user_id', true)::uuid`. Postgres ORs multiple permissive policies together for the same command, so this only ever widens SELECT visibility past the tenant policy, never past it for INSERT/UPDATE/DELETE, proven by a dedicated isolation test asserting a `nodia_platform` write outside any asserted tenant context still affects zero rows.
- **`Identity/Capability.php`**: the ten-entry initial registry the plan names, plus `isFinanciallyPrivileged(): bool` marking `OrdersRefund` and `PayoutsView` (system-design 5.1/5.4: "MFA is mandatory for platform-scope staff and for tenant roles that include payout or refund permissions"; MFA's own enforcement mechanism is a later Stage 3 task, this only seeds the flag on the registry now so the two never drift apart). Locked down by a Unit test asserting the exact registry list and the exact financially-privileged subset, so a later stage extending the enum gets a loud diff instead of a silent rename.
- **Template seeder**: `App\Identity\Actions\SeedTemplateRoles`, keyed on `(tenant_id: null, name)` via `updateOrCreate`, so repeated invocation is idempotent by construction. `2026_07_09_000014_seed_template_roles.php` runs it once under `nodia_platform` inside the migration (same data-migration pattern as the tenants sentinel row and the task-01 Passport clients: tracked like every other schema change, not a re-runnable `Database\Seeders` class), and `tests/Unit/Identity/SeedTemplateRolesTest.php` calls it a second time after `MigratedDatabase`'s `migrate:fresh` already ran it once, asserting the row count and every capability set are unchanged. Designed the five templates' capability sets myself, since the plan names only the five role titles: Owner gets every capability except `tenants.manage` (platform-only concern); Event Manager gets the event and order-view/check-in surface without financial or membership capabilities; Box Office gets order view, event view, and check-in; Finance gets the two financially privileged capabilities plus order/event view; Check-in Agent gets only `checkin.scan` and `events.view`. Recorded here as a design decision open to revisit once task-08/09 build the role endpoints that make these sets user-visible.
- **`scope = platform` implies sentinel tenant invariant**: the plan says this is "enforced in the Action," but no Action creates real `Membership` rows until task-09 (`InviteUser`/`AssignRole`). Enforced now at the model layer instead: `Membership::assertScopeInvariant(MembershipScope $scope, string $tenantId)` is a pure, DB-free static method (throws `App\Identity\Exceptions\InvalidMembershipScopeException`, an `InvalidArgumentException` not implementing `HasErrorCode` since a violation is a programming bug, not user input, mirroring `App\Support\Tenancy\InvalidTenantIdException`), called from the model's `saving` event listener. `tests/Unit/Identity/MembershipScopeInvariantTest.php` exercises the static method directly, matching the plan's "Unit: ... the platform-scope invariant" line under Slice 3's own test list, landed here instead since the model exists now. Task-09's Actions inherit the guarantee automatically by going through `Membership::create()`/`save()`, without repeating the check.

**Deviation, investigated and resolved within this task, not deferred: `users` had no GRANT to `nodia_app`/`nodia_platform`.** Confirmed by direct psql probe against the running test database (`has_table_privilege('nodia_app', 'users', 'INSERT')` returned `false`) before writing any fixture code, per the CLAUDE.md verify-before-claiming rule. `memberships.user_id` is a real foreign key, and the isolation suite's connection is downgraded process-wide to the unprivileged `nodia_isolation` role (via `RlsHonestConnection`), so seeding a `User` row inside an isolation test's fixture requires `users` to be reachable under one of the two RLS group roles; without a grant, `nodia_isolation` has zero privilege on `users` under any role assumption, confirmed by a second probe (`probe_login`, a fresh unprivileged role granted membership in both group roles) against a throwaway RLS-protected table. `2026_07_09_000011_grant_users_table_to_rls_roles.php` adds `Rls::grantUnscoped('users')` (the same helper and justification already used for the Passport oauth tables: authentication infrastructure keyed to an identity, not tenant-scoped domain data, `users` carries no RLS at all per the plan). This is not purely a test-harness fix: task-09's `InviteUser` Action will need this same grant in production to create a new `users` row from inside a tenant-scoped `nodia_app` transaction, so landing it now, next to the migration that first requires it, avoids a retrofit later. Also empirically confirmed while investigating (not otherwise acted on, recorded for later stages' awareness): Postgres RLS `TO role` policies and plain table GRANTs are both satisfied by mere role *membership* with default `INHERIT`, with no `SET LOCAL ROLE` required; the codebase's explicit `SET LOCAL ROLE` pattern throughout Stage 1-3 does not add a privilege boundary beyond what persistent membership already grants, though it is unaffected here since `Rls::grantMembershipToCurrentUser()` only ever runs against the migration-time connection, not a persistent production runtime identity.

**Deviation: `Database\Factories\Identity\Models\{Role,Membership}Factory` cannot default `tenant_id` to `App\Tenancy\Models\Tenant::factory()`.** First attempt did exactly that (mirroring `TenantDomainFactory`'s own-context pattern) and failed `ContextBoundariesTest` (`Database\Factories\Identity` is not in that test's per-context factory exemption, only `Database\Factories\Tenancy` is exempt for `App\Tenancy\Models`). Both factories now omit a `tenant_id` default entirely, requiring every caller to pass a real tenant id explicitly; `App\Models\User` stays defaulted via `User::factory()` since it is not a bounded-context model (`App\Models`, not `App\{Context}\Models`) and so is unrestricted.

New test support: `tests/Isolation/Support/IdentityFixture.php` builds on `TenantFixture`'s two tenants with a multi-tenant staff member (membership in both tenants) and a single-tenant one, so the `memberships_self_read` isolation tests can prove a caller's own rows are visible across tenants while another user's rows never are, and that a tenant context on top of self-read only ever widens visibility (OR of two permissive policies), never narrows it. `actingAsRole()` in `tests/Isolation/Support/helpers.php` gained an optional fourth `$userId` parameter (backward compatible, existing call sites unaffected) that sets the second `app.user_id` `SET LOCAL` setting `memberships_self_read` matches against.

Isolation coverage: `tests/Isolation/RolesIsolationTest.php` (tenant isolation of custom roles in both directions; template roles readable from both tenant contexts and mutable from neither via `nodia_app`, including a forged-insert probe; `nodia_platform` reads and writes both templates and tenant-owned custom roles) and `tests/Isolation/MembershipsIsolationTest.php` (standard tenant deny matrix including a raw-SQL bypass probe; `nodia_platform` cross-tenant read; `nodia_platform` write denial with no tenant asserted, proving the absent platform-write policy; `memberships_self_read` returning exactly one user's rows across tenants and excluding another user's row, with and without a tenant context layered on top).

Test evidence: `composer -d apps/api run test` (all six suites) passed 475 tests, 1698 assertions, 0 failures on three consecutive full runs — up from task-03's 432/1629, i.e. 43 new tests and 69 new assertions (32 isolation across the two new files, 3 unit for `Capability`, 3 unit for the scope invariant, 5 unit for the template seeder, plus the Architecture suite's `PresetTest` updated for the two new ignored enum/exception classes). Also ran `php artisan test --testsuite=Isolation` standalone (71 tests, 152 assertions) and a manual `migrate:rollback --step=4` followed by `migrate` against the real test database to prove every new migration's `down()` is syntactically correct and reversible; both passed. `composer -d apps/api run lint` (Pint) passed after one auto-fix (an unused `use function` import in `IdentityFixture.php`, redundant because the file already declares that namespace). `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}`: the project's `TypeScriptTransformerServiceProvider` registers `EnumTransformer` globally, so both new enums (`Capability`, `MembershipScope`) are exported automatically without needing a `#[TypeScript]` attribute, even though no endpoint references them yet; committed with no drift on a second run.

Commit: `be9f520` (`feat(identity): memberships and roles schema with RBAC capability registry`), plus this journal entry.

#### task-05: Slice 3, activate membership validation in tenant resolution (2026-07-09)

Activates the Stage 2 deferral point named in the stage plan's own docblock (`ResolveTenantFromHeader`: "Stage 3 adds the membership check behind the same 403"). Wrote the failing resolution-matrix feature test first (`tests/Feature/Tenancy/TenantMembershipResolutionTest.php`), then the two new Unit test files for the invariants (`tests/Unit/Identity/ResolveTenantAccessTest.php`, and new cases appended to `tests/Unit/Tenancy/TenantTransactionTest.php`), then implemented to green; no new laravel-data Data classes or OpenAPI paths were needed (`CurrentUserData`/`MembershipData` and the `GET /v1/me` path already carried their full stage-endpoint-table shape since task-02, ahead of schedule, exactly so this task would only need to change what backs them, not their shape), so the "contract" step of the double loop was two OpenAPI description-text edits rather than a schema change.

**Auth wiring.** `TenancyServiceProvider`'s `tenancy.admin` middleware group changed from `[ResolveTenantFromHeader::class]` to `['auth:staff', ResolveTenantFromHeader::class]`: membership validation needs the caller's id, so Passport staff bearer authentication now runs ahead of header validation for every admin-group request. A missing or invalid bearer is rejected 401 before header shape is ever checked; header presence and shape validation (`missing_tenant_header`, `invalid_tenant_header`, both 400) still run first inside `ResolveTenantFromHeader` itself, ahead of tenant existence and membership, preserving the Stage 2 contract's exact codes and ordering for an authenticated caller. This is a real behavior change from Stage 2 (an unauthenticated request with a missing header now gets 401 instead of 400, since no admin routes existed yet to exercise this in Stage 2), noted as expected fallout of activating membership validation, not a deviation from the plan.

**Membership resolution.** New `App\Identity\Enums\MembershipAccessOutcome` (`Denied`, `TenantMember`, `PlatformMember`) and `App\Identity\Actions\ResolveTenantAccess`, called from `ResolveTenantFromHeader` rather than querying `memberships` there directly (CLAUDE.md: "a context never touches another context's models or tables directly"; `tests/Architecture/ContextBoundariesTest` only restricts `Models` and `Http`, not `Actions`, so this crossing is the sanctioned one). `MembershipAccessOutcome` is deliberately a pure, unbacked enum: `Spatie\TypeScriptTransformer\Transformers\EnumProviders\PhpEnumProvider::isValidUnion()` requires a backed enum, so the globally-registered `EnumTransformer` (task-04 journal) skips it instead of leaking an internal decision type into the generated TypeScript, confirmed by a no-diff `types:generate` run. The Action runs a single query (`memberships` filtered by `user_id` plus `(tenant_id = target AND scope = tenant) OR scope = platform`) and does none of its own `SET LOCAL` work; it depends entirely on the RLS context the caller already established, proven by a Unit test that omits `app.user_id` and shows a genuine platform-scope membership resolves to `Denied` instead of `PlatformMember`, since `memberships_self_read` is the only policy that could have surfaced it and it requires `app.user_id`.

**Ordering guarantee (Risks: "Membership validation ordering").** `ResolveTenantFromHeader`'s tenant transaction runs exactly two queries — the pre-existing `Tenant::query()->whereKey($header)->exists()` existence check, then `ResolveTenantAccess::forUser()` — before ever invoking the wrapped handler; `$handler()` (and therefore any query the real downstream endpoint would run) is unreachable until both have completed and access is granted. Pinned two ways: a Feature test with `DB::listen()` proving a marker query planted in a probe handler appears only after queries touching `"tenants"` and `"memberships"` in the query log, and a second Feature test proving the marker query never appears at all when access is denied (the handler closure is simply never called). `TenantTransaction::asTenant()` gained an optional `$userId` parameter (`SET LOCAL app.user_id` set immediately after `app.tenant_id`, before any caller code runs), backward compatible with the two pre-existing callers (`ResolveTenantFromHost`, the Stage 2 storefront resolver, passes none).

**Platform-scope fallback.** New `TenantTransaction::elevateToPlatformRole()`: called from inside the already-open tenant transaction when `ResolveTenantAccess` returns `PlatformMember`, it issues a second `SET LOCAL ROLE nodia_platform` (well-defined mid-transaction in Postgres) and re-enters `TenantContext` with the same tenant id under the new role, so the rest of the request proceeds as the platform role against the header's own tenant id, not the sentinel platform tenant `asPlatform()` uses. `TenantContext::isPlatform()` (already existing, from Stage 2) reports this correctly with no changes needed there. The tenant-existence check still runs before the membership lookup regardless of scope, so a platform-scope member presenting a header naming a tenant that does not exist is still denied, covered by a dedicated feature test; activity-log recording of this platform-role use is explicitly deferred to task-15 per the task description, not implemented here.

**`GET /v1/me` memberships.** New `App\Identity\Actions\ListOwnMemberships`, called from `CurrentUserController`, and a new `TenantTransaction::asAuthenticatedStaff()` posture (`nodia_app` role, `app.user_id` set, no `app.tenant_id`, no `TenantContext` entry). Fetching the membership rows themselves is one query under that posture (`memberships_self_read` alone answers it, since no tenant is asserted). Resolving each membership's `role_name` cannot reuse that same posture, though: `roles`' own RLS (`roles_template_or_tenant_read`: `tenant_id IS NULL OR tenant_id = app.tenant_id`) is tenant-scoped with no user-identity escape hatch analogous to `memberships_self_read`, so a caller's custom (non-template) role in a tenant they belong to is invisible without asserting that specific tenant. Resolved with one ordinary `asTenant($membership->tenant_id)` lookup per distinct tenant id, run sequentially after the self-only transaction has already committed rather than nested inside it: nesting was rejected because `SET LOCAL` inside a Postgres savepoint survives the savepoint's release and would bleed the per-tenant `app.tenant_id` into the outer self-only transaction (the same hazard `asDomainResolver`'s existing docblock records for its own posture), and `memberships.unique(user_id, tenant_id)` guarantees at most one such lookup per distinct tenant, never more. `Membership` gained `role()` and `user()` `BelongsTo` relations (`App\Models\User` is not a bounded-context model, per the task-04 journal, so referencing it here is unrestricted the same way `MembershipFactory` already does). `CurrentUserData::fromModel()` and `CurrentUserController` now take the resolved membership list instead of a hardcoded `[]`; covered by a new `GET /v1/me` feature test with two real memberships (one tenant-scope with a custom role, one platform-scope) asserting every `MembershipData` field, including the resolved `role_name` for both a per-tenant custom role and a global template role.

**Test churn from the auth-wiring change.** Every existing test that hit the `tenancy.admin` group without a bearer token needed a real staff token and (for cases that must still pass) a real membership: `tests/Feature/Tenancy/TenantResolutionTest.php` (the 5 admin rows moved out of its Stage 2 dataset test into the new file, since a bearer token cannot be a static dataset literal; the "never leaks tenant context" cross-group test kept its admin leg but gained a token and a tenant-scope membership), `tests/Feature/Tenancy/PlatformRoleAuditTest.php` ("admin header resolution (nodia_app)" case, proving no platform audit entry fires for an ordinary tenant-scope admin request), and `tests/Feature/Tenancy/TenancyRouteGroupsTest.php` (the `tenancy.admin` group-shape assertion). `tests/Architecture/PresetTest.php` needed `MembershipAccessOutcome::class` added to the Laravel preset's enum-location `ignoring()` list, the same treatment `Capability` and `MembershipScope` already got in task-04, since the preset otherwise expects every enum under `App\Enums`. New shared test helper `tests/Support/StaffTokens.php` issues a real access token through the actual `POST /v1/auth/staff/token` endpoint (mirroring a genuine client) rather than faking one, used by every new or updated test that needs a staff bearer.

**Deviation, investigated and fixed during test-writing, not a design change:** the first draft of every new test's cleanup wrapped `DB::table('memberships')->delete()` in `TenantTransaction::asPlatform()`, which failed with a dangling foreign-key violation on the next test's tenant or role deletion. Root cause: `memberships` carries no platform write policy (task-04 journal, "standard single-table policy" per the stage plan), so a delete issued as `nodia_platform` silently affects zero rows under RLS rather than erroring, leaving the row in place. Fixed everywhere by deleting memberships under `nodia_app` scoped to the specific tenant id each row belongs to (mirroring `Tests\Isolation\Support\IdentityFixture::clean()`'s existing pattern), including the sentinel platform tenant for platform-scope rows.

No isolation or concurrency coverage this task: no new tenant-scoped table (`memberships` and `roles` already got their isolation suite in task-04); no new invariant-guarding conditional UPDATE (membership validation is a read-only decision, not a state transition).

Test evidence: `composer -d apps/api run test` (all six suites) passed 492 tests, 1733 assertions, 0 failures on two consecutive full runs — up from task-04's 475/1698, i.e. a net 17 new tests and 35 new assertions (22 added: 11 feature in the new `TenantMembershipResolutionTest.php`, 5 unit in the new `ResolveTenantAccessTest.php`, 5 unit appended to `TenantTransactionTest.php`, 1 feature appended to `StaffAuthenticationTest.php`; 5 removed: the admin rows pulled out of `TenantResolutionTest.php`'s Stage 2 dataset test). `composer -d apps/api run lint` (Pint) passed after one auto-fix (`lambda_not_used_import` in `TenantTransactionTest.php`). `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` produced no diff against the committed generated TypeScript, confirmed on a second run, since `MembershipAccessOutcome` (the only new enum) is deliberately unbacked and no Data class shape changed.

Commit: `51a79b0` (`feat(identity): activate membership validation in tenant resolution`), plus this journal entry.

#### task-10: users MFA columns and mfa_recovery_codes migration, failing guard tests (2026-07-09)

Pure migration task per the plan (task breakdown item 10), and unlike every prior migration task in this run (task-04, task-12 pattern), this one is explicitly instructed to land in a red state: "written first and failing before the guard exists ... the guard goes green in task-11." Read literally against the master plan's double loop (step 1: "It fails because nothing exists") and test-first rule 2 (a failing concurrency test precedes the invariant-guarding transition it proves): task-10 ships the schema plus the failing tests for the not-yet-built consumption guard; task-11 is where those tests go green, not this one. This is a deliberate scope split recorded here, not an oversight or a fake-green shortcut (CLAUDE.md: "report failures as failures").

**Schema shipped**: two migrations, `2026_07_09_000015_add_mfa_columns_to_users_table.php` (adds `mfa_enabled` boolean not null default false, `mfa_secret` text nullable, `mfa_confirmed_at` timestamptz nullable, no new grant needed since task-04's `2026_07_09_000011_grant_users_table_to_rls_roles.php` already grants the whole `users` table) and `2026_07_09_000016_create_mfa_recovery_codes_table.php` (`id` uuid pk, `user_id` uuid fk `constrained()`, `code_hash` string, `used_at` nullable timestamptz, `timestampTz` created/updated, an explicit `index('user_id')` per the plan's own wording since `constrained()` alone does not index, `Rls::grantUnscoped('mfa_recovery_codes')`, mirroring `users` and the Passport oauth tables: authentication infrastructure keyed to a staff identity, not tenant-scoped domain data, so no isolation coverage is needed the same way `PassportMigrationsTest.php` needed none). `App\Identity\Models\MfaRecoveryCode` (plain Eloquent mapping, `user()` relation, `used_at` datetime cast, a factory under `Database\Factories\Identity\Models`) and `App\Models\User`'s cast list (`mfa_enabled` => boolean, `mfa_secret` => `encrypted`, `mfa_confirmed_at` => datetime) ship now too: these are schema-layer, not the guard, mirroring how task-04 shipped the `Membership`/`Role` models in its own migration task ahead of any Action using them.

**Deliberately not implemented this task: `App\Identity\Actions\ConsumeRecoveryCode` and `App\Identity\Support\RecoveryCodeHasher`.** Both are named and designed now (their exact call shape is pinned by the tests below) so task-11 has an unambiguous target, but neither file exists. Design pinned for task-11: `RecoveryCodeHasher::hash(string $plainCode): string` must be deterministic (unsalted), never a per-call-random hash like password hashing, because the plan's own consumption guard (`UPDATE mfa_recovery_codes SET used_at = now() WHERE user_id = ? AND code_hash = ? AND used_at IS NULL`, Data model "mfa_recovery_codes") looks a presented code up by an equality match on `code_hash`, which a random-salt hash could never satisfy twice for the same input; `ConsumeRecoveryCode` is a container-resolved Action with `__invoke(string $userId, string $plainCode): bool`, matching this codebase's established Action shape (`RevokeStaffSession`, `RevokeRefreshTokenFamily`) rather than a static call, and the conditional UPDATE's affected-row count is what the returned bool reports, never a read-then-write existence check first (master plan test-first rule 2; CLAUDE.md). The tests' own fixtures hash their plaintext fixture code directly with `hash('sha256', ...)` rather than through the not-yet-built `RecoveryCodeHasher`, pinning the exact algorithm task-11 must implement for `RecoveryCodeConsumptionTest.php` and the concurrency test to go green without themselves changing.

**Failing tests, three files, all referencing the two not-yet-built classes:** `tests/Unit/Identity/RecoveryCodeHasherTest.php` (determinism, distinctness across inputs, never returns the plaintext unchanged — 3 tests), `tests/Unit/Identity/RecoveryCodeConsumptionTest.php` (consumes a matching unused code exactly once and sets `used_at`; rejects a second attempt on an already-used code; rejects a wrong plaintext code; rejects a matching hash presented for a different user — 4 tests), and `tests/Concurrency/RecoveryCodeConsumptionContentionTest.php` (two parallel presentations of one recovery code resolve to exactly one winner — 1 test), mirroring `StaffRefreshRotationContentionTest.php`'s established pattern of calling production code directly inside `Tests\Concurrency\Support\ParallelRunner`'s forked workers (each worker inherits the already-booted application) rather than through an HTTP endpoint, since no endpoint consumes a recovery code until task-11 ships one.

**Verified, not assumed, that these 8 tests fail for the right reason and nothing else regresses.** The 4 `RecoveryCodeConsumptionTest.php` tests fail with `Target class [App\Identity\Actions\ConsumeRecoveryCode] does not exist.` (Laravel's container resolving the missing class), the 3 `RecoveryCodeHasherTest.php` tests fail with `Class "App\Identity\Support\RecoveryCodeHasher" not found` (a direct static-call miss), and the concurrency test fails a genuine assertion (`Failed asserting that actual size 0 matches expected size 1`): Spatie Fork does not propagate the forked worker's `BindingResolutionException` back to the parent as a thrown exception, it resolves to a falsy per-worker result instead, so `array_filter` on the two results yields zero winners rather than the one the guard will produce once built — confirmed this is a legitimate failure of the assertion itself, not a broken test, by inspecting the test's own logic: once `ConsumeRecoveryCode` exists and performs the real conditional UPDATE, exactly one of the two parallel calls will return `true`, and the assertion needs no change to go green in task-11. `composer -d apps/api run analyse` (Larastan) is unaffected by any of this: its `phpstan.neon` `paths` is `app` only, never `tests`, confirmed by reading the config before writing a single failing test, so referencing not-yet-built classes from test files cannot trip static analysis the way it would if they were referenced from `app`.

**New schema-proving coverage, all green, distinct from the guard tests above**: `tests/Unit/Identity/MfaSchemaTest.php` (6 tests) proves the migration itself against real Postgres (`information_schema.columns` types/nullability/defaults for the three `users` columns and all six `mfa_recovery_codes` columns, the `user_id` index via `pg_indexes`, `pg_class.relrowsecurity`/`relforcerowsecurity` both false, and the four-privilege grant to both RLS group roles, mirroring `PassportMigrationsTest.php`'s established structural-proof pattern for platform-global tables) plus one round-trip proof that `User::$mfa_secret`'s `encrypted` cast actually encrypts at rest (writes a known TOTP-shaped secret through the model, reads the raw column value directly via `DB::selectOne` and asserts it differs from the plaintext, then reloads the model and asserts the plaintext round-trips back).

Test evidence: `composer -d apps/api run test` (all six suites) reports 756 tests, 3053 assertions, 748 passed, 1 concurrency failure plus 7 unit errors — up from task-09's 742/3022, i.e. 14 new tests (6 in `MfaSchemaTest.php`, all passing; 3 in `RecoveryCodeHasherTest.php`, all failing as designed; 4 in `RecoveryCodeConsumptionTest.php`, all failing as designed; 1 in the new concurrency file, failing as designed), with the failing 8 confirmed identical across two consecutive full runs and unchanged after a manual `migrate:rollback --step=2` followed by `migrate` against the real test database to prove both new migrations' `down()` methods are syntactically correct and reversible. `composer -d apps/api run lint` (Pint) passed with no changes needed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors, confirmed unaffected by the guard tests' missing-class references per the reasoning above. `composer -d apps/api run types:generate` produced no diff against the committed generated TypeScript, confirmed by a direct `git status` check on `packages/api-client/src/generated`: no laravel-data Data class changed this task, since no endpoint ships until task-11.

Commit: `44f9f44` (`feat(identity): users MFA columns and mfa_recovery_codes schema`), plus this journal entry.

#### task-06: Slice 4a, capability Gate and Policy layer, authorization matrix harness (2026-07-09)

Built the failing Unit tests first for the two named invariants (`tests/Unit/Identity/ResolveActingMembershipTest.php`, `tests/Unit/Identity/CapabilityGateTest.php`), then the data-driven Feature test (`tests/Feature/Identity/AuthorizationMatrixTest.php`), then implemented to green, per the double loop. No new laravel-data Data class or OpenAPI path ships this task: the stage plan's endpoint table lists no route for task breakdown item 6 (role and membership endpoints arrive in tasks 8 and 9; the Stage 2 tenant CRUD rebinding that first attaches a capability check to a real endpoint is task 7, next), so the "contract" step is the one new `ErrorCode` case plus its `HasErrorCode` exception, not a schema change.

**`App\Identity\Actions\ResolveActingMembership`** is the new primitive: given a user id and an asserted tenant id, it resolves the `Membership` (with its `role` relation loaded) the user acts under, trying a tenant-scope row in the target tenant first and falling back to a platform-scope row only when none exists. It deliberately does none of its own `SET LOCAL` work, the same precedent `ResolveTenantAccess`'s docblock already recorded, relying entirely on the RLS context the caller (the tenant transaction) already established. `ResolveTenantAccess` (Slice 3, task-05) now delegates to it instead of duplicating the same lookup, mapping the resolved membership's `scope` to its own `MembershipAccessOutcome`; this is a pure refactor with no behavior change, proven by the pre-existing `ResolveTenantAccessTest.php` passing unmodified against the new implementation.

**`App\Identity\Authorization\CapabilityGate`** wires Laravel's real Gate: `register()` (called from `IdentityServiceProvider::boot()`) installs a `Gate::before` callback that resolves any ability matching a `Capability::tryFrom()` value by asking `ResolveActingMembership` for the acting membership under the request's asserted tenant (`App\Support\Tenancy\TenantContext`) and checking the resolved role's `capabilities` array; it returns `null` for any other ability name, per Laravel's documented `Gate::before` semantics (verified against current Laravel 13.x docs before writing the callback: a non-null return short-circuits, `null` leaves normal resolution untouched), so this is the single place capability resolution happens and future Policies (`TenantPolicy`, `RolePolicy`, `MembershipPolicy` — task breakdown items 7 through 9) can use the idiomatic `Gate::allows()`/`$user->can()` with a capability's wire value as the ability name. `authorize(Capability $capability): void` is the entry point controllers and Actions call directly; it maps a denial to `App\Identity\Exceptions\MissingCapabilityException` (`missing_capability`, 403) rather than Laravel's own `AuthorizationException`, matching every other domain exception in this codebase's `HasErrorCode` convention (`TenantAccessDeniedException`, `InvalidCredentialsException`, ...) instead of introducing a second error-mapping path through `ProblemRenderer`.

**Decision: no standalone Policy class ships this task.** The task description names "capability Gate and Policy layer" as a pair, but every concrete Policy (`TenantPolicy`, `RolePolicy`, `MembershipPolicy`) attaches to a model whose endpoints do not exist until task breakdown items 7 through 9; writing one now with no consumer would be speculative and risks drifting from what those tasks actually need. The "Policy layer" this task delivers is the mechanism those Policies will be built on: `Gate::before`'s registration means any future Policy method can simply `return Gate::allows(Capability::X->value)` (or a controller/Action can call `CapabilityGate::authorize()` directly, the pattern task 7 uses first since Tenancy's `TenantController` currently has no Policy at all). Recorded here as a deliberate scope decision, not an oversight.

**Authorization matrix harness.** `capabilityMatrixCases()` builds its dataset directly from `Capability::cases()` and `SeedTemplateRoles::templates()` (task-04), so it stays exhaustive as later stages extend the capability registry without any hand-maintained list to fall out of sync — the "single source" the stage plan says Stage 12's completeness check extends. Because no capability-gated production endpoint exists yet at this point in the task sequence, the test registers an ad hoc probe route inside the test file itself (`GET /v1/__probe/capability/{capability}` under the `tenancy.admin` middleware group), the same established pattern `tests/Feature/Tenancy/TenantMembershipResolutionTest.php` already uses for the Slice 3 resolution matrix, rather than shipping a permanent internal endpoint with no product caller. The probe calls `CapabilityGate::authorize()` and nothing else, so a 403 `missing_capability` is proven end to end through the real request pipeline (`auth:staff`, `ResolveTenantFromHeader`'s membership validation, then the Gate) and the exception handler, not by calling the Gate in isolation.

For every `Capability` case, against each of the five seeded templates plus one tenant-owned custom role (`Tenant Custom Probe`, holding `tenants.manage`, `events.view`, `orders.view` — deliberately including `tenants.manage`, since no template role carries it per task-04's design, so the matrix still exercises an allow path for it), the test asserts the home-tenant outcome matches whether the role's capability set contains the capability under test. A second, fixed membership under a different role (`Cross-Tenant Probe`, holding only `events.view`) in a foreign tenant proves the foreign-tenant outcome tracks that fixed role's own capability set regardless of what the home role was — capability evaluation is scoped to the asserted tenant's own membership, never leaked from a membership held elsewhere (system-design 5.3: "never role names"). 10 capabilities times 6 roles is 60 dataset rows, each asserting both legs, for 120 authorization decisions total.

**Deviation investigated and resolved, not a design change: implicit Laravel route-model-binding for backed enums does not fire on this route.** The first attempt type-hinted the probe closure's parameter as `Capability $capability` directly (per current Laravel 13.x routing docs on implicit Enum binding), which failed every request with "Target [App\Identity\Capability] is not instantiable." — a container-resolution error, not a 404, meaning `ImplicitRouteBinding::resolveBackedEnumsForRoute()` never ran at all. Root-caused by checking how the app's own `withRouting()` call in `bootstrap/app.php` registers routes and by comparing against `TenantController::show(string $tenant)`, which already takes a plain string and resolves manually rather than relying on implicit binding; the fix taken here matches that existing precedent instead of chasing the middleware-group root cause further, since the app's established convention is manual `Capability::from()` conversion inside the handler, not implicit binding, everywhere else in this codebase.

No isolation or concurrency coverage this task: no new tenant-scoped table (`memberships` and `roles` already got their isolation suite in task-04); no new invariant-guarding conditional UPDATE (capability resolution is a read-only decision, not a state transition).

**Deviation: `apps/api/phpunit.xml` gained `<ini name="memory_limit" value="512M"/>`.** The suite grew from 492 to 566 tests this task (mostly the 60-row, 120-HTTP-call authorization matrix, each Feature-suite request going through Spectator's OpenAPI schema validation), and `composer -d apps/api run test` reliably hit PHP's default 128M CLI `memory_limit` and died mid-run with a `Fatal error` from `nikic/php-parser` (Spectator's schema-validation dependency) on this machine's PHP CLI configuration, confirmed not a code defect by rerunning the exact same suite with `php -d memory_limit=1G vendor/bin/pest` directly (566/566 passed identically). Setting the limit in `phpunit.xml`'s `<php>` block is a repo-controlled fix that makes the mandated `composer test` command reliably green regardless of a developer machine's PHP CLI default, rather than requiring every contributor to edit their own global php.ini; CI is unaffected either way since GitHub Actions' PHP setup typically defaults to an effectively unlimited `memory_limit`.

Test evidence: `composer -d apps/api run test` (all six suites) passed 566 tests, 2173 assertions, 0 failures on three consecutive full runs — up from task-05's 492/1733, i.e. 74 new tests and 440 new assertions (6 unit in the new `ResolveActingMembershipTest.php`, 7 unit in the new `CapabilityGateTest.php`, 60 feature in the new `AuthorizationMatrixTest.php`'s data-driven test, 1 unit appended to `ErrorCodeTest.php` for the new `missing_capability` code). `composer -d apps/api run lint` (Pint) passed after one auto-fix (import ordering in `CapabilityGateTest.php`). `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for the `missing_capability` addition to the generated `ErrorCode` union, committed with no drift on a second run.

Commit: `0ef045f` (`feat(identity): capability gate and authorization matrix`), plus this journal entry.

#### task-07: Stage 2 alias rebinding, Passport bearer plus tenants.manage (2026-07-09)

Closes the Stage 2 deferral named in `TenancyServiceProvider`'s own docblock and in `PlatformAuthPlaceholder`'s: the `tenancy.platform` middleware group (tenant and domain CRUD, `platform.php`) now runs `auth:staff` (Passport bearer), then `PlatformRequestTransaction` (unchanged: sentinel tenant, `nodia_platform` posture, the Stage 2 audit seam), then a new `RequireCapability::class.':tenants.manage'` entry that calls Identity's `CapabilityGate::authorize()`. `PlatformAuthPlaceholder` (the pass-through denying everything outside testing/local) and its alias registration are deleted outright, not deprecated.

Built the failing feature tests first (`tests/Feature/Tenancy/PlatformCapabilityAuthorizationTest.php`, a data-driven matrix over all eight Stage 2 routes proving `auth.unauthenticated` with no bearer, `missing_capability` with a bearer lacking `tenants.manage`, and success with one that holds it — the "authorization matrix dataset" the task breakdown item names, extending Identity's existing `AuthorizationMatrixTest.php` coverage of the `tenancy.admin` surface to this platform-only surface), then no new contract shapes were needed (no laravel-data class changes: the capability check is a pure authorization gate, not a new endpoint or field) so the "contract" step was OpenAPI-only — `security: [{BearerAuth: []}]`, a `401` (`UnauthenticatedProblem`, already existed) and a `403` (new `MissingCapabilityProblem` schema) response added to all eight `/v1/tenants*` and `/v1/tenant-domains*` operations in `docs/openapi/openapi.yaml` — then the failing unit test for the new middleware (`tests/Unit/Tenancy/RequireCapabilityTest.php`, driven directly the way `CapabilityGateTest.php` drives `CapabilityGate`), then implemented `App\Tenancy\Http\Middleware\RequireCapability` to green.

**Middleware ordering, not a controller-level check.** Task-06's journal had flagged "a controller/Action can call `CapabilityGate::authorize()` directly, the pattern task 7 uses first" as the anticipated approach. Implemented as a middleware instead, a deliberate deviation: `TenantController` and `TenantDomainController` together have eight actions that would each need the identical `CapabilityGate::authorize(Capability::TenantsManage)` call repeated verbatim, whereas the existing `tenancy.platform` group is already built from one middleware per cross-cutting concern (`PlatformRequestTransaction` for the transaction posture, the deleted placeholder for auth); a fourth middleware entry keeps that precedent instead of introducing a second enforcement style alongside it, and the group is deliberately capability-agnostic (`RequireCapability` takes the capability as a route-middleware parameter, `:tenants.manage`, not a hardcoded constant) so a future platform-only endpoint needing a different capability does not need a new middleware class. `RequireCapability` lives under `App\Tenancy\Http\Middleware` rather than `App\Identity\Http\Middleware`: `tests/Architecture/ContextBoundariesTest` forbids any context from using another context's `Http` classes, so `TenancyServiceProvider` could not reference an Identity-owned middleware; it references `App\Identity\Authorization\CapabilityGate` and `App\Identity\Capability` instead (neither is under `Identity\Http` or `Identity\Models`), the same sanctioned crossing `ResolveTenantFromHeader` already uses for `App\Identity\Actions\ResolveTenantAccess`. Ordering is load-bearing: `RequireCapability` runs after `PlatformRequestTransaction` in the group array (outer-to-inner middleware wraps `$next`, confirmed by reading `TransactsRequests::transactRequest` before writing any code), so `TenantContext` already carries the sentinel platform tenant, and `Auth::guard('staff')` already has the authenticated user, by the time the capability check runs — `CapabilityGate::allowsFor()` returns `false` unconditionally when `TenantContext::hasTenant()` is false, so the reverse order would deny every request regardless of capability.

**No template role carries `tenants.manage`** (task-04 journal: "Owner gets every capability except tenants.manage, a platform-only concern"), so every test needing a capable bearer creates a dedicated custom role scoped to the sentinel platform tenant and a platform-scope membership (`scope: platform`, `tenant_id`: sentinel, via `Membership::factory()->platform()`) holding it. Factored into a new shared test helper, `tests/Support/PlatformStaff::token(Capability $capability = Capability::TenantsManage)`, issuing a real bearer through the actual `/v1/auth/staff/token` endpoint the way `tests/Support/StaffTokens.php` already does, reused across the new tests and every pre-existing test this task touched.

**Test churn from the auth-wiring change**, all fallout from activating real authentication on a surface that previously ran unauthenticated in tests, not new invariants: `TenancyRouteGroupsTest.php` (group-shape assertion, the platform-posture probe, the auth/capability denial-and-pass probes, the rollback probe — all now issue and pass a `PlatformStaff::token()`), `PlatformRoleAuditTest.php` (the ten-row `platform crud requests` dataset and the "no bearer" and "carries the generated correlation id" cases now carry a capable bearer; the Stage 2 "denies with 401 outside testing/local" case, which used an `app()->detectEnvironment()` hack the deleted placeholder needed, is replaced by a genuine "no bearer" 401 case, plus a new case proving a `missing_capability` denial still emits the audit entry, since `PlatformRequestTransaction`'s audit fires on role assumption, before the capability check runs, per its own docblock, "auth denials excluded" — a capability denial is not an auth denial, the platform role was genuinely assumed), `TenantEndpointsTest.php` and `TenantDomainEndpointsTest.php` (a `beforeEach` now calls `$this->withHeaders(['Authorization' => ...])` once per test with a `tenants.manage` bearer, which Laravel's test helpers merge into every subsequent request in that test method, so the ~44 existing assertions in both files needed no per-call changes), `PlatformEndpointsIsolationTest.php` and `PlatformDomainEndpointsIsolationTest.php` (same bearer addition; the isolation suite's downgraded `nodia_isolation` connection role has no bearing on this, since Passport token issuance and validation are ordinary queries, not RLS-scoped tenant data), and `TenantDomainContentionTest.php` (the two forked-worker races now issue one `PlatformStaff::token()` bearer in the parent process before forking and pass it into each worker closure via `HTTP_AUTHORIZATION`, mirroring `StaffRefreshRotationContentionTest`'s existing pattern of logging in before the fork barrier). `tests/Contract/DocumentedResponseCoverageTest.php` gained a `contractPlatformBearer(Capability $capability = Capability::TenantsManage)` helper (fixed emails via `firstOrCreate`, mirroring `contractStaffUser()`, so ~50 dataset iterations reuse one row instead of colliding) and 16 new exercisers (`401`/`403` for all eight operations) alongside header additions to the 20 pre-existing tenant/domain exercisers.

**Genuine blocker hit and resolved within this task, not deferred: a cross-suite foreign-key violation, not a design flaw.** The first full `composer test` run after all files above were green individually failed `StaffRefreshRotationContentionTest` (Concurrency suite, runs last) with `update or delete on table "users" violates foreign key constraint "memberships_user_id_foreign"` on its own blanket `User::query()->delete()`. Root-caused (not guessed) by checking `phpunit.xml`'s testsuite order (Feature, Unit, Contract, Architecture, Isolation, Concurrency, one shared process, `MigratedDatabase::ensure()` never resets between suites) against which files leave a `users` row with a live `memberships` reference: every Feature/Isolation/Concurrency file that calls `PlatformStaff::token()` already deletes its own memberships and users in its own `afterEach` (added deliberately, following this codebase's existing per-file cleanup convention), but `contractPlatformBearer()`'s two fixed-email users, reused deliberately across the Contract suite's ~50 dataset rows, were never cleaned up at all, so their platform-scope memberships outlived the whole Contract suite and blocked the later Concurrency suite's unrelated blanket delete. Fixed by adding membership and non-template-role cleanup to `DocumentedResponseCoverageTest.php`'s existing `afterEach` (which already deleted tenants and domains every test) while deliberately keeping the two `User` rows themselves un-deleted, matching `contractStaffUser()`'s own precedent of a reused row with no memberships attached once cleaned — the membership is idempotently recreated on the next dataset row's call, matching this file's other fixture helpers (`contractTenant()`, `contractDomain()`), which already recreate their rows every test. Confirmed fixed by two consecutive full `composer test` runs (605/605 both times) after the fix, plus running the previously-failing Concurrency file alone.

No isolation or concurrency coverage of a new invariant this task: no new tenant-scoped table, and the two pre-existing `TenantDomainContentionTest` races needed only a bearer added, not new races (the conditional-UPDATE guards they prove are Stage 2's, unchanged here).

Test evidence: `composer -d apps/api run test` (all six suites) passed 605 tests, 2372 assertions, 0 failures on two consecutive full runs — up from task-06's 566/2173, i.e. 39 new tests and 199 new assertions (2 unit in the new `RequireCapabilityTest.php`, 24 feature in the new `PlatformCapabilityAuthorizationTest.php`, 3 feature appended to `TenancyRouteGroupsTest.php` replacing 2 removed Stage 2 environment-based cases net +1, 1 feature appended to `PlatformRoleAuditTest.php`, plus the Contract suite's data-driven coverage test picking up 16 new documented responses and their exercisers). Also ran `tests/Concurrency/TenantDomainContentionTest.php` and `tests/Isolation/Platform*IsolationTest.php` standalone and three times each to check for flakiness from the new forked-worker bearer plumbing; all passed identically every time. `composer -d apps/api run lint` (Pint) passed after one auto-fix (`no_unused_imports` in `tests/Support/PlatformStaff.php`, a leftover `MembershipScope` import made redundant by using the factory's `platform()` state instead). `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` produced no diff against the committed generated TypeScript, confirmed on a second run: no laravel-data Data class changed this task (the capability check is pure authorization, no new wire shape), and `MissingCapabilityProblem` is an OpenAPI-only schema with no laravel-data counterpart, matching the precedent every other `*Problem` schema in `docs/openapi/openapi.yaml` already sets.

Commit: `4f00f5f` (`feat(identity): rebind platform route group to Passport bearer plus tenants.manage`), plus this journal entry.

#### task-08: Slice 4b, role endpoints and GET /v1/capabilities (2026-07-09)

Built the failing feature tests first (`tests/Feature/Identity/RoleEndpointsTest.php`, `tests/Feature/Identity/CapabilitiesEndpointTest.php`), then the contract (`RoleData`, `CreateRoleData`, `UpdateRoleData`, `CapabilityData`, `CapabilityListData`, the new OpenAPI paths and schemas), then the failing unit test for the new model invariant (`tests/Unit/Identity/RoleCapabilityInvariantTest.php`), then implemented to green, per the double loop. The isolation denial probe (`tests/Isolation/RoleEndpointsIsolationTest.php`) was written alongside the feature tests, ahead of the controller, per the master plan's isolation-first rule for anything proving an RLS-dependent guarantee.

Endpoints shipped: `GET /v1/roles` (query-builder allowlist, `filter[name]` and `sort` in `name`/`-name`), `POST /v1/roles`, `GET /v1/roles/{role}`, `PATCH /v1/roles/{role}`, `DELETE /v1/roles/{role}`, all under the `tenancy.admin` group; `GET /v1/capabilities` under bearer-only auth (no `X-Tenant-Id`, mounted alongside `GET /v1/me` in `Identity/Http/routes/auth.php`). Reading roles needs only a valid tenant membership (no capability check: `roles_template_or_tenant_read` RLS from task-04 already scopes the result set to global templates plus the caller's own tenant's custom roles); mutating requires `roles.manage`, enforced the same way task-07 wired `tenants.manage` on the platform group.

**Deviation, decided before writing any endpoint code, not discovered mid-task: `RequireCapability` had to move out of `App\Tenancy\Http\Middleware`.** Task-07 built it there, reasonably, since it was that task's only consumer. This task's routes are Identity's own, and `tests/Architecture/ContextBoundariesTest` forbids any context from using another context's `Http` classes (the same rule task-07's own journal already navigated once, for the reverse direction, when it kept `RequireCapability` from reaching into `Identity\Http`). A capability-gate middleware two contexts both need cannot live in either one's `Http` namespace, so it now lives at `App\Http\Middleware\RequireCapability`, the same app-global location `App\Http\Middleware\CorrelationId` already established as precedent (not under any of the eight bounded contexts, so `ContextBoundariesTest` does not apply to it at all). Moved with `git mv`, one import fixed in `TenancyServiceProvider`, and its dedicated unit test moved from `tests/Unit/Tenancy/` to `tests/Unit/` unchanged otherwise; the full suite (605 tests, the task-07 baseline) was re-run green before writing any task-08 code, to isolate this refactor from the task's own changes.

**Decision: `GET /v1/capabilities` is bearer-only, not under `tenancy.admin`, despite the stage plan's endpoint table heading grouping it under "Roles and memberships (staff bearer plus `X-Tenant-Id`...)".** The table's own error-code column for this row lists only `auth.unauthenticated`, with no `tenant_access_denied` or header-shape codes the way the `GET /v1/roles` row explicitly lists both — read as intentional, not an omission, since the Capability registry is a static enum, not tenant-scoped data, and asserting a tenant to read it would be pure friction with no access-control purpose. Mounted next to `GET /v1/me` instead, the codebase's existing precedent for a bearer-only, tenant-free read.

**Decision: capabilities-array validity is a model invariant, not laravel-data validation.** `Role::assertKnownCapabilities()` is a pure, DB-free static method called from the model's `saving` event listener, exactly mirroring `Membership::assertScopeInvariant`'s precedent from task-04: both `CreateRole` and `UpdateRole` inherit the guarantee automatically by going through `Role::create()`/`update()`, without repeating the check, and `unknown_capability` (422) stays a distinct code from `request.validation_failed` because the payload shape is valid (a list of strings) even when a value in it is not a real capability. `CreateRoleData`'s `capabilities` rule is `present` rather than `required`: Laravel's `required` rule fails an empty array, but a role with no capabilities yet (to be granted later through `PATCH`) is a valid create payload; a first attempt at a feature test asserting exactly this (`role_name_taken`, `capabilities: []`) failed with `request.validation_failed` instead, caught before any implementation of the fix.

**Decision: `role_in_use` is detected via the `memberships.role_id` foreign key violation, not a read-then-write existence check.** `memberships.role_id` already carries a foreign key with no cascade (task-04's migration), so Postgres itself refuses a role delete while a membership still references it; `DeleteRole` catches the `23503` (`foreign_key_violation`) SQLSTATE the same way `RegisterDomain` (Stage 2) catches `23505` via `UniqueConstraintViolationException` for `role_name_taken`/`domain_already_registered`, except no Laravel-native subclass exists for foreign-key violations, so the SQLSTATE is checked directly on the base `QueryException`. This satisfies CLAUDE.md's "never read-then-write" rule for invariant-guarding transitions without a dedicated concurrency test: the constraint is Postgres's own atomic guarantee, not application-level locking, so there is no race to prove the way Stage 2 and 3's conditional-`UPDATE`-by-affected-row-count guards needed one.

**Blocker investigated and resolved within this task, not deferred: Larastan flagged the `QueryException` catch in `DeleteRole` as dead code (`catch.neverThrown`), but not the structurally identical `UniqueConstraintViolationException` catch in the new `CreateRole`/`UpdateRole`.** Root-caused by a throwaway probe file, not guessed: `Illuminate\Support\Facades\DB::table('roles')->where(...)->delete()` (a facade-dispatched call PHPStan cannot fully resolve statically) is accepted without complaint, while `$role->delete()` (a concretely-typed `Illuminate\Database\Eloquent\Model::delete()` call, whose own signature carries no `@throws QueryException`) is confidently analyzed as never throwing it. `Model::create()`/`update()`, called through the same magic/facade-proxied path as `DB::table()`, get the same benefit of the doubt Larastan extends to calls it cannot fully resolve, which is presumably why the `UniqueConstraintViolationException` catches elsewhere in this codebase (Stage 2's `RegisterDomain`) were never flagged. Fixed by performing the delete through `DB::table('roles')->where('id', $role->id)->delete()` instead of `$role->delete()`, which is also arguably the more correct form regardless: it is one direct statement rather than relying on Eloquent's model-event pipeline for a delete whose only guard is a database constraint.

**Isolation denial probe design.** `tests/Isolation/RoleEndpointsIsolationTest.php` proves a foreign-tenant role id in the URL renders `request.not_found`, not `403`, for `GET`, `PATCH`, and `DELETE`. The caller needs a genuine tenant-scope membership, not `Tests\Support\PlatformStaff`: a platform-scope membership resolves under `nodia_platform`, whose `roles_platform_write` policy (task-04) sees every tenant's roles regardless of the asserted tenant, which would make the probe vacuous (confirmed by reasoning through the RLS policy set before writing the test, not by an initial false pass). The `PATCH`/`DELETE` legs need the caller's own-tenant role to hold `roles.manage`, or `RequireCapability` would turn the request away before `RoleController::roleOrFail()` is ever reached, which would prove the wrong thing (403 vs 404 for the wrong reason).

**Generic 404 exception.** `RoleNotFoundException` maps to `ErrorCode::RequestNotFound` (the existing generic code), not a new `role_not_found` code: the stage plan's endpoint table lists `request.not_found` for `GET /v1/roles/{role}`, unlike `TenantNotFoundException`'s own dedicated `tenant_not_found` code, and the two causes (genuinely nonexistent id, and a foreign tenant's custom role `roles_template_or_tenant_read` RLS already hides) render identically on purpose, matching `TenantController`'s own `?? throw ...Exception` idiom (not `abort(404)`, kept consistent with every other not-found lookup in this codebase) rather than a bare `abort()` call.

**`GET /v1/capabilities` wraps its list in an object, not a bare array.** `tests/Contract/ResponseSchemaStrictnessTest.php` (Stage 1, ADR 019) requires every documented response schema to declare `additionalProperties: false`, which only an object schema can carry; the first draft (`type: array, items: $ref Capability`, matching the endpoint table's literal "list of CapabilityData" wording) failed that gate immediately. `CapabilityListData` (`{ data: CapabilityData[] }`) is the fix: consistent with every other list response in this API already being an object (a paginator envelope), just without pagination metadata since the registry is small and static.

No concurrency coverage this task: the stage plan's Slice 4 test list names no concurrency guard for role CRUD, and `role_in_use`'s FK-violation guard (above) is Postgres's own atomic constraint, not an application-level race the way Stage 2/3's conditional-`UPDATE` guards are.

Test evidence: `composer -d apps/api run test` (all six suites) passed 672 tests, 2717 assertions, 0 failures on two consecutive full runs — up from task-07's 605/2372, i.e. 67 new tests and 345 new assertions (32 feature in the new `RoleEndpointsTest.php`, 2 feature in the new `CapabilitiesEndpointTest.php`, 1 isolation in the new `RoleEndpointsIsolationTest.php`, 3 unit in the new `RoleCapabilityInvariantTest.php`, plus the Contract suite's data-driven coverage test picking up 25 new documented responses and their exercisers). `composer -d apps/api run lint` (Pint) passed with no changes needed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors after the `DeleteRole` fix above. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for the five new Data classes and the four new `ErrorCode` union members, committed with no drift confirmed by a second run (after recovering from a self-inflicted `git stash`/`stash pop` mishap while checking for drift, which briefly desynced tracked source from untracked new files; caught before committing, the stash was popped cleanly and the recovered state re-verified fully green rather than trusted).

Commit: `1ab2485` (`feat(identity): role CRUD endpoints and GET /v1/capabilities`), plus this journal entry.

#### task-09: Slice 4c, membership endpoints, InviteUser, AssignRole, invitation acceptance (2026-07-09)

Built the failing feature tests first (`tests/Feature/Identity/MembershipEndpointsTest.php`, `tests/Feature/Identity/InvitationAcceptanceTest.php`), then the contract (`InviteUserData`, `ChangeMembershipRoleData`, `AcceptInvitationData`, `MembershipData::fromModel()`, the three new OpenAPI paths and their schemas), then the failing unit tests per invariant (`tests/Unit/Identity/InvitationTokenTest.php`, `tests/Unit/Identity/InviteUserActionTest.php`, `tests/Unit/Identity/LastOwnerGuardTest.php`), then implemented to green, per the double loop.

Endpoints shipped: `GET /v1/memberships` (query-builder allowlist, `filter[user_id]`/`filter[role_id]` exact match, `sort` in `created_at`/`-created_at`), `POST /v1/memberships`, `PATCH /v1/memberships/{membership}`, `DELETE /v1/memberships/{membership}`, all under the `tenancy.admin` group (mirroring task-08's `roles.php` route file exactly); `POST /v1/auth/staff/invitation/accept`, unauthenticated, mounted in `Identity/Http/routes/auth.php` next to the token and refresh endpoints. Reading needs only a valid tenant membership; mutating requires `memberships.manage`, wired through the same `App\Http\Middleware\RequireCapability` task-08 already moved to the app-global namespace.

**Invitation token is a stateless sealed token, not a new table.** `App\Identity\Support\InvitationToken::issue()`/`verify()` wraps `Illuminate\Support\Facades\Crypt::encryptString()`/`decryptString()` around a JSON payload (`user_id`, `expires_at`), rather than a database row with a hash and a `used_at` column the way the stage plan's staff password reset (task breakdown item 16) is expected to need. This was a deliberate reading of the plan's own error code registry for this endpoint: only `invitation_token_invalid` and `invitation_token_expired` are listed, no `invitation_token_reused` the way reset tokens will need one, so no single-use tracking is required and a stateless design (the encrypter's own HMAC gives the "signed" and tamper-evident property the plan's language asks for; the embedded expiry, checked against `Date::now()` so it honors the Stage 1 fake clock) is simpler and needs no migration. `config('identity.invitation_token_ttl_minutes')`, defaulting to 7 days (`STAFF_INVITATION_TOKEN_TTL_MINUTES`), is a new explicit, config-driven, testable value, the same kind of undetermined-duration decision task-01's journal already made for the refresh token TTL.

**`InviteUser` and `AssignRole` need no `DB::transaction()` of their own.** Confirmed by reading `App\Tenancy\Http\Middleware\ResolveTenantFromHeader` before writing either Action: the whole admin request already runs inside one transaction opened by `TenantTransaction::asTenant()`, which wraps the entire `$handler()` call, so every state mutation a controller action performs during that request is already atomic with every other one in the same request. This is the "single transaction with an obvious Stage 4 outbox attachment point" the Domain events section asks for; each Action marks the point with a comment (`Outbox::record(UserInvited::class, ...)` / `Outbox::record(UserRoleChanged::class, ...)`) rather than adding a redundant nested transaction, mirroring how task-08's `CreateRole`/`UpdateRole` also needed no wrapper for their own single writes.

**`InviteUser`'s mail send is deferred to `DB::afterCommit()`, not sent inline.** The request-level transaction covers the user-lookup-or-create and the membership insert, so sending the invitation email from inside the Action (called mid-transaction) would mail a token for a membership that might still roll back later in the same request. `Illuminate\Database\Concerns\ManagesTransactions::afterCommit()` (confirmed reachable via the `DB` facade: `db.transactions` is bound unconditionally in `DatabaseServiceProvider`, and `DatabaseManager::connection()` calls `setTransactionManager()` on every resolved connection, not just under the testing traits) schedules the `Mail::to(...)->send(...)` call to run synchronously the instant the enclosing transaction actually commits, satisfying the plan's "no queue dependency" mandate without the inline-send race. Verified working both through real HTTP requests (the middleware's own transaction) and through direct Action calls inside `TenantTransaction::asTenant()` in the unit suite (also a real transaction), since `Mail::fake()`'s captured mailables were asserted in both.

**`StaffInvitationMail` lives under `App\Identity\Mail`, not the top-level `App\Mail` the Pest Laravel preset expects.** Investigated before writing the class: that preset also requires every class in `App\Mail` to implement `ShouldQueue`, and `Illuminate\Mail\Mailer::sendMailable()` checks that interface and routes a plain `send()` call through `->queue()` instead of `->send()` when it is present, the opposite of what the plan's "no queue dependency" mandate needs. Rather than force `ShouldQueue` and call `sendNow()` everywhere to defeat it, the class stays under `App\Identity\Mail` and is added to `tests/Architecture/PresetTest.php`'s `ignoring()` list, the same treatment `Identity\Http\Controllers` and `Identity\Models` already get there.

**`last_owner_removal` is a shared trait, not duplicated logic.** `App\Identity\Support\GuardsLastOwner::assertNotLastOwner()`, used by both `AssignRole` (only when the new role id differs from the current one, so reassigning an Owner to the same role is a no-op that never trips the guard) and `RemoveMembership`. "Owner" means a membership whose `role_id` resolves to the global template row named "Owner" (`tenant_id` null), the one `SeedTemplateRoles` seeds, not any tenant-chosen custom role that happens to share the name. The guard's own `DB::transaction()` plus `lockForUpdate()` on the remaining-Owner count is a defensive measure with no dedicated concurrency test: the stage plan's TDD sequencing section lists a "Concurrency (first for the guard)" step for Slices 2, 5, 6, and 8 only, not Slice 4's last-owner invariant, the same reading task-08's `role_in_use` guard already relied on for its own database-constraint-only guard. Investigated and confirmed correct rather than assumed: a `SELECT ... FOR UPDATE` row lock taken inside a Postgres savepoint (the trait's own nested `DB::transaction()` call, since it always runs inside the already-open request-level transaction) persists for the life of the *enclosing* real transaction, not just the savepoint, so the lock still covers the subsequent role-change or delete that runs after the trait's own nested transaction returns.

**`GET /v1/memberships` and the membership-by-id lookup both add an explicit `where('tenant_id', ...)` beyond what RLS alone provides.** Reasoned through before writing the controller, not discovered by a failing test: `memberships_self_read` (task-04) is a second permissive SELECT policy that widens visibility to the caller's own membership rows in *every* tenant, independent of the asserted one. Postgres ORs multiple permissive policies together, so a bare `Membership::query()` under an asserted tenant would still let a caller's own membership rows in *other* tenants leak into "list memberships in this tenant" (`index()`), or let a caller's own foreign-tenant membership id resolve via `find()` in `PATCH`/`DELETE`'s `membershipOrFail()` even though the base tenant policy (which self-read never widens for non-SELECT commands, per task-04's own journal) would then silently affect zero rows on the actual mutation. The explicit filter, ANDed with RLS's permissive OR, narrows both back down to exactly the asserted tenant. `tests/Feature/Identity/MembershipEndpointsTest.php`'s "lists memberships in the acting tenant only" and "renders request.not_found for a foreign-tenant membership id" cases exercise this directly.

**`Spatie\QueryBuilder\Concerns\FiltersQuery::allowedFilters()` is variadic, not array-typed**, unlike the mental model carried over from `RoleController`'s single-argument call; caught immediately by the first feature test run (`TypeError`), fixed by spreading the two `AllowedFilter::exact()` calls as separate arguments rather than an array. A second, Larastan-only issue surfaced after that fix: `QueryBuilder::for(Membership::class)->where(...)->allowedFilters(...)` loses its `QueryBuilder` static return type through PHPStan's inference of the intermediate `->where()` call (typed as the parent `Illuminate\Database\Eloquent\Builder`, which has no `allowedFilters()` method), while `RoleController`'s call chain never has a plain `Builder` method between `for()` and `allowedFilters()`. Fixed by reordering: every `QueryBuilder`-specific call (`allowedFilters()`, `allowedSorts()`, `defaultSort()`) now runs immediately after `for()`, with the explicit tenant `where()` afterward, which both satisfies Larastan and reads more naturally as "declare the allowlist, then apply this endpoint's own scoping."

No isolation coverage this task: no new tenant-scoped table (`memberships` and `roles` already got their isolation suite in task-04); the foreign-tenant denial probes (mirroring task-08's `RoleEndpointsIsolationTest.php` pattern) are feature-test cases in `MembershipEndpointsTest.php` and `InvitationAcceptanceTest.php` instead, since they exercise the same `tenancy.admin` HTTP surface those files already cover, and adding a third file for exactly two probes would have split otherwise-identical setup code for no isolation-suite-specific reason (no `nodia_isolation` role downgrade is needed here, unlike task-04's own isolation suite). No concurrency coverage this task, for the reason recorded above under the `GuardsLastOwner` note.

Test evidence: `composer -d apps/api run test` (all six suites) passed 742 tests, 3022 assertions, 0 failures on two consecutive full runs — up from task-08's 672/2717, i.e. 70 new tests and 305 new assertions (20 feature in the new `MembershipEndpointsTest.php`, 6 feature in the new `InvitationAcceptanceTest.php`, 6 unit in the new `InvitationTokenTest.php`, 5 unit in the new `InviteUserActionTest.php`, 7 unit in the new `LastOwnerGuardTest.php`, plus the Contract suite's data-driven coverage test picking up 22 new documented responses and their exercisers, and the pre-existing `ErrorCodeTest.php` updated for the four new codes). `composer -d apps/api run lint` (Pint) passed with no changes needed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors after the `MembershipController` query-builder ordering fix above. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for the three new Data classes and the four new `ErrorCode` union members, committed with no drift confirmed by a second run.

Commit: `8c7e44f` (`feat(identity): membership endpoints, InviteUser, AssignRole, invitation acceptance`), plus this journal entry.

#### task-11: Slice 5, MFA endpoints, staff token-exchange challenge, enforcement middleware (2026-07-10)

Built the failing feature tests first (`tests/Feature/Identity/MfaEnrollmentTest.php`, `tests/Feature/Identity/MfaDisableTest.php`, `tests/Feature/Identity/MfaEnforcementTest.php`, `tests/Feature/Identity/StaffMfaTokenChallengeTest.php`), then the contract (`ConfirmMfaData`, `DisableMfaData`, `MfaEnrollmentData`, `MfaRecoveryCodesData`, the three new `/v1/auth/mfa/*` OpenAPI paths, plus the `mfa_code` addition to the existing staff token-exchange path), then the failing unit tests per invariant (`tests/Unit/Identity/TotpVerifierTest.php`, `tests/Unit/Identity/MfaEnforcementPolicyTest.php`, on top of task-10's already-failing `RecoveryCodeHasherTest.php`/`RecoveryCodeConsumptionTest.php`/`RecoveryCodeConsumptionContentionTest.php`), then implemented to green, per the double loop. This task resumed and finished a session that had been interrupted after the code was substantially written but before the green-suite run, lint/analyse/types-generate pass, and this journal entry; every file was re-read and the full suite re-run twice from a clean state before trusting any of it, rather than assuming the prior session's uncommitted work was correct.

**Endpoints shipped**: `POST /v1/auth/mfa/enrollment` (`EnrollMfa`, generates and persists an unconfirmed TOTP secret, returns the secret plus an `otpauth://` URI), `POST /v1/auth/mfa/enrollment/confirm` (`ConfirmMfaEnrollment`, verifies a code against the pending secret, flips `mfa_enabled`/`mfa_confirmed_at`, and returns a fresh batch of recovery codes in plaintext exactly once), `POST /v1/auth/mfa/disable` (`DisableMfa`, denied with `mfa_enforced_for_role` before the code is even checked when any of the caller's memberships anywhere mandates MFA). All three sit in `Identity/Http/routes/auth.php`'s existing `auth:staff` group, deliberately never behind `tenancy.admin` or `tenancy.platform`, so an enforcing-but-unconfirmed caller can always reach enrollment regardless of `X-Tenant-Id`.

**The Risks paragraph's question resolved in the plan's preferred direction: `mfa_code` parameter injection into the existing token exchange, no two-step `mfa_token` fallback needed.** `App\Identity\Actions\IssueStaffToken` now verifies the email/password pair once, manually, with `Hash::check` against the same bcrypt digest Passport's own `UserRepository` checks, before calling `App\Identity\Actions\VerifyMfaChallenge` and only then driving Passport's `AuthorizationServer`; this way a wrong `mfa_code` never mints and then discards a real token pair, and unknown-email/wrong-password still renders identically (`invalid_credentials`) regardless of whether the account has MFA enabled, so enrollment status is never leaked to a caller who fails on credentials alone. `VerifyMfaChallenge` is a no-op for a non-MFA account, throws `mfa_required` when `mfa_code` is omitted, and accepts either a valid TOTP code or a still-unused recovery code (consumed atomically via task-10's `ConsumeRecoveryCode`), both collapsing to `mfa_code_invalid` on failure with no way for a client to distinguish "wrong TOTP" from "wrong or already-used recovery code," mirroring the no-enumeration posture `InvalidCredentialsException` already established for the password leg. No contract freeze issue ever materialized, so no `mfa_token` response shape needed to be designed or documented.

**`App\Identity\Support\MfaEnforcementPolicy` is the single, pure, DB-free evaluation of "does this membership mandate confirmed MFA"** (platform-scope, or a role capability set intersecting the financially privileged set), shared by two different callers with two different scopes: `App\Http\Middleware\EnforceMfaCompliance` (the single acting membership under one asserted tenant, attached to both the `tenancy.admin` and `tenancy.platform` route groups in `TenancyServiceProvider`, right after the middleware that opens the tenant transaction and before `RequireCapability` on the platform group, so the denial fires uniformly across every route including capability-free reads) and `App\Identity\Actions\HasEnforcingMembership` (every membership the caller holds across every tenant, for `POST /v1/auth/mfa/disable`, which has no `X-Tenant-Id` to scope by since MFA is a bearer-only namespace). Task-07's platform route group is fully enforced as of this task: `MfaEnforcementTest.php` proves an unconfirmed platform-scope caller is denied `mfa_enforcement_required` on both `GET /v1/roles` and `GET /v1/tenants` while still reaching MFA enrollment, and a tenant-scope caller whose role holds `orders.refund` gets the identical denial and identical enrollment access, matching the Risks paragraph's two named scenarios exactly; a non-privileged tenant-scope caller is let through without confirmed MFA. `Tests\Support\PlatformStaff` (used throughout the existing platform-group feature suites since task-07) now mints its staff with MFA pre-confirmed, and one `AuthorizationMatrixTest.php` case (Owner/Finance rows, which carry a financially privileged capability) needed the same pre-confirmation added so that suite keeps proving capability evaluation in isolation from this task's new enforcement layer, rather than incidentally failing on it.

**`App\Identity\Support\TotpVerifier` derives its period counter from `Illuminate\Support\Facades\Date::now()`, never from `Google2FA`'s own default wall-clock read.** `Google2FA::verifyKey()` defaults to `microtime(true)` when no timestamp is passed, which the Stage 1 fake clock (`freezeTime()`/`travelTo()`) cannot control; every call here instead passes an explicit `timestamp` computed from the framework clock, the same one `tests/Architecture/TimeSourceTest.php` already requires app code to use, so `TotpVerifierTest.php`'s fake-clock unit tests (exact-period, one-period-past, one-period-future within Google2FA's default window, well-outside-window, and a code that never matches) can travel the clock and get exactly the pass/fail Google2FA's own window algorithm predicts, rather than an unpredictable result racing real time. `pragmarx/google2fa` (`^9.0`) is a new Composer dependency, added to `composer.json`/`composer.lock`.

**Recovery codes are 16 characters of `Str::random` entropy in four dash-grouped blocks** (`RecoveryCodeGenerator`), hashed with the unsalted, deterministic `sha256` task-10 already pinned (`RecoveryCodeHasher`), returned in plaintext only from `ConfirmMfaEnrollment`'s response and never again. `config('identity.mfa_recovery_code_count')` (`MFA_RECOVERY_CODE_COUNT`, defaulting to 8) is a new explicit, config-driven value for a count the plan does not mandate, the same kind of undetermined-value decision task-01 and task-09 already made for other TTLs; 8 mirrors the common GitHub/Google-authenticator-ecosystem default. Re-confirming a fresh enrollment (after a second `EnrollMfa` call replaced the pending secret) first deletes any recovery codes left over from an earlier confirmation, so stale codes can never remain valid against a secret they were never issued for.

**Task-10's deliberately-failing guard tests go green with no change to the tests themselves**, exactly as that task's journal predicted: `RecoveryCodeHasherTest.php`, `RecoveryCodeConsumptionTest.php`, and `tests/Concurrency/RecoveryCodeConsumptionContentionTest.php` (two parallel presentations of one recovery code resolve to exactly one winner, via a real conditional `UPDATE ... WHERE ... used_at IS NULL` checked by affected-row count) now pass against this task's `RecoveryCodeHasher` and `ConsumeRecoveryCode`, which is the "goes green against the task-10 concurrency test" success condition this task's own brief names.

No new tenant-scoped table this task (no isolation coverage needed): `mfa_recovery_codes` got its isolation-equivalent structural proof in task-10's `MfaSchemaTest.php` as platform-global authentication infrastructure, the same precedent the Passport oauth tables already set.

Test evidence: `composer -d apps/api run test` (all six suites) passed 805 tests, 3261 assertions, 0 failures on two consecutive full runs — up from task-10's 756 reported (748 passing, 8 failing as designed), i.e. a net 49 new tests and 208 new assertions (7 feature in the new `MfaEnrollmentTest.php`, 6 feature in the new `MfaDisableTest.php`, 4 feature in the new `MfaEnforcementTest.php`, 5 feature in the new `StaffMfaTokenChallengeTest.php`, 5 unit in the new `TotpVerifierTest.php`, 5 unit in the new `MfaEnforcementPolicyTest.php`, the 8 guard tests task-10 shipped failing now passing unchanged, plus the Contract suite's data-driven coverage test picking up the new documented MFA responses and their exercisers, and the pre-existing `ErrorCodeTest.php` updated for the six new codes). `composer -d apps/api run lint` (Pint) passed with no changes needed. `composer -d apps/api run analyse` (Larastan, level 5) passed with 0 errors. `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/{index.ts,typescript-transformer-manifest.json}` for the four new Data classes and the six new `ErrorCode` union members, committed with no drift confirmed by a second run.

Commit: `03773bb` (`feat(identity): MFA endpoints, token-exchange challenge, enforcement middleware`), plus this journal entry.
