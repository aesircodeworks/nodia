# Stage 12 Execution Journal: Compliance, Operations, Hardening

## Run: 2026-07-13

- Stage: 12 (docs/plans/stage-12-hardening.md)
- Date: 2026-07-13
- Branch: feat/api-implementation
- Base commit: a6cd0cd1e9493cf555789c7681befa0e572f3e71

### Pre-run verification

Nothing from this stage has landed. Verified against the codebase, not the docs:

- No `data_subject_requests` or `archive_segments` migration; the migration set has no `payload_pruned_at` column on `gateway_webhook_events` (`2026_07_11_000040_create_gateway_webhook_events_table.php`).
- `customers.anonymized_at` exists from Stage 3 (`2026_07_09_000017_create_customers_table.php`), but no `AnonymizeCustomer` Action, no `CustomerAnonymized` event class, and no reference to it anywhere outside the column itself.
- No `customers.erase` or `customers.export` capability in the Identity capability registry.
- No `config/retention.php`; the config set ends at `tenancy.php` with no retention windows anywhere.
- No archival, pruning, `outbox:replay-failed`, `payments:reconcile`, or `holds:release-stuck` commands. `app/Console/Commands` holds the earlier-stage commands only (`ReplayOutboxCommand`, `SweepOutboxCommand`, `ReconcilePendingPaymentsCommand`, `ReleaseExpiredHoldsCommand`, `ReportingRebuildCommand`, and the payments/search/gateway helpers).
- `phpunit.xml` declares six suites (Feature, Unit, Contract, Architecture, Isolation, Concurrency); there is no `Smoke` suite.
- No `infra/load/` directory (only `infra/compose`) and no `docs/load-targets.md`.
- No coverage-completeness meta-tests, no `env()` architecture test, no secret scanner in CI.

Dependencies are in place: Stage 3 (customers with `anonymized_at`, guest claim, activity log, `RevokeAllUserTokens` and the refresh-token family revocation primitives), Stage 4 (outbox recording, `outbox_deliveries` idempotence, `OutboxReplay` with its stability window, `failed_jobs`), Stages 5a through 9 (the full publish, hold, order, payment, refund, scan path the smoke suite drives, plus `gateway_webhook_events` and the reconcile and hold-sweeper paths the operational commands wrap), Stage 10 (waiting room admission, shipped in full, so the third load scenario is not omitted), and Stage 11 (read models for the `CustomerAnonymized` scrub, and `BuildExport`'s cursor-paginated `ExportSource` pattern plus the Orders, Payments, and CheckIn read Actions it added, which the data subject export assembler reuses).

Stage 8d is still In progress and gated on the launch-gateway ADR. Per the stage plan's Scope section, dispute ingestion stays deferred and nothing here blocks on it; the webhook negative matrix (T15) covers registered gateways, so it picks up the 8d adapter only if it has merged by then.

### Task checklist

- [x] T1 `data_subject_requests` migration, model, enums, conditional status transitions (identity)
- [x] T2 `AnonymizeCustomer`, `CustomerAnonymized`, erasure endpoint, registry update (identity, docs)
- [x] T3 Orders `CustomerAnonymized` consumer scrubbing `attendee_name` (orders)
- [x] T4 Reporting `CustomerAnonymized` consumer (reporting)
- [x] T5 Data subject export assembler and queued job (identity)
- [x] T6 Export download URL plus list and show endpoints (identity)
- [x] T7 `config/retention.php`, webhook payload pruner, export attachment pruner, scheduler (payments, identity)
- [x] T8 Activity log scoped delete path plus system-design 14.2 amendment (support, docs)
- [x] T9 `archive_segments` migration and activity log archive-then-prune command (support)
- [x] T10 Outbox archiver and archive-aware replay (support)
- [x] T11 `outbox:replay-failed` command (support)
- [x] T12 `payments:reconcile` command (payments)
- [x] T13 `holds:release-stuck` command with the sweeper-race concurrency test (inventory)
- [x] T14 Coverage completeness, authorization matrix, and payload PII meta-tests (support)
- [x] T15 Webhook negative matrix across registered gateways (payments)
- [x] T16 Secret scanner in CI, `env()` architecture test, `.env.example` assertions (ci) - partial, scanner dropped at operator direction, see the entry
- [ ] T17 Smoke suite and required CI gate (support, ci)
- [ ] T18 Load tooling, `infra/load/` scenarios, `docs/load-targets.md` (ci, docs)

### Review rounds

### Decisions and deviations

## T1: data_subject_requests table, model, enums, conditional status transitions

2026-07-13 15:15 -03

Landed stage-12 plan Data model "data_subject_requests" (task breakdown item 2): the schema, the model's conditional-UPDATE transitions, and the two new capabilities the later endpoint and command tasks gate on. No Data class, no endpoint, no OpenAPI path in this task (per the task's own scope), so the double-loop's contract step doesn't apply here; task 2 (`AnonymizeCustomer`, the erasure endpoint) carries the first contract step for this table.

What landed:

- `database/migrations/2026_07_13_000060_create_data_subject_requests_table.php`: UUIDv7 PK, `tenant_id`, real FK `customer_id` (not nullable, unlike `holds.customer_id`: a data subject request always names a real customer), `type` and `status` as plain strings backed by the new enums (the `orders.status`/`OrderStatus` posture, no database check constraint), real FK `requested_by_user_id` into `users` (mirroring `exports.requested_by_user_id`), nullable `completed_at`, an index on `(tenant_id, customer_id)` for the admin list filter, a partial unique index `data_subject_requests_open_per_customer_idx` on `(customer_id, type) where status in ('pending', 'processing')` (custom name per data-conventions' index-naming rule, mirroring `event_signing_keys_active_event_idx`'s own precedent for a builder-inexpressible index), and `Rls::applyTenantPolicies('data_subject_requests')` in the same migration (no platform-write policy: the plan names cross-tenant platform reads only).
- `App\Identity\Enums\DataSubjectRequestType` (`erasure`, `export`) and `App\Identity\Enums\DataSubjectRequestStatus` (`pending`, `processing`, `completed`, `failed`), neither marked `#[TypeScript]` yet: no Data class exposes them on the wire until task 2, mirroring `ExportType`/`ExportStatus`'s own precedent (stage-11 T10) of a bare backed enum with no wire attribute until something types it directly.
- `App\Identity\Models\DataSubjectRequest`: `HasUuids`, `HasFactory`, `#[Fillable]`, enum casts for `type`/`status`, `datetime` cast for `completed_at`. Two static transition methods, each a single conditional UPDATE guarded on the current status and checked by affected-row count, never read-then-write: `claim()` (pending to processing, the exactly-one-worker guard) and `complete()` (processing to completed, stamps `completed_at`); a third, `fail()` (processing to failed), does not stamp `completed_at`, mirroring `App\Reporting\Models\Export::fail()`'s own exact precedent of leaving its terminal timestamp unset on the failure edge. Placed on the model itself rather than a dedicated Action class, the same reasoning `Export`'s own docblock gives: no caller exists yet (tasks 2 and 5 are the future callers).
- `Database\Factories\Identity\Models\DataSubjectRequestFactory`: `tenant_id`, `customer_id`, and `requested_by_user_id` have no default, mirroring `ExportFactory`; defaults to `erasure`/`pending` with no `completed_at`.
- `App\Identity\Capability`: added `CustomersErase = 'customers.erase'` and `CustomersExport = 'customers.export'`, additive at the end of the enum, neither marked financially privileged (not money-shaped).
- Template role seeds decision (explicit, since the task names no specific role): granted both new capabilities to Owner only, not Event Manager, Box Office, Finance, or Check-in Agent. Reasoning: erasure and export are destructive/PII-sensitive compliance actions gated behind the highest-privilege template, distinct from the broader `customers.view` grant (Owner, Box Office, Finance) that only reads customer records; nothing in the stage-12 plan text names a second role, so the narrower grant is the minimal reading, and the two new tests below make the choice explicit rather than implicit, so a later task can widen it deliberately if a feature test demands it. Owner's automatic "every capability except tenants.manage" invariant (`tests/Unit/Identity/SeedTemplateRolesTest.php`, pre-existing) required the new capabilities on Owner's own list to keep passing, independent of this choice.
- `tests/Isolation/Support/DataSubjectRequestFixture.php` and `tests/Isolation/DataSubjectRequestsIsolationTest.php`: one open (pending) request per tenant, built on `CustomerFixture`; own-row visibility, cross-tenant select/update/delete all affecting zero rows, a foreign `tenant_id` insert rejected by `WITH CHECK`, a raw-SQL query still isolated, `nodia_platform` cross-tenant read, `nodia_platform` write rejected (no platform-write policy), and an insert violating the partial unique index rejected — the requesting staff users are created inline under `nodia_platform`, mirroring `CheckInFixture`'s own posture for `user_id`.
- `tests/Unit/Identity/DataSubjectRequestClaimTest.php`: the pending-to-processing claim wins exactly once and a second claimant gets zero affected rows (the task's named acceptance case), plus `complete()` stamping `completed_at`, `complete()` refusing an unclaimed (still-pending) row, and `fail()` succeeding without stamping `completed_at`.
- `tests/Unit/Identity/CapabilityTest.php` and `tests/Unit/Identity/SeedTemplateRolesTest.php`: extended the exact-registry list and the not-financially-privileged dataset with the two new capabilities; added a new assertion that Owner alone carries `customers.erase`/`customers.export` and the other four templates carry neither.
- `tests/Architecture/PresetTest.php`: added `DataSubjectRequestType::class` and `DataSubjectRequestStatus::class` to the Laravel preset's `ignoring()` list (the preset expects backed enums only under `App\Enums`; every other context's own status/type enum is already exempted the same way, e.g. `MembershipScope`, `ExportType`), with a docblock note following the file's own running commentary convention. No exemption needed for `DataSubjectRequest` itself: `'App\Identity\Models'` was already on the list from Stage 3.

Test evidence:

- Confirmed red first: moved the migration, model, enums, and factory files aside and reverted the `Capability`/`SeedTemplateRoles` edits, then ran the new Isolation and Unit files. Isolation failed with `Class "App\Identity\Models\DataSubjectRequest" not found`; the Unit/Capability/SeedTemplateRoles files failed to load (`Capability::CustomersErase`/`CustomersExport` undefined, breaking the `->with([...])` dataset resolution). Restored all files and re-applied the two registry edits.
- Green after implementation: `./vendor/bin/pest tests/Isolation/DataSubjectRequestsIsolationTest.php tests/Unit/Identity/DataSubjectRequestClaimTest.php tests/Unit/Identity/CapabilityTest.php tests/Unit/Identity/SeedTemplateRolesTest.php tests/Unit/Identity/RoleCapabilityInvariantTest.php` — 40 tests, 40 passed, 74 assertions.
- Full-suite regression per the task ("scoped run of the new Isolation and Unit files only" plus the touched shared-file blast radius): `./vendor/bin/pest --testsuite=Isolation` — 354 tests, 354 passed; `./vendor/bin/pest --testsuite=Unit` — 1191 tests, 1191 passed; `php artisan test --testsuite=Architecture` — 40 tests, 40 passed (after the `PresetTest` enum-location fix above, first run failed on `DataSubjectRequestStatus.php` not being ignored).
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed file: both pass with zero errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run (nothing to regenerate).

Deviation from the plan: none in scope. One judgment call recorded above (Owner-only template grant for the two new capabilities, since the task text names no specific role).

## T2: AnonymizeCustomer, CustomerAnonymized event, erasure endpoint

2026-07-13 15:52 -03

Landed stage-12 plan Slice 1's erasure branch and task breakdown item 3 in full: `AnonymizeCustomer`, the `CustomerAnonymized` event and payload, the registry update, and `POST /v1/customers/{customer}/data-subject-requests` with its contract and problem codes. Export (Slice 2) is out of scope for this task per the plan's own task breakdown ("erasure endpoint") and per Slice 1's test list, which names only erasure; the route and its Data classes are shaped so Slice 2 (task 6) is additive, not a rewrite.

What landed:

- `App\Identity\Support\AnonymizationPlaceholder`: HKDF-keyed derivation of the placeholder name and email from the customer id and the application secret, mirroring `App\Orders\Support\DerivedTicketSigningKeyProvider`'s own base64-key-handling precedent exactly. Deterministic (same customer id, same placeholder), non-reversible (keyed on a secret the placeholder never carries), and per-tenant unique (customer ids are globally unique, so the derived email can never collide against the `customers (tenant_id, email)` constraint).
- `App\Identity\Events\CustomerAnonymized` and `CustomerAnonymizedPayload`: envelope per event-conventions, aggregate `customer`, payload carrying `customer_id` and `data_subject_request_id` only, `#[Hidden]` from TypeScript generation like `CustomerRegisteredPayload`. Registered in `IdentityServiceProvider::boot()`'s `EventTypeRegistry`.
- `App\Identity\Actions\AnonymizeCustomer`: the whole erasure core in one conditional UPDATE (`anonymized_at is null`) checked by affected-row count, never read-then-write; overwrites `name`/`email` with the placeholder, nulls `password`, stamps `anonymized_at`. Zero affected rows throws `CustomerAlreadyAnonymizedException` (409 `customer_already_anonymized`) before any token revocation or outbox recording happens. On success, revokes every live access and refresh token via `RevokeAllUserTokens` (it already covers both, since a live access token's own refresh token is revoked alongside it — `RevokeRefreshTokenFamily` is Stage 3's reuse-detection primitive for one family, not needed here) and records exactly one `CustomerAnonymized` event in the same transaction.
- `App\Identity\Actions\CreateDataSubjectRequest`: creates the `data_subject_requests` row (pending), claims it (pending to processing) via the T1 model transitions, runs `AnonymizeCustomer`, then completes it. The partial unique index violation on a concurrent open request maps to `DataSubjectRequestAlreadyOpenException` (409 `data_subject_request_already_open`) by index name, mirroring `CreateRole`'s own precedent. No explicit `fail()` path: the whole request runs inside the ambient tenant transaction `App\Tenancy\Http\Middleware\ResolveTenantFromHeader` opens (confirmed by reading `TransactsRequests`/`TenantTransaction::run()`), so any thrown exception rolls back everything this call wrote, including the just-inserted request row — "a failed request records nothing" holds structurally, not by extra bookkeeping.
- `App\Identity\Data\CreateDataSubjectRequestData` and `DataSubjectRequestData`: `type` validates against `erasure` only for now via `Rule::in()` (mirroring `CreateExportData`'s registered-types restriction), even though the property itself is typed to the full `DataSubjectRequestType` enum for forward compatibility; `download_url` is always `null` until Slice 2 attaches a file. `DataSubjectRequestType`/`DataSubjectRequestStatus` marked `#[TypeScript]` now that a Data class exposes them.
- `App\Identity\Http\Controllers\DataSubjectRequestController` and the `POST /v1/customers/{customer}/data-subject-requests` route in `customers.php`: the capability required (`customers.erase` vs `customers.export`) depends on the request body's own `type`, so this route carries no static `RequireCapability` middleware entry; the controller calls `CapabilityGate::authorize()` directly after the Data object resolves (422 for an unaccepted type fires first, automatically, since laravel-data validation runs during controller-method dependency resolution, before any of the controller's own body executes). `RecordActivityAudit` still applies at the route level since it only logs a response that actually succeeded. Response status tracks the resulting request status (201 once `completed`, 202 otherwise) rather than being hardcoded, so Slice 2's `pending` export path needs no controller change.
- Two new `ErrorCode` cases (`customer_already_anonymized`, `data_subject_request_already_open`, both 409) and their `CustomerAlreadyAnonymizedException`/`DataSubjectRequestAlreadyOpenException`; a third exception, `CustomerNotFoundException`, maps the customer route param's 404 to the generic `request.not_found` code, mirroring `RoleNotFoundException`/`MembershipNotFoundException`.
- `App\Identity\Actions\ClaimGuestAccount` fix: an anonymized customer also carries a null password (erasure nulls it), so without an explicit `anonymized_at !== null` check the existing password-only guard would let a still-valid claim token re-arm a supposedly erased account with a real credential. Rendered as `claim_token_invalid`, identical to a missing customer, per the plan's own instruction that the response must not reveal the account was ever erased; no new error code introduced. Added a Unit case to the existing `ClaimGuestAccountTest.php` alongside the Feature-level proof.
- `docs/system-design.md` 9.3: added `CustomerAnonymized` to the Identity event list (list item 2 in the actual document; the stage-12 plan text calls this "group 1," a label mismatch against the document's own numbering, not acted on further — the substantive instruction, adding the event to Identity's line, is what landed).
- `docs/openapi/openapi.yaml`: the new path plus `CreateDataSubjectRequestRequest`, `DataSubjectRequest`, and `DataSubjectRequestConflictProblem` (one schema combining both 409 codes via a shared `code` enum, mirroring `ExportDownloadConflictProblem`'s own precedent, since the coverage test keys exercisers by status only). `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/index.ts` (also picked up `Capability::CustomersErase`/`CustomersExport`, a stale-generation gap left over from T1's own no-Data-class-changed reasoning, since `Capability` was already transitively exported before this task).
- `tests/Contract/DocumentedResponseCoverageTest.php`: one exerciser per documented status (201, 401, 403, 404, 409, 422) plus the `data_subject_requests` cleanup line the new table's own tenant-scoped rows need ahead of the `customers` delete.
- `tests/Architecture/PresetTest.php`: the three new exception classes added to the Laravel preset's Throwable-location `ignoring()` list, the same way every other context exception is exempted.

Test evidence:

- New/changed files run in isolation: `./vendor/bin/pest tests/Feature/Identity/DataSubjectErasureTest.php tests/Feature/Identity/CustomerAnonymizedOutboxTest.php tests/Unit/Identity/AnonymizationPlaceholderTest.php tests/Unit/Identity/CustomerAnonymizedPayloadTest.php tests/Unit/Identity/ClaimGuestAccountTest.php tests/Unit/Identity/DataSubjectRequestClaimTest.php tests/Isolation/DataSubjectRequestsIsolationTest.php tests/Concurrency/CustomerAnonymizationContentionTest.php tests/Unit/Problems/ErrorCodeTest.php --testsuite=Contract` — 158 tests, 158 passed, 601 assertions.
- `php artisan test --testsuite=Architecture` — 40 tests, 40 passed (after adding the three new exceptions to `PresetTest`'s ignoring list; first run failed with "not to implement Throwable" on `DataSubjectRequestAlreadyOpenException`).
- `./vendor/bin/pest --testsuite=Isolation` — 354 tests, 354 passed (regression check; no new isolation surface this task, T1 already covers `data_subject_requests`).
- `./vendor/bin/pest tests/Feature/Identity tests/Unit/Identity` — 500 tests, 500 passed.
- `./vendor/bin/pest --testsuite=Contract` (full) — 472 tests, 472 passed.
- Given the number of shared files touched (`ErrorCode`, `PresetTest`, `DocumentedResponseCoverageTest`, `IdentityServiceProvider`, `customers.php` routes), ran the whole suite once as an extra regression check beyond what the method calls for: `./vendor/bin/pest` — 3433 tests, 3433 passed, 13642 assertions, ~7m46s.
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed PHP file: both pass with zero errors.
- `composer -d apps/api run types:generate` regenerated `packages/api-client/src/generated/index.ts` and the manifest; `pnpm --filter api-client typecheck` passes against the regenerated output.

Deviation from the plan: none in scope. Three judgment calls recorded above: `CreateDataSubjectRequestData.type` restricted to `erasure` only until Slice 2 lands export (the plan's own Endpoints section describes the union of both slices; Slice 1's test list names only erasure); `ClaimGuestAccount`'s anonymized-customer rejection reuses `claim_token_invalid` rather than a new code (the plan names no third code for that endpoint); the system-design 9.3 "group 1" label mismatch noted but not corrected, since the plan's substantive instruction (add the event to Identity's own line) is unambiguous regardless of the label.

## T3: Orders CustomerAnonymized consumer scrubbing ticket attendee_name

2026-07-13 16:00 -03

Landed stage-12 plan Domain events "Consumed" and task breakdown item 4: the Orders subscriber for Identity's `CustomerAnonymized`. No contract step in this task's TDD double loop: it is an internal outbox consumer with no endpoint and no Data class, the same shape as `CancelOrderOnHoldExpired` (Stage 7) and `HandlePaymentConfirmed` (Stage 8a), so only the duplicate-delivery Feature test and the implementation apply.

What landed:

- `App\Orders\Jobs\ScrubTicketAttendeeNames`: an `OutboxSubscriber` that overwrites `attendee_name` with a constant placeholder (`Erased Attendee`) on every ticket whose `order_id` belongs to an order for the anonymized customer, via one UPDATE scoped by a subquery over `orders.customer_id` and `whereNotNull('attendee_name')` (a ticket that never carried a name is left alone rather than stamped with the placeholder). No per-customer HKDF derivation like `AnonymizationPlaceholder`: `attendee_name` carries no uniqueness constraint, so a single shared constant satisfies "naturally idempotent" without inventing per-row uniqueness nobody needs. Reads and writes only Orders' own `orders`/`tickets` tables (event-conventions: a consumer never writes to another context's tables); the tenant scope comes for free from `ProcessOutboxDelivery` already running the handler inside `TenantTransaction::asTenant()`, so no extra scoping call was needed in the handler itself.
- `App\Orders\OrdersServiceProvider::boot()`: registered `ScrubTicketAttendeeNames::NAME` against `['CustomerAnonymized']` in the `SubscriberRegistry`, alongside the existing Orders subscribers.
- `tests/Feature/Orders/CustomerAnonymizedConsumerTest.php`, mirroring `HoldExpiredConsumerTest.php`'s structure: subscriber-registration assertion; a scrub test proving a named ticket is overwritten with the placeholder while a null-named ticket on the same order stays null; a duplicate-delivery test running the same outbox event id through `ProcessOutboxDelivery::handle()` twice and asserting both the final `attendee_name` and exactly one `processed` row in `outbox_deliveries`; a cross-boundary test with a second customer in the same tenant and a customer in a second tenant, both with named tickets, proving neither is touched by the first customer's anonymization. The `CustomerAnonymized` event itself is produced by driving the real `AnonymizeCustomer` Action inside an explicit `DB::transaction()` (needed here since, unlike the HTTP path, there is no ambient `TransactsRequests` middleware transaction), the same "drive the real producer" posture `tests/Feature/Payments/LedgerProjectionTest.php` uses for `PaymentConfirmed` rather than hand-rolling an outbox row.

Test evidence:

- Confirmed red first: ran the new test file before `ScrubTicketAttendeeNames` existed. All four cases failed with `Class "App\Orders\Jobs\ScrubTicketAttendeeNames" not found`.
- Green after implementation: `./vendor/bin/pest tests/Feature/Orders/CustomerAnonymizedConsumerTest.php` — 4 tests, 4 passed, 8 assertions.
- `php artisan test --testsuite=Architecture` (the task's named context-boundary guard) — 40 tests, 40 passed.
- Scoped regression per the task ("plus the Architecture suite files that guard the context boundary"), widened to the whole touched directory since `OrdersServiceProvider` is shared: `./vendor/bin/pest tests/Feature/Orders tests/Unit/Orders` — 194 tests, 194 passed, 613 assertions.
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on the two changed `app/` files: both pass with zero errors. (`phpstan.neon`'s `paths` covers `app` only, not `tests`; running phpstan directly against the test file produces `Pest\PendingCalls\TestCall` false positives on every `$this->tenantId`-style dynamic property, confirmed as pre-existing noise by reproducing the identical error shape against the untouched `HoldExpiredConsumerTest.php`, not a defect introduced here.)
- No Data class changed, so `composer -d apps/api run types:generate` was not run (nothing to regenerate).

Deviation from the plan: none. The consumer scrubs to a fixed constant rather than a per-customer derived value (unlike `AnonymizationPlaceholder` for the customer row itself); this is a judgment call, not a deviation, since the plan's own idempotence reasoning ("overwriting a placeholder with the same placeholder is a no-op") only requires a stable value, and `attendee_name` has no uniqueness constraint for a derived value to satisfy.

## T4: Reporting CustomerAnonymized consumer

2026-07-13 16:04 -03

Landed stage-12 plan Domain events "Consumed" and task breakdown item 5: the Reporting subscriber for Identity's `CustomerAnonymized`. Inspected Stage 11's three read models before writing anything (`report_daily_sales`, `report_event_finance`, `report_event_attendance` migrations and their owning models): every column across all three is `tenant_id`/`event_id`/`ticket_type_id` (identifiers), a count, a money `*_amount` column, `sales_date`/`first_scan_at`/`last_scan_at` (dates/timestamps), or `currency`/`created_at`/`updated_at`. No name, email, or any other customer-identifying field was denormalized. This forced the no-op shape the plan names as acceptable ("If Stage 11's read models carry no PII, the subscriber is still registered with a no-op assertion test"), not the scrubbing shape Orders (T3) landed.

What landed:

- `App\Reporting\Jobs\ScrubReportingPii`: an `OutboxSubscriber` whose `handle()` body is intentionally empty (see class docblock), registered against `CustomerAnonymized` all the same, so a future PII-carrying read model inherits the registration, the outbox plumbing, and the duplicate-delivery guarantee already in place, needing only an implementation.
- `App\Reporting\ReportingServiceProvider::boot()`: registered `ScrubReportingPii::NAME` against `['CustomerAnonymized']` in the `SubscriberRegistry`, alongside the three existing Reporting projectors.
- `tests/Feature/Reporting/CustomerAnonymizedConsumerTest.php`, mirroring `tests/Feature/Orders/CustomerAnonymizedConsumerTest.php`'s structure for the parts that carry over: subscriber-registration assertion; a duplicate-delivery test running the same outbox event id through `ProcessOutboxDelivery::handle()` twice and asserting exactly one `processed` row in `outbox_deliveries` (required regardless of which of the two shapes the codebase landed in, per the task); and the no-op assertion test the plan calls for in place of a scrub test — `Schema::getColumnListing()` against all three read-model tables, asserted against a PII-name-pattern blocklist (`name`, `email`, `phone`, `address`, `document`, `dob`, `birth`), so a future migration that adds a column matching any of those patterns fails this test immediately and points at `ScrubReportingPii` as the place to implement the scrub, rather than the obligation going unnoticed. The `CustomerAnonymized` event itself is produced by driving the real `AnonymizeCustomer` Action inside an explicit `DB::transaction()`, the same posture T3's Orders test and `LedgerProjectionTest.php` already use.

Test evidence:

- Confirmed red first: ran the new test file before `ScrubReportingPii` existed. The registration and duplicate-delivery cases failed with `Class "App\Reporting\Jobs\ScrubReportingPii" not found`; the PII-column assertion test passed immediately (as expected, since it asserts a true fact about the already-migrated schema, not a class that doesn't exist yet).
- Green after implementation: `./vendor/bin/pest tests/Feature/Reporting/CustomerAnonymizedConsumerTest.php` — 3 tests, 3 passed, 247 assertions.
- `php artisan test --testsuite=Architecture` (context-boundary guard) — 40 tests, 40 passed.
- Scoped regression on the touched directory (`ReportingServiceProvider` is shared): `./vendor/bin/pest tests/Feature/Reporting tests/Unit/Reporting` — 125 tests, 125 passed, 775 assertions.
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on the two changed `app/` files: both pass with zero errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run (nothing to regenerate).

Commit: `e473add` (`feat(reporting): register CustomerAnonymized consumer as a no-op`).

Deviation from the plan: none. Per the task's own instruction to note which of the two shapes the codebase forced: the no-op-with-assertion shape, not the scrubbing shape, since Stage 11 denormalized no PII into any of the three read models.

## T5: Data subject export assembler and queued job

2026-07-13 16:32 -03

Landed stage-12 plan Slice 2 (first half) and the export half of task breakdown item 6: the export branch of `POST /v1/customers/{customer}/data-subject-requests`, the assembler Action, and the queued job, reusing task 2's own request/response Data classes and controller (no controller change needed: it already computed 201 vs 202 from the resulting request's status). Download URL, list, and show endpoints stay out of scope for task 6's second half (T6).

What landed:

- Widened `App\Identity\Data\CreateDataSubjectRequestData::rules()` from an erasure-only `Rule::in()` to both `DataSubjectRequestType` cases, and `App\Identity\Actions\CreateDataSubjectRequest` now branches per type: erasure keeps its exact prior synchronous claim/run/complete sequence; export creates the row and stays pending, deferring `App\Identity\Jobs\BuildDataSubjectExportJob::dispatch()` to `DB::afterCommit()`, mirroring `App\Reporting\Actions\CreateExport`'s own posture and its identical reasoning (the request's ambient tenant transaction has not committed yet; a synchronous dispatch under `QUEUE_CONNECTION=sync` would attempt a nested tenant transaction `TenantTransaction::run()` rejects). The job itself claims the row (pending to processing) once it runs, so the response this endpoint returns for export always shows `pending`, reflecting the row's real state at response-build time rather than the eventual one, since medialibrary attachment happens after the request's transaction has already committed and the JSON body was already serialized (confirmed by reading `App\Tenancy\Http\Middleware\TransactsRequests`: the response is built inside `$next($request)`, before the wrapping `$posture()` closure returns and the transaction commits).
- `App\Identity\Jobs\BuildDataSubjectExportJob`, mirroring `App\Reporting\Jobs\BuildExportJob`'s exact bootstrap: the `tenant_id` is read under the cross-tenant platform role (`data_subject_requests`' own platform-read RLS policy, from T1), then the build runs inside that tenant's own transaction; a vanished request id exits cleanly.
- `App\Identity\Actions\BuildDataSubjectExport`, the export counterpart to `App\Reporting\Actions\BuildExport`: claims the request (`DataSubjectRequest::claim()`, the same exactly-one-worker guard T1 shipped), assembles a single JSON document (`customer`, `orders`, `tickets`, `payments`, `refunds`, `check_ins`), attaches it via medialibrary, and completes or fails the request. Any throwable is caught, reported, and mapped to `DataSubjectRequest::fail()` (no failure code column on this table, unlike `exports.failure_code`, matching T1's actual `fail(string $id): bool` signature). The customer's name and email are read directly off the `Customer` row exactly as `AnonymizeCustomer` left them, so exporting an anonymized customer needs no special-casing: the row already carries the placeholder by the time this action runs.
- `App\Identity\Models\DataSubjectRequest` gained `HasMedia`/`InteractsWithMedia`/`HasMediaCapability`, a single-file `data_subject_export` collection accepting `application/json` (verified empirically that PHP's fileinfo sniffs a bare JSON string as `application/json`, no CSV-style multi-row fixture workaround needed), and `mediaManageCapability()` returning `Capability::CustomersExport`, mirroring `App\Reporting\Models\Export`'s own shape.
- Five new cursor-paginated read Actions, one per category the literal Stage-11 `PaginateXForExport` Actions cannot serve unchanged, plus the DTOs Stage 11 did not already have: `App\Orders\Actions\PaginateOrdersForDataSubjectExport` and `PaginateTicketsForDataSubjectExport` (customer-scoped, reusing `OrderExportRowData`/`TicketExportRowData` and `GetTicketSaleFacts` unchanged), `App\Payments\Actions\PaginatePaymentsForDataSubjectExport` and `PaginateRefundsForDataSubjectExport` (order-ID-scoped, since `payments`/`refunds` carry no `customer_id` column; two new DTOs, `PaymentExportRowData`/`RefundExportRowData`, since Stage 11's own Payments export type is `ledger_entries`, a distinct double-entry-accounting shape, not the payment/refund facts this document names as its own categories), and `App\CheckIn\Actions\PaginateCheckInsForDataSubjectExport` (ticket-ID-scoped, since `check_ins` carries no `customer_id` column; reuses `CheckInExportRowData` unchanged). `App\Identity\Actions\BuildDataSubjectExport` collects order IDs while paging orders and ticket IDs while paging tickets, then passes those lists into Payments' and CheckIn's own Actions, the same "load it through the owning context's Actions" posture `App\Orders\Actions\GetOrderEventIds`/`GetTicketTypeIds` already established for a bulk cross-context lookup, just cursor-paginated per api-conventions' high-volume rule instead of a single bulk pluck.
- `tests/Architecture/ContextBoundariesTest.php`: an explicit `Identity does not import {Orders,Payments,CheckIn} models` assertion (the pre-existing generic "only the {context} context uses its own Models" rule already forbids this structurally; this names the specific boundary the task calls for).
- `docs/openapi/openapi.yaml`: added the `202` response to `POST /v1/customers/{customer}/data-subject-requests`, widened the operation and `CreateDataSubjectRequestRequest` descriptions past "erasure only," and clarified the `DataSubjectRequest` schema's `download_url` note to point at T6's own show endpoint rather than implying this create response ever carries a URL.
- Test-list corrections forced by widening the accepted `type` values: `tests/Feature/Identity/DataSubjectErasureTest.php`'s "rejects an unaccepted type" case (previously asserting 422 for `type: export`, which now succeeds) now asserts 422 against a genuinely unknown type string; `tests/Contract/DocumentedResponseCoverageTest.php`'s `422` exerciser for the same route switched from `type: export` to the same unknown-type string, and a new `202` exerciser was added (`Queue::fake()` in this file's own `beforeEach` keeps it from running the real job, mirroring every other queued-job exerciser already in this file).

Test evidence:

- Confirmed red first: moved the five new Actions, `BuildDataSubjectExport`, and `BuildDataSubjectExportJob` aside and reverted the `CreateDataSubjectRequest`/`CreateDataSubjectRequestData` edits, then ran the new Unit and Feature files. Unit files failed with "Class ... not found" for each new Action; the Feature build test failed the same way via the job; the Feature request test's 202/409/403 cases failed because the export branch still threw `LogicException('Data subject export requests are not implemented yet.')`. Restored all files and re-applied the edits.
- Green after implementation: `./vendor/bin/pest tests/Unit/Orders/PaginateOrdersForDataSubjectExportTest.php tests/Unit/Orders/PaginateTicketsForDataSubjectExportTest.php tests/Unit/Payments/PaginatePaymentsForDataSubjectExportTest.php tests/Unit/Payments/PaginateRefundsForDataSubjectExportTest.php tests/Unit/CheckIn/PaginateCheckInsForDataSubjectExportTest.php` — 13 tests, 13 passed, 46 assertions.
- `php artisan test --testsuite=Architecture` (the task's named Identity import-boundary guard) — 43 tests, 43 passed.
- `./vendor/bin/pest tests/Feature/Identity/DataSubjectExportRequestTest.php` — 3 tests, 3 passed, 20 assertions. `./vendor/bin/pest tests/Feature/Identity/DataSubjectExportBuildTest.php` — 5 tests, 5 passed, 72 assertions (document contents/wire shape; anonymized-customer placeholders; duplicate delivery producing one attachment and one completed transition; an already-processing row left untouched by a second claim attempt; a vanished request id exiting cleanly).
- `./vendor/bin/pest tests/Feature/Identity/DataSubjectErasureTest.php tests/Feature/Identity/DataSubjectExportRequestTest.php tests/Feature/Identity/DataSubjectExportBuildTest.php tests/Feature/Identity/CustomerAnonymizedOutboxTest.php` — 21 tests, 21 passed, 158 assertions (regression check on the shared `CreateDataSubjectRequestData`/`CreateDataSubjectRequest` edit).
- `./vendor/bin/pest --testsuite=Contract` (full, since `DocumentedResponseCoverageTest.php` and the OpenAPI file both changed) — 473 tests, 473 passed on the second run; the first run showed one unrelated failure (`get /v1/events 200`, a `users_email_unique` collision from an unseeded `fake()->unique()->safeEmail()` call elsewhere in that same giant exerciser file), confirmed pre-existing and order-dependent by re-running that single exerciser alone (passes) and the full suite again (473/473) — not caused by this task's changes.
- `./vendor/bin/pest tests/Feature/Identity tests/Unit/Identity` — 508 tests, 508 passed, 2267 assertions. `./vendor/bin/pest tests/Feature/Orders tests/Unit/Orders tests/Feature/Payments tests/Unit/Payments tests/Feature/CheckIn tests/Unit/CheckIn` — 649 tests, 649 passed, 2354 assertions.
- `./vendor/bin/pest --testsuite=Isolation` — 354 tests, 354 passed (regression check; no new isolation surface this task, T1 already covers `data_subject_requests` and its platform-read policy).
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` and `tests/` file: both pass with zero errors.
- `composer -d apps/api run types:generate` was run; `git status` on `packages/api-client` showed no diff (no Data class shape changed, only `CreateDataSubjectRequestData::rules()`'s validation logic and `DataSubjectRequestData`'s docblock, neither of which the TypeScript transformer reads), so nothing was committed for it.

Deviation from the plan: one judgment call, recorded here since the task text names literal reuse of "the cursor-paginated ExportSource read Actions Stage 11 added in Orders, Payments, and CheckIn." Read literally, the four Stage-11 Actions (`PaginateOrdersForExport`, `PaginateTicketsForExport`, `PaginateLedgerEntriesForExport`, `PaginateCheckInsForExport`) filter by `event_id`/date window (Orders, CheckIn) or nothing but date window (Payments' `ledger_entries`), none of which is a customer-scoped filter a data subject export needs, and Payments' own Stage-11 export type is `ledger_entries`, a double-entry-accounting projection distinct from the `payments`/`refunds` facts this document names as its own categories. Widening those four Actions in place to also accept an optional `customerId` (or, for Payments, adding two brand-new Payment/Refund Actions that still had to be new either way) would have mixed two genuinely different filter shapes into Reporting's own already-shipped, already-tested export path for no reuse gained on the Payments half regardless. This task instead reuses what Stage 11 actually reuses cleanly: the exact same generator/cursorPaginate *pattern* those four Actions established, and, where the row shape was already customer-agnostic, the exact same DTOs unchanged (`OrderExportRowData`, `TicketExportRowData`, `CheckInExportRowData`), while adding five new, purpose-built Actions (and two new DTOs for Payments/Refunds, which Stage 11 never produced) rather than overloading Reporting's own Stage-11 classes with a second, unrelated filter contract.

## T6: Export download URL plus GET show and GET list endpoints

2026-07-13 16:58 -03

Landed stage-12 plan Slice 2's remainder and the endpoint half of task breakdown item 6: `GET /v1/data-subject-requests/{data_subject_request}` (download URL plus show) and `GET /v1/data-subject-requests` (admin list). Both are available to either `customers.erase` or `customers.export`, unlike `store()`'s own type-dependent single-capability check, since a caller who can only act on one type still needs to see the requests they can act on.

What landed:

- `App\Identity\Authorization\CapabilityGate::authorizeAny(Capability ...$capabilities)`: passes if the acting membership holds at least one of the given capabilities, denying with `MissingCapabilityException::for()` the first listed capability when it holds none. No existing precedent for an "any of" check in this codebase (`authorize()` only ever checked one), so this is new API surface on the gate itself, unit-tested directly (`tests/Unit/Identity/CapabilityGateTest.php`) the same way `authorize()` already was.
- `App\Identity\Exceptions\DataSubjectRequestNotFoundException`, mapped to the generic `request.not_found` code, mirroring `App\Reporting\Exceptions\ExportNotFoundException`'s own precedent (RLS already makes a cross-tenant row indistinguishable from absence, so no new per-resource code).
- `App\Identity\Data\DataSubjectRequestData::fromModel()` gained an optional `$downloadUrl` parameter (default null): `App\Identity\Actions\CreateDataSubjectRequest`'s own two callers (201 erasure, 202 export) keep passing nothing, since neither has anything to sign yet; the show endpoint is the one caller that passes a real signed URL.
- `App\Identity\Http\Controllers\DataSubjectRequestController::show()`: resolves the row (404 via `DataSubjectRequestNotFoundException` on a foreign-tenant or unknown id, RLS making the two indistinguishable), then computes `download_url` from `$model->getFirstMedia('data_subject_export')` alone, never by inspecting `type` or `status` directly. This works uniformly for both types without special-casing: an erasure request's media collection is always empty (it never attaches anything), so it always renders null; an export renders null until `App\Identity\Actions\BuildDataSubjectExport` completes it, since that action's own attach-then-complete order (confirmed by re-reading it) means a media row implies completion. Deferred to a new `config('identity.data_subject_export_download_url_ttl_minutes')` (`DATA_SUBJECT_EXPORT_DOWNLOAD_URL_TTL_MINUTES`, default 15) rather than sharing Reporting's own `reporting.export_download_url_ttl_minutes`, since the two contexts should not read each other's config for an unrelated TTL that happens to share a shape.
- `App\Identity\Http\Controllers\DataSubjectRequestController::index()`: `QueryBuilder` with `AllowedFilter::exact` on `customer_id`, `type`, `status`, `allowedSorts('created_at')`, `defaultSort('-created_at')`, page pagination via `PaginatedDataCollection`, mirroring `App\Identity\Http\Controllers\RoleController`'s own page-pagination posture (a bounded, non-high-volume collection) rather than the cursor pagination Reporting's `GET /v1/exports` uses. `download_url` stays null for every list row: signing one URL per row on every page would mean a medialibrary lookup per row, which the show endpoint already serves as the one documented way to fetch a real one.
- Both new routes registered in `app/Identity/Http/routes/customers.php` (already the home of the `POST .../data-subject-requests` route from task 3), read-only so no `RecordActivityAudit`, capability-gated inside the controller via `authorizeAny` rather than a static `RequireCapability` route middleware entry.
- `docs/openapi/openapi.yaml`: two new paths (`/v1/data-subject-requests`, `/v1/data-subject-requests/{data_subject_request}`), a new `DataSubjectRequestPage` schema mirroring `RolePage`'s standard-envelope shape, and the shared `DataSubjectRequest` schema's description widened to explain `download_url` across all three surfaces that now return it (create response, show, list row) instead of only the create response T5 originally documented. `X-Tenant-Id` is not documented as an explicit parameter on either new GET operation, matching `GET /v1/roles` and `GET /v1/memberships`'s own precedent (not every tenancy.admin GET route documents it, unlike the `POST .../data-subject-requests` operation T2 already had).
- `composer -d apps/api run types:generate` was run; `git status` on `packages/api-client` showed no diff. `DataSubjectRequestData` (and `CreateDataSubjectRequestData`) still carry no `#[TypeScript]` attribute, a pre-existing Identity posture from Stage 3 this task does not change (most Identity Data classes lack it, unlike Reporting's and Payments'), so nothing to commit there, exactly as T5 found for the same class.
- `tests/Architecture/PresetTest.php`: added `DataSubjectRequestNotFoundException` to the Laravel preset's `ignoring()` list next to `DataSubjectRequestAlreadyOpenException`, the same "lives in its bounded context's own Exceptions directory, not `App\Exceptions`" entry every other `HasErrorCode` exception in this codebase needs.

Test evidence:

- Confirmed red first: with none of the controller, route, `CapabilityGate::authorizeAny`, `DataSubjectRequestNotFoundException`, or `DataSubjectRequestData::fromModel()` signature changes applied, the new Feature and Isolation tests were run and failed as expected (`show`/`index` undefined route method errors, then, after adding just the routes pointing at not-yet-existing controller methods, a 500 from the missing methods); the new `CapabilityGateTest` cases failed with "Call to undefined method authorizeAny()". All files were then applied together and re-run green (the controller's `show`/`index` and the gate method were written together since one is meaningless without the other for this task's own 403 requirement).
- `./vendor/bin/pest tests/Unit/Identity/CapabilityGateTest.php tests/Feature/Identity/DataSubjectRequestShowTest.php tests/Feature/Identity/DataSubjectRequestListTest.php tests/Isolation/DataSubjectRequestEndpointIsolationTest.php` — 25 tests, 25 passed, 79 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (the new `DataSubjectRequestNotFoundException` preset entry).
- `./vendor/bin/pest tests/Contract/DocumentedResponseCoverageTest.php` (full, since the OpenAPI file and this file both changed) — 476 tests, 476 passed.
- `./vendor/bin/pest tests/Feature/Identity tests/Unit/Identity` — 525 tests, 525 passed, 2332 assertions (regression check).
- `./vendor/bin/pest --testsuite=Isolation` — 355 tests, 355 passed (354 prior plus this task's own new endpoint-isolation file).
- `./vendor/bin/pint --test` on every new/changed file: passed. `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: 0 errors; `composer analyse`'s own configured path is `app` only (`phpstan.neon`), so the four new/changed `tests/` files were not run through Larastan individually (running them directly on the CLI produces `Pest\PendingCalls\TestCall` false positives on any Pest test file analyzed outside the full suite, confirmed pre-existing and unrelated to this task by reproducing the identical error class against an untouched, already-merged test file, `tests/Feature/Identity/DataSubjectErasureTest.php`).

No deviation from the plan beyond the two judgment calls already named above (the `authorizeAny` API shape and the dedicated Identity-owned TTL config key), both direct minimal extensions of existing precedent rather than open design decisions.

## T7: config/retention.php, webhook payload pruner, export attachment pruner, scheduler

2026-07-13 17:11 -03

Landed stage-12 plan Slice 3's webhook and export-attachment halves and task breakdown item 7 in full, as two commits (payments, then identity) per the task's own split. Slice 3's remaining two tests (activity log archive-then-prune, the append-only isolation update) are task 8/9's own scope, not this one.

What landed:

- `config/retention.php`: all four stage-12 windows in one file (`webhook_payload_days` default 90, `activity_log_days` default 400, `outbox_archival_days` default 180, `export_attachment_days` default 7), each `env()`-backed, matching the plan's Risks section defaults exactly. Only the first and last are consumed by this task; the middle two exist now so task 8/9/10 read from an already-shipped file rather than adding keys to it later, per the task's own instruction that this file "holds every window."
- `database/migrations/2026_07_13_000061_add_payload_pruned_at_to_gateway_webhook_events_table.php`: additive migration on the merged stage-08a table (never edited) adding nullable `payload_pruned_at`, plus `alter column payload drop not null` (the original migration created `payload` NOT NULL, which pruning requires relaxing). `App\Payments\Models\GatewayWebhookEvent` gained the column to its `Fillable` list, `datetime` cast, and docblock (`payload` now nullable in the type).
- `App\Payments\Actions\PruneWebhookPayloads`: one conditional UPDATE (`whereNull('payload_pruned_at')` and `received_at <= cutoff`) nulling `payload` and stamping `payload_pruned_at`, returning the affected-row count. Runs under `TenantTransaction::asTenant($sentinelTenantId, ...)` rather than a cross-tenant platform scan: every `gateway_webhook_events` row already carries the sentinel platform tenant (stage-08a docblock), so there is exactly one tenant to touch, mirroring `App\Payments\Actions\IngestGatewayWebhook`'s own posture rather than `ReleaseExpiredHolds`' asPlatform-then-per-tenant shape. A zero or negative `retention.webhook_payload_days` throws a plain `RuntimeException` before any query runs.
- `App\Identity\Actions\PruneDataSubjectExportAttachments`: discovers completed export requests with `completed_at` past `retention.export_attachment_days` under `asPlatform()` (the platform-read policy T1 gave `data_subject_requests`), then deletes each `data_subject_export` medialibrary attachment one at a time through `Media::delete()` (never a bulk query, since medialibrary's file-and-conversion removal is a `MediaObserver` hook wired to the Eloquent delete event, which a bulk DELETE bypasses). Deletion itself needs no per-tenant loop either: the `media` table's own `platformWrite` policy (stage-05c migration) already lets `nodia_platform` delete any tenant's media row. The `data_subject_requests` row itself is never written to, so it survives as audit trail exactly as the plan specifies; `GET /v1/data-subject-requests/{id}` already derives `download_url` from attachment presence (T6), so it reads null with no extra code. Same zero/negative-window refusal as the webhook pruner.
- `App\Console\Commands\PruneWebhookPayloadsCommand` (`webhooks:prune-payloads`) and `App\Console\Commands\PruneDataSubjectExportAttachmentsCommand` (`data-subject-requests:prune-exports`): thin wrappers catching the `RuntimeException` refusal and reporting it via `$this->error()` with `self::FAILURE`, mirroring the existing sweeper commands' `$this->info()` posture on success.
- `bootstrap/app.php`: both commands scheduled `->daily()` (day-scale windows, unlike the per-minute sweepers already registered), one line added per commit.
- Tests: `tests/Feature/Payments/WebhookPayloadPruningTest.php` (old rows pruned, young rows untouched, a duplicate webhook delivered through the real `POST /v1/webhooks/fake` ingestion path after pruning still deduplicates via the surviving `(gateway, gateway_event_id)` row, rerunning the pruner is a no-op on already-pruned rows, ledger entries untouched); `tests/Unit/Payments/PruneWebhookPayloadsTest.php` (a zero/negative window throws and touches no row, a Pest dataset over `0, -1, -90`); `tests/Feature/Identity/DataSubjectExportAttachmentPruningTest.php` (old completed export request's attachment deleted and `GET /v1/data-subject-requests/{id}`'s `download_url` reads null afterward through the real endpoint and a real staff bearer via `Tests\Support\TenantStaff`, a young one untouched with a real signed `download_url` still returned, rerun no-op, ledger entries untouched); `tests/Unit/Identity/PruneDataSubjectExportAttachmentsTest.php` (same zero/negative dataset); `tests/Unit/Support/RetentionConfigTest.php` (all four defaults, per-key override, fake-clock-driven cutoff computation, and a schedule assertion per command, extended across both commits).

Test evidence:

- Confirmed red first for both halves: moved `PruneWebhookPayloads`/`PruneWebhookPayloadsCommand` aside and ran the new payments Feature/Unit files — the Feature file errored with `Target class [App\Payments\Actions\PruneWebhookPayloads] does not exist`, the Unit refusal dataset failed with a `BindingResolutionException` instead of the expected `RuntimeException` (config/retention.php was already in place, so only the container resolution failed, not the config assertions). Restored, green. Repeated identically for `PruneDataSubjectExportAttachments`/its command: the Feature and Unit files failed the same way (`Target class ... does not exist`, `BindingResolutionException` instead of `RuntimeException`). Restored, green.
- Payments commit: `./vendor/bin/pest tests/Feature/Payments/WebhookPayloadPruningTest.php tests/Unit/Payments/PruneWebhookPayloadsTest.php tests/Unit/Support/RetentionConfigTest.php` — 11 tests, 11 passed, 36 assertions. `php artisan test --testsuite=Architecture` — 43 tests, 43 passed. `./vendor/bin/pest tests/Feature/Payments tests/Unit/Payments` — 373 tests, 373 passed, 1474 assertions. `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: both pass with zero errors. No Data class changed, so `composer -d apps/api run types:generate` was not run.
- Identity commit: `./vendor/bin/pest tests/Feature/Identity/DataSubjectExportAttachmentPruningTest.php tests/Unit/Identity/PruneDataSubjectExportAttachmentsTest.php tests/Unit/Support/RetentionConfigTest.php` — 12 tests, 12 passed, 38 assertions (one fix mid-loop: comparing `completed_at` with Carbon's `equalTo()` against an in-memory microsecond-precision value failed against the DB's own zero-precision `timestampTz`, switched to a `toDateTimeString()` comparison). `php artisan test --testsuite=Architecture` — 43 tests, 43 passed. `./vendor/bin/pest tests/Feature/Identity tests/Unit/Identity` — 532 tests, 532 passed, 2355 assertions. `./vendor/bin/pest --testsuite=Isolation` — 355 tests, 355 passed (regression check; no new tenant-scoped table this task). `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: both pass with zero errors. No Data class changed, so `composer -d apps/api run types:generate` was not run.

Commits: `e31cc91` (`feat(payments): prune raw webhook payloads past the retention window`), `3f48afc` (`feat(identity): prune data subject export attachments past the retention window`).

Deviation from the plan: none in scope. One judgment call: the webhook pruner runs under `asTenant(sentinel)` rather than the cross-tenant `asPlatform()`-then-per-tenant shape `ReleaseExpiredHolds`/`SweepExpiredPayments` use, since every candidate row already carries the single sentinel tenant (no cross-tenant scan is needed at all), matching `IngestGatewayWebhook`'s own existing precedent for the same table rather than introducing a new shape.

## T8: Activity log scoped delete path and system-design 14.2 amendment

2026-07-13 17:18 -03

Landed stage-12 plan task breakdown item 8 and the first entry in Risks and open questions: the narrow DELETE path Stage 3's `activity_log` never had, and the 14.2 amendment reconciling the append-only wording against 14.3's retention window with the archive-then-delete reading the plan already commits to. No Data class, no endpoint, no OpenAPI path in this task (it is a pure migration plus isolation-test task, the same shape T1's table-only slice took), so the double-loop's contract step does not apply; the isolation suite is the whole loop here.

What landed:

- `database/migrations/2026_07_13_000062_add_scoped_delete_policy_to_activity_log_table.php`: an additive migration on top of the merged Stage 3 `activity_log` migration (never edited). Grants `DELETE` to `nodia_platform` only, never to `nodia_app`, so a tenant-scoped request handler (always `nodia_app`, system-design 4.1) still gets a hard `permission denied` error on any delete attempt, unchanged from before this migration. A new `activity_log_platform_delete` policy (`for delete to nodia_platform`) restricts even that grant to rows whose `created_at` is before `app.activity_log_prune_cutoff`, a session setting nobody sets except the retention pruning command task 9 ships; ordinary platform-scope request handling (`TenantTransaction::elevateToPlatformRole()`, `::asPlatform()`) never sets it, so `current_setting(..., true)` reads NULL, the comparison is never true (deny by default, the same posture `applyTenantPolicies()` uses for `app.tenant_id`), and an ordinary platform-role delete affects zero rows even though the raw grant exists. No `tenant_id` predicate on the new policy, mirroring `activity_log_platform_read`'s own cross-tenant shape: the pruning command scans and deletes across every tenant in one pass, unlike the outbox archiver's per-tenant-under-`nodia_app` shape the plan's Dependencies section explicitly contrasts this task against. `UPDATE` stays ungranted to both roles, unconditionally: nothing in this stage needs an update path, and 14.2 (amended alongside this migration) now states rows are never updated.
- `docs/system-design.md` 14.2: replaced "log rows are never updated or deleted inside the application" with wording stating the archive-then-delete reading explicitly: rows are never updated by application code, and are never deleted except by the scheduled retention pruner (14.3), which archives past-window rows to object storage before removing them from PostgreSQL; no ordinary request path can delete a row, and the pruner's delete is the narrowly scoped path this task's migration ships, distinct from the database privileges every other write path uses.
- `tests/Isolation/ActivityLogIsolationTest.php`: updated to the Slice 3 expectations named in the task. The pre-existing UPDATE-denied cases (app role, platform role) are unchanged, both still `permission denied` unconditionally. The pre-existing app-role DELETE-denied case is unchanged in behavior, renamed to name "request context" explicitly. The old platform-role DELETE test (previously asserting a `permission denied` `QueryException`, no longer true now that the grant exists) is replaced by two new cases: one proving an ordinary platform-role delete with no cutoff set removes zero rows (row still exists afterward), and one proving a platform-role delete with `app.activity_log_prune_cutoff` explicitly set via `set_config()` removes only the row whose `created_at` precedes the cutoff, spanning both tenants in one statement, leaving a younger row (one day inside the window) untouched. File-level docblock updated to describe the new DELETE posture.
- `tests/Isolation/Support/ActivityLogFixture.php`: docblock updated to note DELETE is now grantable to `nodia_platform` (previously "grants no UPDATE or DELETE privilege to any role at all"), while `clean()`'s behavior and reasoning are unchanged: fixture rows are seeded at "now", never past any realistic cutoff, so the new narrow path still cannot (and need not) remove them.

Test evidence:

- Confirmed red first: ran the updated `tests/Isolation/ActivityLogIsolationTest.php` before adding the migration. The no-cutoff platform-role case failed with a `permission denied for table activity_log` `QueryException` (the grant did not exist yet, so the delete threw instead of returning zero affected rows); the cutoff-scoped case failed on setup, both as expected pre-migration. Added the migration (auto-applied by the test bootstrap's own migration-on-demand mechanism, confirmed by the very next run picking it up with no manual `migrate` step), re-ran green; one self-inflicted bug fixed mid-loop (an `use ($cutoff)` closure missing `$oldId`/`$youngId`, "Undefined variable" on the first green attempt).
- `./vendor/bin/pest tests/Isolation/ActivityLogIsolationTest.php` — 10 tests, 10 passed, 19 assertions.
- `./vendor/bin/pest --testsuite=Isolation` (full regression, the task's own acceptance line: "scoped run of the activity log isolation tests," widened to the full suite since a DB-privilege change on a shared role is exactly the kind of edit that can have blast radius beyond its own file) — 356 tests, 356 passed, 690 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (no new exception or Data class this task; run as a cheap regression check only).
- `./vendor/bin/pint --test` on the new migration and the two changed test files: passed with zero errors. Larastan was not run: `phpstan.neon`'s configured `paths` is `app` only, and no `app/` file changed this task (migration, tests, and docs only).
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Deviation from the plan: none. One judgment call, following the plan's own named alternative rather than inventing a third: "a DELETE policy restricted to rows past a cutoff the pruning command supplies" (the plan's first-listed option) over "an equivalently narrow dedicated path" (e.g. a `SECURITY DEFINER` function) — the cutoff-scoped policy needed no new database object beyond a grant and a policy, matching every other RLS mechanism this table (and every tenant-scoped table) already uses, so it is the minimal reading of the plan's own alternative, not a new pattern.

## T9: archive_segments migration and activity log archive-then-prune command

2026-07-13 17:32 -03

Landed stage-12 plan Data model "archive_segments", the activity log half of Slice 3, and task breakdown item 9: the platform infrastructure manifest table both archivers (this task's activity log archiver now, task 10's outbox archiver later) share, and the command that exports `activity_log` rows past `config('retention.activity_log_days')` to one checksummed object storage segment, then deletes the source rows only once the upload verifies. Depends on task 7 (`config/retention.php`, already shipping `activity_log_days`) and task 8 (the `activity_log_platform_delete` scoped delete path, already shipping the `app.activity_log_prune_cutoff` session-variable mechanism this command is the first caller of).

What landed:

- `database/migrations/2026_07_13_000063_create_archive_segments_table.php`: `id` (UUIDv7), `source` (enum-backed string), `range_from`/`range_to` (plain strings per the plan's own typing), unique `object_key`, `row_count`, `checksum`, `archived_at`, timestamps, an index on `(source, range_from)`. No `tenant_id`, no RLS: `Rls::grantUnscoped('archive_segments')` grants full CRUD to `nodia_app` and `nodia_platform` with row level security never enabled, the exact Stage 4 `failed_jobs` precedent the plan names, since segments batch rows across every tenant.
- `App\Support\Database\UnscopedTables::names()`: added `'archive_segments'`, which is what turns the pre-existing generic isolation sweep test (`tests/Isolation/UnscopedTablesSweepTest`, "every public base table without RLS must be on the exclusion list") red the moment the migration lands and green once the exclusion is added — the task's own "isolation-sweep exclusion updated" instruction, proven by an existing test rather than a new bespoke one.
- `App\Support\Archive\Enums\ArchiveSegmentSource` (`outbox_events`, `activity_log`) and `App\Support\Archive\Models\ArchiveSegment`: both live under `App\Support\Archive`, the same "infrastructure with no single owning bounded context" placement `App\Support\Audit\Models\ActivityLogEntry` and `App\Support\Outbox\Models` already established, not any one context's own namespace. `OutboxEvents` is unused until task 10; the enum ships now so both archivers share one registry and one manifest table from the start rather than task 10 widening it later.
- `App\Support\Archive\Actions\ArchiveActivityLog`: the command's whole core, run entirely under `TenantTransaction::asPlatform()` in one transaction. Selects `activity_log` rows with `created_at` before the cutoff (the existing cross-tenant `activity_log_platform_read` policy from Stage 3), returns 0 with no further action if none are found, else serializes each row to one JSON line (NDJSON; jsonb `attribute_changes`/`properties` decoded before re-encoding rather than double-encoded as opaque strings), computes a SHA-256 checksum of the whole content, uploads it to `config('retention.archive_disk')` (default `'s3'`, a new `config/retention.php` key: the generic S3-compatible disk, distinct from the medialibrary-managed `'media'` disk that serves public tenant assets, not archival manifests) under a fresh UUIDv7-derived object key, reads the uploaded object back and re-hashes it, and only if that hash matches the pre-upload checksum does it write the `archive_segments` manifest row and then delete the source rows — setting `app.activity_log_prune_cutoff` to the exact same cutoff the select used (task 8's own session-variable mechanism) so the archived set and the deleted set are identical within the one transaction. Any throwable from disk resolution or upload, or a checksum mismatch on read-back, is wrapped in a plain `RuntimeException` and propagates out of the `asPlatform()` closure, which rolls the whole transaction back: no manifest row, no deleted row, exactly the plan's "archive-then-delete, never delete-then-archive" invariant. A zero or negative `retention.activity_log_days` refuses to run the same way `PruneWebhookPayloads`/`PruneDataSubjectExportAttachments` already do, before any query runs.
- `App\Console\Commands\ArchiveActivityLogCommand` (`activity-log:archive`): a thin wrapper mirroring `PruneWebhookPayloadsCommand`'s exact catch-`RuntimeException`-and-report shape. Scheduled `->daily()` in `bootstrap/app.php` alongside the two Slice 3 pruners — a judgment call, not named explicitly in this task's own text (only task 7 says "scheduler registration"), but the minimal reading of "activity log archive-then-prune command" as a day-scale retention sweep like its two siblings, made explicit rather than left as a silent gap; a new `RetentionConfigTest` case proves the daily cron expression the same way the two existing schedule tests do.
- `config/retention.php`: added `archive_disk` (env `RETENTION_ARCHIVE_DISK`, default `s3`), documented alongside the four existing windows.
- Tests: `tests/Feature/Support/ActivityLogArchivalTest.php` (fake clock) — old rows archived to a segment whose manifest row and uploaded object checksum both verify, young rows untouched; a no-op run when nothing is past the window writes no manifest; an upload failure (mocked `put()` throwing) leaves the row in place and writes no manifest; a dedicated case proves ledger entries are never touched by this pruner, mirroring `WebhookPayloadPruningTest`'s own "never touches ledger entries" case. `tests/Unit/Support/ArchiveActivityLogTest.php` — the archiver refuses to run and deletes nothing when object storage itself is unreachable (mocked `Storage::disk()` throwing before any upload attempt, distinct from the Feature test's later-stage upload-failure case), plus a zero/negative-window dataset mirroring the Slice 3 Unit item every other pruner already carries. `tests/Architecture/PresetTest.php`: `ArchiveSegmentSource` and `'App\Support\Archive\Models'` added to the Laravel preset's `ignoring()` list, the same story as every other Support-namespace infrastructure enum and model already exempted.

Test evidence:

- Confirmed red first: moved `ArchiveActivityLog`, `ArchiveActivityLogCommand`, the migration, `ArchiveSegment`, and `ArchiveSegmentSource` aside, then ran the new Feature and Unit files. The Unit refusal cases failed with `BindingResolutionException` instead of the expected `RuntimeException` (the class did not exist for the container to resolve); the Feature file errored on an unrelated self-inflicted bug first (`Undefined property` from appending to an uninitialized dynamic test-case property), fixed by initializing it in `beforeEach`, then re-ran and errored the expected way once the implementation files were confirmed absent (`Indirect modification of overloaded property Pest\Support\HigherOrderTapProxy` when the tracking mechanism tried to mutate state through the `test()` proxy — a second self-inflicted issue, fixed by replacing the property-based id tracker with a plain by-reference static-array registry function, since these tests' helper functions run outside any `it()` closure's own `$this` binding). Restored all files, re-ran; the tests then exercised the real implementation and were green on the first attempt after both harness fixes.
- `./vendor/bin/pest tests/Feature/Support/ActivityLogArchivalTest.php tests/Unit/Support/ArchiveActivityLogTest.php tests/Unit/Support/RetentionConfigTest.php` — 14 tests, 14 passed, 46 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (the new `PresetTest` ignoring-list entries).
- `./vendor/bin/pest --testsuite=Isolation` (full regression, the task's own new unscoped-table exclusion being exactly the kind of shared-registry edit the sweep test is designed to catch) — 356 tests, 356 passed, 690 assertions.
- `./vendor/bin/pest tests/Feature/Support tests/Unit/Support` — 95 tests, 95 passed, 344 assertions.
- `./vendor/bin/pint --test` on every new/changed file: one auto-fixable violation in the new Feature test file (`fully_qualified_strict_types`, `ordered_imports`), fixed by running `./vendor/bin/pint` directly; re-verified clean afterward. `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: 0 errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Commit: `ad91b96` (`feat(support): archive activity log rows to object storage before pruning`).

Deviation from the plan: none in scope. Two judgment calls, both recorded above: scheduling `activity-log:archive` daily even though only task 7's own text says "scheduler registration" (the minimal reading of a day-scale retention command sharing its siblings' cadence); and choosing `'s3'` as `retention.archive_disk`'s default over adding a dedicated `filesystems.php` disk, since the generic S3-compatible disk already exists, is unused by anything else, and needs no new disk definition to serve as an object storage target distinct from the medialibrary-managed `'media'` disk.

## T10: Outbox archiver and archive-aware replay

2026-07-13 18:09 -03

Landed stage-12 plan Slice 4 and task breakdown item 10: the outbox archiver, `OutboxReplay` extended to read archived segments, and the relaxation of Stage 4's never-deleted invariant wording. Depends on T9's `archive_segments` table and `ArchiveSegmentSource` enum, both reused unchanged. No Data class, no endpoint, no OpenAPI path in this task (an internal archiver plus an internal replay primitive, the same shape T9 took), so the double-loop's contract step does not apply; Feature and Unit tests are the whole loop.

Resolved the plan's second open question before implementing, per the task's own instruction: system-design 9.1's "archived ... independent of delivery state" is read as the archival *schedule* not waiting on any delivery-completion signal, not as archiving over undelivered work. A row with any pending `outbox_deliveries` row (any subscriber) is never archived; the archiver stops the whole batch at the first such row in ascending sequence order rather than skipping past it, so every segment's `[range_from, range_to]` is a true contiguous prefix with no live row inside it. No system-design or stage-12-hardening.md edit was made for this resolution: unlike task 8's activity-log question, this task's own instructions state the reading directly rather than asking for a document amendment.

What landed:

- `config/retention.php`: added `outbox_archive_batch_size` (env `RETENTION_OUTBOX_ARCHIVE_BATCH_SIZE`, default 5000), the only input segment boundaries are computed from, so a given eligible row set splits into the same segments run to run (the task's own "segment boundaries deterministic from configuration" unit case).
- `App\Support\Archive\Actions\ArchiveOutboxEvents`: runs the scan, NDJSON serialization, upload, checksum verification, and `archive_segments` manifest write in one `TenantTransaction::asPlatform()` transaction (SELECT-only on `outbox_events`/`outbox_deliveries`, the Stage 4 grant shape neither table's migration widened with a platform-write policy), then deletes the verified rows in one `asTenant()` transaction per tenant id found while building the batch, iterating them in the order encountered, deleting `outbox_deliveries` before `outbox_events` in each to satisfy the foreign key. Both deletes go through `DB::table(...)` directly, never the `OutboxEvent` Eloquent model, so the model's own append-only guard (`updating`/`deleting` static hooks, fired only by an individual model's own `update()`/`delete()`, never by a query-builder mass delete) is never in this path — the same bypass `ArchiveActivityLog` already established for `activity_log`. A zero or negative `retention.outbox_archival_days` or `retention.outbox_archive_batch_size` refuses to run before any query, mirroring every other pruner/archiver this stage shipped.
- `App\Console\Commands\ArchiveOutboxEventsCommand` (`outbox:archive`): the same catch-`RuntimeException`-and-report shape as `ArchiveActivityLogCommand`. Scheduled `->daily()` in `bootstrap/app.php` alongside the activity log archiver — a judgment call the same as T9's own for that command, since the task text names no explicit schedule requirement but every other stage-12 retention sweep runs on this cadence; a new `RetentionConfigTest` case proves the cron expression the same way the three existing schedule tests do.
- `App\Support\Outbox\OutboxReplay`: `eventsFor()` now merges two sources before sorting by sequence — `archivedEventsFor()` (new, private), which reads every `archive_segments` row with `source = outbox_events` under `asPlatform()`, downloads and decodes each NDJSON line from `config('retention.archive_disk')`, filters by the subscriber's types and the optional `fromSequence` floor, and hydrates an unsaved `OutboxEvent` instance per line (constructed via `fill()` plus a direct `sequence` property set, `exists = true`, never saved) so the `OutboxSubscriber::handle(OutboxEvent $event)` contract is identical whether a row is still live or has moved to object storage; and the existing live-table query, unchanged except for being merged rather than returned directly. Archived rows carry no separate stability-window filter: the archival window is months, the stability window seconds, so an archived row is always well past it.
- `App\Support\Outbox\Models\OutboxEvent` docblock and `tests/Unit/Support/Outbox/OutboxEventTest.php` docblock: reworded from "never updated or deleted by the application" to "never updated, and never deleted before the retention window or by anything but the archiver," per the task's own instruction. The two existing test cases (`update()`/`delete()` on the model both throw `LogicException`) are unchanged: the archiver's query-builder bypass means this guard is never exercised on that path, so no new test was needed to prove the relaxation — the existing two cases still prove exactly what they always proved (ordinary application code cannot reach the model's own update/delete surface), and the docblocks now say why that is no longer the *entire* deletion story for this table.
- `App\Support\Archive\Enums\ArchiveSegmentSource` docblock: updated from "OutboxEvents is not written by anything yet" to name `ArchiveOutboxEvents` as the writer, closing the forward reference T9 left open.

Tests (all new files, Slice 4's own four cases):

- `tests/Feature/Support/OutboxArchivalTest.php`: old rows and their processed deliveries archived to a checksummed segment with young rows untouched (decodes the NDJSON line and asserts `id`, `sequence`, `tenant_id`, and payload fields, not just substring containment); a no-op with no manifest when nothing is past the window; a failed upload (mocked `put()` throwing) leaves the row in place and writes no manifest; the batch stops at the first row with a pending delivery, keeping the archived range contiguous (a three-event fixture: first processed and archived, a middle event left pending and never archived, a later processed event also left untouched even though it has no pending delivery of its own, proving the "stop the whole batch" behavior rather than "skip the one blocked row"); a no-op when the only eligible row has a pending delivery; the sweeper never re-enqueues a delivery for an event the archiver already removed (`outbox:sweep` run past the grace window after archiving pushes nothing).
- `tests/Unit/Support/ArchiveOutboxEventsTest.php`: with `outbox_archive_batch_size` set to 3 against 5 eligible rows, the archiver takes exactly 3 (the earliest by sequence) and a second run archives the remaining 2 into a second segment (segment boundaries deterministic from configuration, and stable across repeated runs); the archiver refuses to run and deletes nothing when object storage is unreachable (`Storage::shouldReceive('disk')->andThrow(...)`); refuses to run when the retention window or the batch size is zero or negative (the same `[0, -1, -30]` non-positive dataset every other pruner/archiver in this stage reuses — file-scoped, so the repeated dataset name across files does not collide, confirmed by T7/T9's own precedent already using it).
- `tests/Feature/Support/Outbox/OutboxReplayArchiveBoundaryTest.php`: re-proves the Stage 4 rebuild-equivalence test (`tests/Feature/Support/Outbox/OutboxReplayTest.php`, "rebuilds a fresh projection identical to incremental state") across the archive boundary — two early events are recorded, delivered, and archived, two more are recorded and delivered live, and replaying into a fresh projection from sequence zero visits all four exactly once in sequence order with a snapshot identical to the incrementally built one; a second case proves an inclusive `fromSequence` that falls inside an archived segment (rather than at its start) still returns only the matching suffix, read out of the archived NDJSON content itself rather than the live table.

Test evidence:

- Confirmed red first: ran all three new test files (plus the `RetentionConfigTest`/`OutboxEventTest` edits) before `ArchiveOutboxEvents` or the `OutboxReplay` extension existed. Every case touching the archiver failed with `Target class [App\Support\Archive\Actions\ArchiveOutboxEvents] does not exist`; the new `outbox:archive` schedule assertion failed with "Expecting null not to be null." Implemented, re-ran; two self-inflicted bugs fixed mid-loop, both recorded for the next reader: (1) `OutboxArchivalTest.php`'s early `it()` cases initially recorded events without `Queue::fake()`, so `QUEUE_CONNECTION=sync` in testing delivered every subscriber synchronously at record time (`OutboxRecorder::record()`'s `dispatchAfterCommit`), silently processing a delivery meant to stay pending on purpose — fixed by moving `Queue::fake()` into the file's `beforeEach`, all consumption driven explicitly through a local `processArchivalDelivery()` helper afterward, the same posture every other outbox Feature test in this codebase already takes; (2) `OutboxReplayArchiveBoundaryTest.php`'s `afterEach` initially cleaned up `outbox_events`/`outbox_deliveries`/the tenant row but not `archive_segments`, so a segment it created leaked into whichever test ran next in the same process and broke `OutboxArchivalTest.php`'s own `ArchiveSegment::query()->...->sole()` assertion when both files ran together — fixed by adding the same `ArchiveSegment::query()->where('source', ArchiveSegmentSource::OutboxEvents)->delete()` cleanup line T9's own `ActivityLogArchivalTest.php` and `ArchiveActivityLogTest.php` already carry for their own source.
- Green after both fixes: `./vendor/bin/pest tests/Feature/Support/OutboxArchivalTest.php tests/Unit/Support/ArchiveOutboxEventsTest.php tests/Feature/Support/Outbox/OutboxReplayArchiveBoundaryTest.php tests/Unit/Support/RetentionConfigTest.php tests/Unit/Support/Outbox/OutboxEventTest.php` — 25 tests, 25 passed, 95 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (no new exception or Data class this task; run as a cheap regression check, since neither `ArchiveOutboxEvents` nor `OutboxReplay`'s extension introduces one).
- `./vendor/bin/pest tests/Feature/Support tests/Unit/Support` — 112 tests, 112 passed, 417 assertions (regression check on the shared `OutboxReplay`, `OutboxEvent`, and `ArchiveSegmentSource` edits, and on T9's own `ActivityLogArchivalTest.php`/`ArchiveActivityLogTest.php` files sharing the `archive_segments` table).
- `./vendor/bin/pest --testsuite=Isolation` — 356 tests, 356 passed (regression check; no new tenant-scoped table this task).
- `./vendor/bin/pest --testsuite=Unit` (full) — 1242 tests, 1242 passed. `./vendor/bin/pest --testsuite=Feature` (full) — 1351 tests, 1350 passed, 1 failed on the first attempt (`ActivityLogArchivalTest::it archives old rows...`, asserting `archived === 1` but observing 42) and again on a second attempt; confirmed pre-existing and unrelated to this task's own changes by reproducing the identical failure with every T10 file (`app/` and `tests/`) reverted to the pre-T10 commit via `git stash` (leaving only the pre-existing T9 files and their own untouched behavior in place) — the failure is order-dependent cross-test pollution of the global `activity_log` table somewhere else in the ~1350-test Feature suite, not something this task's changes touch (`ArchiveOutboxEvents`, `OutboxReplay`, and every file this task edited are unrelated to `activity_log`/`ArchiveActivityLog`), and not something T10's own task scope covers fixing. Recorded here rather than hidden: this is a real gap the stage-12 gate phase (or a follow-up) should track down before claiming "all suites green," but it predates this task and this task's own scoped runs (the line above) are fully green.
- `./vendor/bin/pint --test` on every new/changed file: passed with zero errors. `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: 0 errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Commit: `99d9de5` (`feat(support): archive outbox events past retention and make replay archive-aware`).

Deviation from the plan: none in scope. Judgment calls recorded above: the new `outbox_archive_batch_size` config knob (the plan's Slice 4 unit test names "segment boundaries deterministic from configuration" without naming the specific knob); scheduling `outbox:archive` daily on the same cadence as `activity-log:archive`; and reusing the archived-and-still-live safety net implicitly (a segment already manifested but not yet deleted for every tenant, e.g. a crash mid per-tenant delete loop, is tolerated as a duplicate replay visit rather than specially reconciled, since consumer idempotence by event id already makes a duplicate visit harmless and the plan names no reconciliation mechanic for this narrow crash window).

Follow-up worth tracking (not part of this task): the `ActivityLogArchivalTest` full-Feature-suite flake noted above under Test evidence.

## T11: outbox:replay-failed command

2026-07-13 18:30 -03

Landed stage-12 plan Slice 5's first command and task breakdown item 11: a dry-run-by-default, `--operator`-required, activity-logged manual replay for the outbox's dead-lettered `ProcessOutboxDelivery` jobs (Stage 4's `failed_jobs` table) alongside any stranded pending deliveries (`OutboxSweeper`'s own definition). Independent of T12 and T13 per the plan; neither exists yet. No Data class, no endpoint, no OpenAPI path in this task (an operator CLI command with no HTTP surface, the same shape T9/T10 took), so the double-loop's contract step does not apply; Feature and Unit tests are the whole loop.

What landed:

- `App\Support\Outbox\OutboxFailedReplay`: the command's core. `candidates()` issues no writes (filters `failed_jobs` rows by `payload.displayName === ProcessOutboxDelivery::class` via the container-bound `Illuminate\Queue\Failed\FailedJobProviderInterface`, and calls `OutboxSweeper::findStranded()`, both now `public` — see below); `replay(operator, execute)` additionally re-enqueues on `$execute` and always records one `ActivityLogger` entry under `TenantTransaction::asPlatform()` naming the operator, the `execute` flag, and the matched/re-enqueued counts for both candidate kinds, whether or not `$execute` was set (task breakdown item 6 reads "writes an activity log entry naming the operator" as a property of the command, not conditioned on `--execute`; a dry-run investigation is itself auditable).
- A delivery whose job already failed and dead-lettered is itself "stranded" by `OutboxSweeper::findStranded()`'s own definition (pending, past grace, event past the stability window): nothing about `last_enqueued_at` distinguishes "never dispatched" from "dispatched once and failed", since the sweeper's own `reenqueue()` is the only writer of that column and a job that fails on its original post-record dispatch or a Horizon-side retry never touches it. Discovered by the first dry-run Feature test going red with 2 stranded deliveries instead of 1 (the failed job's own delivery counted twice, under both candidate lists) rather than anticipated up front. Fixed by excluding, from the stranded list, any delivery whose `outbox_event_id` appears as a literal substring of a matched failed job's own JSON payload — not by extracting the job's `(eventId, subscriber)` via `unserialize()` and comparing structurally, because `tests/Architecture`'s `arch()->preset()->security()` forbids `unserialize()` anywhere under `app/` with zero existing exceptions, confirmed by a second red run after the first implementation attempt used it (`Expecting 'unserialize' not to be used on 'App\Support\Outbox\OutboxFailedReplay'`). The substring match is exact in practice: constructor arguments are plain strings PHP's serializer writes out literally, and an outbox event id is a UUIDv7 (globally unique).
- Re-enqueuing a failed job mirrors `Illuminate\Queue\Console\RetryCommand::retryJob()` exactly: `Queue::connection($job->connection)->pushRaw($payload, $job->queue)` with the payload's `attempts` key reset to zero when present, never reconstructing a fresh `ProcessOutboxDelivery::dispatch()` from a deserialized command (the same `unserialize()` constraint above). A stranded delivery re-enqueues through `OutboxSweeper::reenqueue()` unchanged, which does dispatch a fresh job (that path never touches a failed job's payload, so no `unserialize()` concern applies there).
- `App\Support\Outbox\OutboxSweeper`: `findStranded()` and `reenqueue()` changed from `private` to `public` so `OutboxFailedReplay` reuses this class's own definitions rather than a second copy that could drift from the standing `outbox:sweep` sweeper; behavior unchanged, confirmed by the full pre-existing `OutboxSweeperTest` suite staying green.
- `App\Providers\AppServiceProvider`: added `$this->app->bind(FailedJobProviderInterface::class, fn ($app) => $app['queue.failer'])`, since the framework only ever binds the UUID-aware failer (`App\Support\Queue\UuidFailedJobProvider`, already extended in this provider) under the `'queue.failer'` container string key, never against its own interface, and `OutboxFailedReplay` needs a normal constructor-injected dependency rather than reaching into the container by string key itself.
- `App\Console\Commands\ReplayFailedOutboxCommand` (`outbox:replay-failed {--operator=} {--execute}`): refuses with a non-zero exit and no side effects (no query, no log entry) when `--operator` is missing or blank after `trim()`; otherwise calls `OutboxFailedReplay::replay()` and reports each candidate plus a summary line, distinct wording for dry-run versus `--execute` output.

Tests (Slice 5's own five Feature cases plus two Unit invariants):

- `tests/Feature/Support/Outbox/ReplayFailedOutboxCommandTest.php`: dry run lists one matching failed job and one stranded delivery with no side effects (`failed_jobs` row survives, delivery stays pending with `last_enqueued_at` null, nothing pushed including no raw push); `--execute` re-enqueues each exactly once (the stranded delivery via `Queue::assertPushed(ProcessOutboxDelivery::class, 1)`, the failed job via one `Queue::pushedRaw()` entry whose decoded payload's `displayName` and embedded command string both check out, `failed_jobs` row deleted, activity log entry's counts distinguish matched from re-enqueued); a duplicate-effect probe using the existing `registerIdempotentOutboxSubscriber()`/`processOutboxDeliveryTwice()` helpers (event-conventions' own mandated shape, reused rather than a bespoke counter) proves a real `--execute` re-enqueue of a stranded delivery followed by two more direct `handle()` calls for the same event/subscriber still lands exactly one effect; a dry-run activity log entry records operator, `execute: false`, and all four counts (2 matched, 0 reenqueued) under the sentinel platform tenant; missing and blank `--operator` both refuse with a non-zero exit and no new activity log row. A `dealSyntheticOutboxFailure()` helper populates a genuine `failed_jobs` row by registering a throwing test subscriber, dispatching for real under the `sync` connection (`QUEUE_CONNECTION=sync` in testing), and capturing the resulting `JobFailed` event with a `beforeEach`-registered listener that calls `queue.failer->log()` the same way `Illuminate\Queue\Console\WorkCommand` does for a live worker — an authentic payload rather than a hand-rolled one; `Event::forget(JobFailed::class)` before each registration keeps exactly one listener active per test.
- `tests/Unit/Support/Outbox/OutboxFailedReplayTest.php`: `candidates()` lists only `failed_jobs` rows whose `displayName` is `ProcessOutboxDelivery::class`, ignoring a fixture row for an unrelated class; `replay(execute: false)` reports matched counts (1 failed job) without pushing anything, deleting the `failed_jobs` row, or moving the delivery off `pending`.

Test evidence:

- Confirmed red first: ran both new test files before `OutboxFailedReplay`, `OutboxFailedReplaySummary`, or `ReplayFailedOutboxCommand` existed — every Feature case failed with `The command "outbox:replay-failed" does not exist`, both Unit cases with `Target class [App\Support\Outbox\OutboxFailedReplay] does not exist`.
- First implementation attempt (dispatch-reconstruction via `unserialize()`) went green for 6 of 8 Feature/Unit tests but red on two: the dry-run test expected 1 stranded delivery and observed 2 (the double-counting bug above), and the execute test expected exactly 2 pushed jobs and observed 3. Fixed by the substring-based dedup in `candidates()`, confirmed green, then `php artisan test --testsuite=Architecture` caught the `unserialize()` violation the two now-passing tests had not (arch rules run separately from the Feature/Unit suites); fixed by switching failed-job retry to `Queue::pushRaw()` and dedup to the substring check described above, both files updated to assert against the new `pushRaw`-based mechanics, re-ran green.
- `./vendor/bin/pest tests/Feature/Support/Outbox/ReplayFailedOutboxCommandTest.php tests/Unit/Support/Outbox/OutboxFailedReplayTest.php` — 8 tests, 8 passed, 45 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (confirms the `unserialize()` fix; no new exception or Data class this task).
- `./vendor/bin/pest tests/Feature/Support tests/Unit/Support` — 120 tests, 120 passed, 462 assertions (regression check on the shared `OutboxSweeper` visibility change and the new `AppServiceProvider` binding).
- `./vendor/bin/pint --test` on every new/changed file: passed with zero errors. `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: 0 errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Commit: `59bc52c` (`feat(support): add outbox:replay-failed operational command`).

Commit: `6d03849` (`feat(payments): bind a timestamp into fake gateway webhook signatures and run the negative matrix over every registered gateway`).

Deviation from the plan: none in scope. One design decision not spelled out in the plan text, recorded above: the "matching failed jobs" and "stranded deliveries" candidate lists are made mutually exclusive (a delivery behind a dead-lettered job is reported only once, as a failed job, never doubly as a stranded delivery too), since the plan's own wording ("lists matching failed jobs and stranded deliveries") does not say whether the two can overlap and an uncorrected overlap would both misreport counts and re-enqueue the same delivery twice on `--execute` (harmless under consumer idempotence, but a misleading audit trail). A second judgment call: failed-job retry pushes the original serialized payload back raw (`Queue::pushRaw`, Laravel's own `queue:retry` mechanics) rather than reconstructing a fresh `ProcessOutboxDelivery::dispatch()` call, driven by the `unserialize()` architecture constraint rather than a stated plan preference; this also means a failed-job replay is not observable through `Queue::fake()->assertPushed(ProcessOutboxDelivery::class, ...)` the way a stranded-delivery replay is, so its test coverage instead inspects `Queue::pushedRaw()`.

## T12: payments:reconcile-orders command

2026-07-13 18:56 -03

Landed stage-12 plan Slice 5's second command and task breakdown item 12: a dry-run-by-default, `--operator`-required, activity-logged manual poll of named `awaiting_payment` orders, reusing the Stage 8a poller's own adapter-poll and conditional-UPDATE machinery for exactly the named orders. No Data class, no endpoint, no OpenAPI path in this task (an operator CLI command with no HTTP surface, the same shape T9 through T11 took), so the double-loop's contract step does not apply; Feature and Unit tests are the whole loop. Independent of T11 and T13 per the plan.

Naming deviation (a genuine collision, resolved rather than blocked on): the plan's own Slice 5 text and task breakdown item 12 both name the command literally `payments:reconcile`, but that exact signature already belongs to the Stage 8a automatic sweep (`App\Console\Commands\ReconcilePendingPaymentsCommand`, scheduled `->everyFiveMinutes()` in `bootstrap/app.php` since stage-08a). Two Artisan commands cannot share one signature (Symfony Console's `Application::add()` silently lets the second registration overwrite the first in its internal name-keyed map, so one of the two would become permanently unreachable with no error at boot). Verified the collision directly: `php artisan list` before this task showed only the automatic sweep at `payments:reconcile`; grepping every `$signature` in `app/Console/Commands` confirmed no other file claims it. Renamed the new manual command to `payments:reconcile-orders`, the same "distinct verb from its automatic sibling" naming precedent `holds:release-stuck` (T13, not yet landed) already takes against `holds:release-expired` and `outbox:replay-failed` (T11) already takes against `outbox:sweep`, since neither of those two pairs collides literally but both keep the manual and automatic paths nameably distinct. The command's own docblock and this journal entry record the deviation; no plan or design document edit was needed since the plan's Slice 5 prose describes behavior, not a binding interface contract the way an OpenAPI path would.

What landed:

- `App\Orders\Data\AwaitingPaymentOrderData` and `App\Orders\Actions\FindAwaitingPaymentOrders`: the candidate lookup, mirroring `OrderExportRowData`/`GetOrderEventIds`'s own "plain internal class returned through the owning context's Action" shape (event-conventions: Payments never queries the orders table directly). Bounded by an order ID allowlist (empty means "no order filter," not "no orders"), an optional tenant, and a required `created_at <=` cutoff; status is always `AwaitingPayment`, unconditionally.
- `App\Payments\Support\ReconcileNamedOrdersSummary` and `App\Payments\Actions\ReconcileNamedOrders`: the command's core, mirroring `App\Support\Outbox\OutboxFailedReplay`/`OutboxFailedReplaySummary`'s own two-method shape (`candidates()` issues no writes; `reconcile()` additionally resolves on `$execute` and always activity-logs, dry-run included, under the sentinel platform tenant). `reconcile()` resolves the named orders' own `initiated` payments (looked up by `order_id`, not by the automatic sweep's own age-based cutoff) through `App\Payments\Actions\ReconcilePendingPayments::reconcile()` — a new public method extracted from that class's own `__invoke()` loop (`__invoke()` now calls `$this->reconcile($this->findCandidates())` unchanged in behavior), so both the automatic sweep and this manual command share one adapter-poll and conditional-UPDATE path rather than a second copy that could drift from it. An order with no `initiated` payment (already resolved, or never one at all) is silently excluded from the payments queried, distinguishing "orders matched" from "orders resolved" in both the summary and the activity log entry, mirroring T11's own matched-versus-reenqueued counts.
- `App\Console\Commands\ReconcileNamedOrdersCommand` (`payments:reconcile-orders {--operator=} {--order=*} {--tenant=} {--before=} {--execute}`): refuses with no side effects when `--operator` is blank, when neither `--order` nor `--tenant` is given, or when `--before` is blank or fails to parse (`Illuminate\Support\Facades\Date::parse()`); otherwise calls `ReconcileNamedOrders::reconcile()` and reports each candidate order plus a summary line. Not scheduled: an operator-invoked command bounded by required arguments, the same unscheduled posture `outbox:replay-failed` already takes.

Tests (Slice 5's own named cases plus unit invariants):

- `tests/Unit/Orders/FindAwaitingPaymentOrdersTest.php`: excludes every non-`awaiting_payment` status; filters by the order ID allowlist; filters by tenant; excludes an order created after the `before` cutoff; combines an allowlist and a tenant filter as an intersection (empty when they disagree).
- `tests/Unit/Payments/ReconcileNamedOrdersTest.php`: `candidates()` alone issues no writes (a dry-run investigation is safe to run freely); `reconcile(execute: false)` resolves nothing yet still records one activity log entry; matched-versus-resolved counts stay distinguishable when one named order carries no `initiated` payment to poll (`orders_matched: 2`, `orders_resolved: 1`).
- `tests/Feature/Payments/ReconcileNamedOrdersCommandTest.php`, built on real order/hold/payment fixtures over the storefront HTTP endpoints (`reconcileOrdersTenantFixture()`/`reconcileOrdersOrderFixture()`, adapted from `PaymentExpiryAndReconcileTest.php`'s own `expiryFixture()` to build more than one order under a shared tenant): refuses without `--operator`; refuses fully unbounded (no `--order`, `--tenant`, or `--before` at all); refuses without `--order` or `--tenant` even with `--before` given; refuses without `--before` even with `--order` given (all four refusal cases assert a before/after activity-log-count delta of zero, not an absolute zero — see the "activity_log never truly deletes" note below); dry-run reports the named order with no side effect (payment stays `initiated`, order stays `awaiting_payment`) and still records a dry-run activity log entry; `--execute` polls exactly the named order and no others, transitioning the named order's payment to `confirmed`, its order to `paid`, and issuing 2 tickets, while a second, unnamed sibling order in the same tenant stays untouched; `--tenant` bounds to every `awaiting_payment` order in that tenant and never another tenant's.

Self-inflicted bugs found and fixed mid-loop, both recorded for the next reader:

- Building two orders for two different customers under one tenant within a single test (needed to prove "exactly the named orders and no others") intermittently converted the second hold against the *first* customer's id, throwing `HoldNotFoundException` even though the hold row provably existed (confirmed by a direct query immediately before the failing HTTP call). Root-caused by reproducing the minimal case standalone: the customer auth guard resolves and caches the bearer token's user for the duration of the test process, and `Auth::forgetGuards()` — already called once per fixture, after the order call and before the payment call, mirroring `expiryFixture()`'s own placement — was never called again before starting the *next* customer's login, so the second `POST .../storefront/orders` request resolved against the first customer's cached identity. Fixed by adding a second `Auth::forgetGuards()` call at the end of `reconcileOrdersOrderFixture()`, so repeated calls are independent; `expiryFixture()` itself needed no equivalent fix since it is called at most once per test.
- `activity_log` grants `nodia_platform` a DELETE restricted to rows past the retention pruner's own cutoff (stage-12 plan, task breakdown item 8, landed by T8/T9): an ordinary `ActivityLogEntry::query()->delete()` with no `app.activity_log_prune_cutoff` session variable set (the ordinary case for every posture except the archiver) affects zero rows, not all of them. An `afterEach` cleanup call written against the old (pre-Stage-12) assumption that platform-role deletes worked unconditionally left every activity log entry this suite ever recorded permanently in the test database, which a later run's `->sole()`/exact-count assertions then failed against (2 leftover rows from an earlier interrupted run). `tests/Feature/Support/Outbox/ReplayFailedOutboxCommandTest.php` (T11) had already solved this the same way and was read for the fix: drop the ineffective delete entirely, scope every activity-log assertion by a unique `properties->operator` value per test with `->latest('created_at')->first()`, and use a before/after count delta rather than an absolute count for the "records nothing" refusal cases. Two stray rows from the debugging run were purged directly from the shared Postgres test database (`payments_reconcile_orders_invoked`) before the final green run, and no test file's cleanup now assumes a delete it cannot make.

Test evidence:

- Confirmed red first: ran all three new test files before `AwaitingPaymentOrderData`, `FindAwaitingPaymentOrders`, `ReconcileNamedOrdersSummary`, `ReconcileNamedOrders`, or `ReconcileNamedOrdersCommand` existed. The Orders Unit file failed every case with `Class "App\Orders\Actions\FindAwaitingPaymentOrders" not found`; the Payments Unit file the same way against `App\Payments\Actions\ReconcileNamedOrders`; the Feature file's all seven cases failed with `The command "payments:reconcile-orders" does not exist.`
- Green after implementation: `./vendor/bin/pest tests/Unit/Orders/FindAwaitingPaymentOrdersTest.php tests/Unit/Payments/ReconcileNamedOrdersTest.php tests/Feature/Payments/ReconcileNamedOrdersCommandTest.php` — 15 tests, 15 passed, 62 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (no new exception or Data class this task; run as the context-boundary regression check, confirming `App\Payments\Actions\ReconcileNamedOrders` importing `App\Orders\Data\AwaitingPaymentOrderData` and `App\Orders\Actions\FindAwaitingPaymentOrders` — Data and Actions, never `App\Orders\Models` — stays inside the existing "only the Orders context uses its own Models" rule with no new exemption needed).
- `./vendor/bin/pest tests/Feature/Payments tests/Unit/Payments tests/Feature/Orders tests/Unit/Orders` (the touched directories plus their sibling suites, since `ReconcilePendingPayments` is a shared file) — 586 tests, 586 passed, 2122 assertions.
- `./vendor/bin/pest --testsuite=Isolation` — 356 tests, 356 passed (regression check; no new tenant-scoped table this task).
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed `app/` file: both pass with zero errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run for this task's own changes. Running it anyway to double check surfaced an unrelated, pre-existing stale-generation gap (`ArchiveSegmentSource`, left over from T9/T10 the same way T2's journal already documented one such gap for `Capability`): reverted that diff (`git checkout -- packages/api-client/...`) since it is out of scope for this task and would misattribute an earlier task's drift to this commit.

Commit: `d63ab97` (`feat(payments): add payments:reconcile-orders operational command`).

Deviation from the plan: the `payments:reconcile` to `payments:reconcile-orders` naming change above, forced by a genuine signature collision with the already-shipped Stage 8a automatic sweep, not a discretionary judgment call.

## T13: holds:release-stuck command with the sweeper-race concurrency test

2026-07-13 19:07 -03

Landed stage-12 plan Slice 5's third command and task breakdown item 13: a dry-run-by-default, `--operator`-required, activity-logged manual release of holds stuck `active` past `expires_at`, releasing each matched hold through the Stage 6 `ReleaseHold` Action rather than a second conditional-UPDATE implementation. No literal-name collision this time (`holds:release-expired` already exists but is spelled differently from the plan's own `holds:release-stuck`), so no naming deviation was needed; the command's own docblock still calls out the "distinct verb from its automatic sibling" precedent `outbox:replay-failed` (T11) and `payments:reconcile-orders` (T12) already established, since T12's own journal entry named this task by that expectation before it landed. No Data class, no endpoint, no OpenAPI path in this task (an operator CLI command with no HTTP surface, the same shape T9 through T12 took), so the double-loop's contract step does not apply; Feature, Unit, and Concurrency tests are the whole loop. Independent of T11 and T12 per the plan.

What landed:

- `App\Inventory\Support\ReleaseStuckHoldsSummary` and `App\Inventory\Actions\ReleaseStuckHolds`: the command's core, mirroring `App\Support\Outbox\OutboxFailedReplay`/`OutboxFailedReplaySummary` and `App\Payments\Actions\ReconcileNamedOrders`/`ReconcileNamedOrdersSummary`'s own two-method shape. `candidates(tenantIds)` issues no writes (a platform-scoped `SELECT ... WHERE status = 'active' AND expires_at <= now()`, optionally bound by a tenant ID allowlist, the same filter `App\Inventory\Actions\ReleaseExpiredHolds::findCandidates()` already uses) — a `committed` hold past `expires_at` is never a candidate since the query's own status filter excludes it outright, proving system-design 7.1's "inventory already moved held to sold on the paid transition" without needing a special case. `release(operator, execute, tenantIds)` additionally releases every matched hold through `App\Inventory\Actions\ReleaseHold` on `$execute`, one hold at a time inside that hold's own `TenantTransaction::asTenant` posture (mirroring `ReleaseExpiredHolds::expireOne`'s own per-hold transaction boundary, so a failure releasing one hold cannot roll back another's), and always records one activity log entry under the sentinel platform tenant naming the operator, the tenant-ID arguments, and the matched/released counts, whether or not `$execute` was set, mirroring T11/T12's own "a dry run is itself auditable" posture.
- `ReleaseHold::__invoke()` stayed untouched (still `void`, no return-value change): it is already exactly-once safe against the standing sweeper on its own (proven by `tests/Concurrency/HoldExpiryRecoveryContentionTest.php`, landed in stage-06 and re-run here as a regression check), so `release()` never needed to ask it who won a given race. Instead, `released` is computed by a second, read-only pass after every `--execute` attempt has committed: re-querying, per tenant, how many of the matched hold IDs now carry status `released`. A hold the sweeper won the race for shows up `expired` there instead, correctly excluded from this command's own count without inspecting `ReleaseHold`'s internals or changing its signature — the minimal-footprint alternative to making `ReleaseHold` return a bool, chosen because this task's own plan text scopes it to reusing the Stage 6 Action, not modifying it, and Stage 6's own `ReleaseHold` call sites (`HoldController::destroy`, `CancelOrder`, `MarkOrderExpired`, `MarkOrderFailed`) all ignore its return value today, so a signature change would have been non-load-bearing everywhere except this new caller.
- `App\Console\Commands\ReleaseStuckHoldsCommand` (`holds:release-stuck {--operator=} {--tenant=*} {--all-tenants} {--execute}`): refuses with no side effects when `--operator` is blank; refuses `--execute` with neither `--tenant` nor `--all-tenants` given ("Refuses unbounded execution without an explicit `--all-tenants` flag", stage-12 plan Slice 5's own wording for this command, read literally as scoping the refusal to `--execute` rather than to every invocation — unlike `payments:reconcile-orders`, whose own plan wording required a bound on every run including dry-run listings). A dry run with no `--tenant` and no `--all-tenants` therefore still lists across every tenant: a read-only investigation is safe to run unbounded, the same posture `outbox:replay-failed`'s own candidate lists already take with no bound argument at all. `--all-tenants` takes precedence over any `--tenant` values given alongside it (an operator combining both gets the wider scope, not an error) since the plan does not call out that combination as a refusal case and an unnecessary refusal there would only get in an operator's way. Not scheduled: an operator-invoked command bounded by required arguments, the same unscheduled posture `outbox:replay-failed` and `payments:reconcile-orders` both already take.

Tests (Slice 5's own four Feature cases plus five Unit invariants plus the one Concurrency simulation):

- `tests/Unit/Inventory/ReleaseStuckHoldsTest.php`: `candidates()` lists an active hold past `expires_at` and excludes one not yet expired; `candidates()` never lists a committed hold even long past `expires_at`; `candidates()` bounds by tenant when a tenant ID list is given; `release(execute: false)` issues no writes (hold stays `active`, `held` counter untouched) yet still records an activity log entry with `holds_released: 0`; `release(execute: true)` releases the matched hold through `ReleaseHold`, recovers `held` to exactly zero and availability to exactly `quantity`, and records `holds_matched`/`holds_released` both `1`.
- `tests/Feature/Inventory/ReleaseStuckHoldsCommandTest.php`, built on direct `App\Inventory\Actions\CreateHold`/`CommitHold` fixtures (no HTTP surface needed, the same posture the Unit test takes, since this is a CLI-only command): refuses without `--operator`; refuses `--execute` with neither `--tenant` nor `--all-tenants`; a dry-run `--all-tenants` listing includes an active hold past `expires_at` in its output and excludes a committed one from ever being released, with the committed hold's own status and the active hold's own counters left untouched, plus a dry-run activity log entry with `execute: false` and `holds_released: 0`; `--execute --tenant=<id>` releases the matched hold through `ReleaseHold`, recovers availability to exactly `quantity - sold`, and records `holds_matched: 1`, `holds_released: 1`; `--tenant` bounds `--execute` to that tenant's own stuck hold only, leaving a sibling tenant's stuck hold `active`.
- `tests/Concurrency/HoldReleaseStuckSweeperRaceContentionTest.php`, adapted directly from `HoldExpiryRecoveryContentionTest`'s own "never double-reconciles or double-records when an explicit release races the sweeper" scenario (`Tests\Concurrency\Support\ParallelRunner::runEach`, two forked workers against real PostgreSQL): one worker calls `App\Inventory\Actions\ReleaseStuckHolds::release('race-operator', true, [$tenantId])`, the other calls the standing `App\Inventory\Actions\ReleaseExpiredHolds` sweeper directly, both racing for the same stuck hold. Asserts the hold's terminal status is exactly one of `released`/`expired`, the `held` counter is exactly zero, availability recovers to exactly `quantity`, and exactly one of `HoldReleased`/`HoldExpired` is recorded in the outbox — the same invariant stage-06's own test already proved for the DELETE endpoint racing the sweeper, now re-proven for this command's own `--execute` path racing it.

Test evidence:

- Confirmed red first: ran all three new test files before `ReleaseStuckHoldsSummary`, `ReleaseStuckHolds`, or `ReleaseStuckHoldsCommand` existed. The Unit file failed every case with `Target class [App\Inventory\Actions\ReleaseStuckHolds] does not exist`; the Feature file's cases with `The command "holds:release-stuck" does not exist.`; the Concurrency file the same way as the Unit file (`app(ReleaseStuckHolds::class)` inside one of the two racing closures). Two of the ten Unit/Feature cases (the ones exercising `App\Inventory\Actions\CommitHold`) initially failed on a different, self-inflicted error before that — `CommitHoldData::from(['hold_id' => $holdId])` — because, unlike this repo's global laravel-data assumption, `config/data.php`'s own `name_mapping_strategy` is `null` for both input and output (no automatic snake_case mapping is configured), so `CommitHoldData`'s constructor parameter must be addressed by its own camelCase name (`holdId`) exactly as `tests/Feature/Inventory/HoldEndpointsTest.php` already does; fixed by correcting both call sites before writing any production code, confirmed still red for the intended reason (missing classes/command) afterward.
- Green after implementation: `./vendor/bin/pest tests/Unit/Inventory/ReleaseStuckHoldsTest.php tests/Feature/Inventory/ReleaseStuckHoldsCommandTest.php` — 10 tests, 10 passed, 45 assertions. `./vendor/bin/pest tests/Concurrency/HoldReleaseStuckSweeperRaceContentionTest.php --testsuite=Concurrency` — 1 test, 1 passed, 4 assertions.
- `./vendor/bin/pest tests/Feature/Inventory tests/Unit/Inventory` (the touched directories plus their sibling suites) — 247 tests, 247 passed, 754 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (no new exception or Data class this task; confirms `App\Inventory\Actions\ReleaseStuckHolds` importing only `App\Inventory\Models\Hold` and `App\Inventory\Actions\ReleaseHold`, both already inside the Inventory context's own boundary, needs no new exemption).
- `./vendor/bin/pest --testsuite=Isolation` — 356 tests, 356 passed (regression check; no new tenant-scoped table this task, count matches T12's own run).
- `./vendor/bin/pint --test` and `./vendor/bin/phpstan analyse --memory-limit=1G` on every new/changed file (`ReleaseStuckHoldsCommand.php`, `ReleaseStuckHolds.php`, `ReleaseStuckHoldsSummary.php`, and the three test files): both pass with zero errors.
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Commit: `cf07bf9` (`feat(inventory): add holds:release-stuck operational command`).

Deviation from the plan: none. One design decision not spelled out in the plan text, recorded above: `release()`'s `holds_released` count is derived from a post-execution re-query of the matched holds' own current status rather than from `ReleaseHold`'s own return value, since `ReleaseHold` stayed untouched (still `void`) rather than being changed to report whether it personally won a given race — the plan names `ReleaseHold` as the Action to release through, not a signature to change, and every existing call site already ignores its return value. A second reading call: the plan's "refuses unbounded execution without an explicit `--all-tenants` flag" is read as scoping the refusal to `--execute` specifically, not to dry-run listings too, since the word used is "execution" and an unbounded dry-run investigation carries none of the blast-radius risk an unbounded `--execute` does — a narrower reading than `payments:reconcile-orders`'s own plan wording took (T12's own command required a bound argument on every invocation, dry run included), but the two commands' plan sentences are worded differently and this task's own text supports the narrower reading on its own terms.

## T14: Coverage completeness, authorization matrix, and payload PII meta-tests

2026-07-13 19:40 -03

Landed stage-12 plan Slice 6 and task breakdown item 14: three meta-tests that enumerate the *live* route table and the *live* payload class list rather than a hand-copied inventory, so a route or payload added after this task is checked automatically and cannot merge without an explicit coverage decision. No Data class, no endpoint, no OpenAPI path (test and test-support code only), so the double-loop's contract step does not apply.

Provenance note, recorded because it changes how much this entry's evidence is worth: this task was implemented by a subagent inside the `/implement-stage` workflow, which was killed by the operator partway through the stage (after T14's code was written and left uncommitted, before the workflow reached its own gate, review, or close-out phases). This entry was therefore written after the fact, from the committed code and from verification runs done at close-out time, not from the implementing agent's own observed loop. Where a claim below was verified at close-out it says so; where it was not, it says that too. The remaining stage tasks (T15 through T18) were never started.

What landed:

- `App\Support\Testing\RouteAuthorizationCoverage`: every mutating (POST/PUT/PATCH/DELETE) `/v1` route mapped to either the `App\Identity\Capability` gating it (35 entries) or a reasoned exemption (23 entries, all self-service or webhook-signature-authenticated paths where no staff capability applies). Four routes resolve their capability dynamically in-controller rather than through static `RequireCapability` middleware (the target depends on request state the route does not carry: `DELETE /v1/media/{media}` follows the media's owning model, `POST /v1/check-ins` and `POST /v1/check-in-batches` follow the scan's QR payload, `POST /v1/customers/{customer}/data-subject-requests` follows the request body's type); those list every capability the dynamic check can pick between, pipe-separated, and are commented as such so the distinction stays visible.
- `App\Support\Testing\RouteIsolationCoverage`: every registered `/v1` route mapped to either the Isolation suite test class proving cross-tenant denial for its underlying table (~100 entries) or a reasoned exemption (18 entries: health, webhook ingestion, the static capability registry read, `/v1/me`, the self-service auth paths over `App\Support\Database\UnscopedTables`, and the two Redis-backed waiting-room routes whose isolation is proven by `tests/Unit/Inventory/OnSaleQueueTest.php` instead of by RLS). This is a traceability record tying each route to the test that proves the mechanism for its table, not a re-proof of RLS itself.
- `App\Support\Testing\OutboxPayloadPiiExemptions`: field-level exemptions for the payload PII scan, keyed `PayloadClass::$property` so a rename or a second colliding field surfaces rather than silently exempting the wrong one. Exactly one entry today: `TenantCreatedPayload::$name`, the tenant's own business name (system-design 8.1 scopes customer PII to `customers.name`/`customers.email`, not the tenant record).
- `tests/Feature/Support/RouteCoverageCompletenessTest.php`: four cases over `Illuminate\Routing`'s own registry — every `/v1` route has an isolation entry or exemption; every `/v1` route has an OpenAPI path (via the existing `Tests\Support\OpenApiSpec`); every *mutating* `/v1` route has an authorization entry or exemption; and every statically gated entry's recorded capability still matches the live `RequireCapability` middleware parameter on that route (so the map cannot drift from the middleware it documents). Read-only routes carry no authorization-completeness requirement here by deliberate scoping: capability leakage on a read is a separate concern from write authorization.
- `tests/Feature/Support/OutboxPayloadPiiTest.php`: reflects over every `app/*/Events/*Payload.php` class that is a laravel-data `Data`, and fails any constructor parameter whose snake_cased name or whose declared type's base name matches `email`, `name`, `document`, `passport`, `cpf`, `ssn`, or `tax_id`, or that is typed as an Eloquent `Model` (which would embed the full row, PII included, into a retained payload). The reason this matters is erasure-versus-replay: system-design 14.3 anonymizes `customers.name`/`customers.email` in place, but outbox rows are retained and replayed (9.1), so a payload that ever carried a customer's name or email would defeat erasure the moment history replays.
- `tests/Feature/Identity/AuthorizationMatrixTest.php` (one case appended, existing file otherwise untouched): asserts every capability named in `RouteAuthorizationCoverage::covered()` is a live `Capability` case. This deliberately ties the route dimension into the file's existing capability-by-role matrix rather than duplicating it as a second HTTP-driven role-by-route matrix: the existing dataset already proves, for every `Capability::cases()` member, that a role lacking it is denied at the same evaluation point every real route reaches (`CapabilityGate::authorize()`, reached by both `RequireCapability` and the in-controller checks), so the only missing link was from the route map back to the live registry. A route entry naming a renamed or deleted capability now fails here instead of silently never being deny-proven.

Test evidence (all run at close-out, on the committed tree):

- `./vendor/bin/pest tests/Feature/Support/OutboxPayloadPiiTest.php tests/Feature/Support/RouteCoverageCompletenessTest.php tests/Feature/Identity/AuthorizationMatrixTest.php` — 139 tests, 139 passed, 1322 assertions.
- `php artisan test --testsuite=Architecture` — 43 tests, 43 passed (regression check; `App\Support\Testing` is shared support code importing only `App\Http\Middleware\RequireCapability` and `App\Identity\Capability`, so no new context-boundary exemption was needed).
- `./vendor/bin/pint --test` on every new and changed file: passes.
- No Data class changed, so `composer -d apps/api run types:generate` was not run.

Not verified, stated plainly rather than assumed:

- **The red-first step was not observed.** Both new test files carry comments claiming they were proven red against a seeded gap during development (a synthetic uncovered route; a temporary `public string $customerEmail` on a payload class), and those comments forward-reference this journal entry. The claims are plausible and the tests do fail on those inputs by construction, but the workflow was killed before it recorded the runs, and no red output was witnessed at close-out. Treat the comments as the implementing agent's assertion, not as evidence this journal stands behind.
- The full Pest suite, the Isolation suite, and the frontend gates were not run for this task; only the three affected test files plus Architecture were.

Pre-existing gate failure found at close-out, **not caused by this task and not fixed here**: the configured Larastan run (`./vendor/bin/phpstan analyse`, which per `phpstan.neon` analyses `app` only, at level 5) fails with one error — `app/Support/Problems/ProblemRenderer.php:105`, "Match expression does not handle remaining values: `ErrorCode::CustomerAlreadyAnonymized|ErrorCode::DataSubjectRequestAlreadyOpen`". Both enum cases were introduced by T2 (`c72905c`, `feat(identity): add AnonymizeCustomer, CustomerAnonymized event, erasure endpoint`); T14's commit touches neither `ProblemRenderer` nor `ErrorCode`. The branch has therefore been red on Larastan since T2, and the workflow never caught it because it died before reaching its gate phase. Left for whoever picks the stage back up. (Note for that reader: pointing Larastan at `tests/` paths explicitly, outside its configured `app`-only scope, produces ~215 spurious Pest-idiom errors; those are an invocation artifact, not real findings.)

Commit: `9425c11` (`test(support): add coverage completeness, authorization matrix, and payload PII meta-tests`).

Deviation from the plan: none in the code. Process deviation: the stage was abandoned after this task at the operator's direction, so T14 landed without the workflow's codex review rounds, without the full local quality gate, and without a push or CI verification. T15 through T18 remain unstarted and the stage checklist reflects that.

## T15: Webhook negative matrix across registered gateways

2026-07-13 20:55 -03

Landed stage-12 plan Slice 6's webhook negatives and task breakdown item 15: the four forgeries (missing signature, body changed after signing, signature from a foreign key, signature outside the freshness window) are now driven over the real ingestion route against every adapter the `GatewayRegistry` binds, enumerated live rather than named by hand, and each must be rejected with `webhook_signature_invalid` before anything is persisted or queued. No Data class and no new endpoint (the 401 arm of `POST /v1/webhooks/{gateway}` is already an OpenAPI-documented response, asserted here with `assertConformsToOpenApi()`), so the double-loop's contract and TypeScript steps do not apply.

Scheme change, decided with the operator before any code was written: the stale-timestamp arm had nothing to bite on. FakeGateway signed `HMAC(secret, body)` with no time component anywhere, so a captured delivery stayed valid forever and no adapter in the registry could demonstrate the rejection the slice requires. The three alternatives put to the operator were a timestamp header bound into the signature (Stripe's shape), a timestamp inside the signed body, and waiving the arm for untimestamped schemes with a reasoned exemption. The operator chose the header. So FakeGateway now signs `HMAC(secret, "{timestamp}.{body}")` and sends `X-Fake-Timestamp` alongside `X-Fake-Signature`; `parseWebhook` rejects a missing or non-numeric timestamp, a signature that does not cover the timestamp it was sent with, and a timestamp further from now than `payments.gateways.fake.webhook_tolerance_seconds` (default 300, `FAKE_GATEWAY_WEBHOOK_TOLERANCE_SECONDS`). Binding the timestamp into the HMAC, rather than checking a free-floating header, is what stops a caller from pasting a fresh timestamp onto a captured signature; there is a unit case for exactly that. The waive option was rejected in the ask because the only working gateway would have waived it, leaving the requirement vacuous until a real adapter lands.

What landed:

- `App\Payments\Gateways\FakeGateway`: the timestamped signing scheme above, on both the emitter (`sign()`) and the verifier (`parseWebhook()`). Time comes from `Illuminate\Support\Facades\Date`, so frozen-clock and time-travelling tests sign and verify against the same clock they manipulate.
- `App\Payments\Gateways\FakeWebhookDelivery::serverHeaders()`: the delivery's headers in the `HTTP_*` server-parameter form a test request takes. Every call site that used to reach into `$delivery->headers['X-Fake-Signature']` by name now spreads this in, so the next scheme change (a second signing secret during rotation, say, per the fixtures README) touches the adapter and not fourteen test files.
- `config/payments.php`: `payments.gateways.fake.webhook_tolerance_seconds`.

Tests:

- `tests/Feature/Payments/WebhookSignatureNegativeMatrixTest.php` (new): three cases. A completeness case asserting every slug in the live `GatewayRegistry` has a probe declared. The matrix itself, iterating gateway by arm over the real HTTP route with `Queue::fake()`, asserting 401, `webhook_signature_invalid`, OpenAPI conformance, zero rows in `gateway_webhook_events`, and zero `ProcessGatewayWebhook` pushes. And a positive control asserting each gateway still accepts what it itself signs, so the negatives cannot pass vacuously (a verifier that rejected everything would satisfy the matrix alone).
- `tests/Support/Payments/WebhookNegativeProbe.php` and `WebhookNegativeProbes.php` (new): the per-adapter forgeries, one probe per registered slug. `PendingGatewayAdapter`'s probe carries `acceptsValidDelivery: false` with the reason stated in place (stage-08d, Slice 3: the skeleton rejects every webhook by contract, so it has no positive control to run).
- `tests/Support/Payments/FakeGatewaySignature.php` (new): the signing scheme restated on the test side, so a test can sign a body the gateway would never emit and the negative matrix can forge a foreign-key or backdated signature. The adapter is therefore verified against an independent statement of its scheme rather than against itself.
- `tests/Unit/Payments/FakeGatewayTest.php`: two new adapter-level cases (the freshness window's boundary, accepted at exactly the tolerance and rejected one second past it; and a fresh timestamp pasted onto a backdated signature, rejected). The existing tampered-signature case now keeps the valid timestamp so it still isolates the signature.
- Fourteen existing files updated to the two-header scheme, no behavior changed: the payments, reporting, contract, and concurrency tests that post fake webhooks.

Test evidence:

- Red first, witnessed: with the matrix written and the adapter untouched, `./vendor/bin/pest tests/Feature/Payments/WebhookSignatureNegativeMatrixTest.php` failed with `Undefined array key "X-Fake-Timestamp"` — the wrong-key and stale arms could not even be forged against a scheme with no timestamp. The missing-signature and tampered-body arms passed from the start; those two were already covered by Stage 8a, which is why this task's plan line calls it a re-run.
- Meta-test proven against a seeded gap, witnessed: registering a third adapter (`$adapters['seeded_gap'] = new PendingGatewayAdapter('seeded_gap')`) in `PaymentsServiceProvider` made two of the three cases fail — the completeness case reporting `seeded_gap` as undeclared, and the matrix's own `assertNotNull` on the missing probe. The provider edit was reverted (`git checkout`, diff confirmed empty) and the file is untouched in this commit.
- `./vendor/bin/pest tests/Unit/Payments tests/Feature/Payments` — 388 passed. `./vendor/bin/pest tests/Contract tests/Feature/Reporting` — 573 passed. `php artisan test --testsuite=Concurrency` — 65 passed. `php artisan test --testsuite=Architecture` — 43 passed.
- `composer analyse` (Larastan) — passed, 0 errors. The T2-era `ProblemRenderer` error T14 recorded is gone, fixed by `32e89ae`/`2488cae`. (Invoking `./vendor/bin/phpstan analyse` directly, outside the Composer script, OOMs at PHP's default 128M memory limit; that is an invocation artifact, not a finding. Use `composer analyse`.)
- `./vendor/bin/pint --test` — passed.
- Full suite, `./vendor/bin/pest` — 3584 tests, 3583 passed, **1 failed**: `tests/Feature/Support/ActivityLogArchivalTest.php:114`, "Failed asserting that 42 is identical to 1". **Pre-existing and not caused by this task**, verified rather than assumed: stashing this task's entire diff and re-running the full suite on the clean tree reproduces the identical failure (3579 tests, same test, same message). T9's archival test sets a 2-day retention window and expects to archive exactly the one old row it inserted, but in a whole-suite run it also sweeps up `activity_log` rows left behind by earlier tests that travel to dates more than two days back (the payments suites travel to 2026-07-11). It passes in isolation. Left for whoever picks up the stage; it is a test-isolation defect in T9, not a defect in the archiver.

Commit: `6d03849` (`feat(payments): bind a timestamp into fake gateway webhook signatures and run the negative matrix over every registered gateway`).

Deviation from the plan: none in scope. One design decision the plan did not spell out, recorded above and confirmed with the operator before implementation: FakeGateway's signing scheme gained a bound timestamp, because the plan's stale-timestamp arm is unprovable against a scheme with no time in it.

## T16: env architecture test and .env.example assertions (secret scanner dropped)

2026-07-13 21:40 -03

Landed two of stage-12 plan Slice 6's four secret-handling arms and part of task breakdown item 16. **The secret scanner arm was dropped at the operator's direction and did not land**; see the deviation at the end of this entry, which the stage's own exit criterion 8 depends on. No Data class, no endpoint, no OpenAPI path (test-only code), so the double-loop's contract step does not apply.

What landed:

- `tests/Architecture/SecretHandlingTest.php`, two cases in the Architecture suite (no database, no application boot, matching `TimeSourceTest`'s own Finder-scan shape):
  - *The environment is read only in config files*. A Finder scan over the application's runtime code (`app`, `bootstrap`, `database`, `routes`, `public`, and `artisan`) failing any line that calls the `env` helper or reaches the `Env` facade behind it, with a lookbehind that leaves `getenv`, `$app->environment()`, and any `$this->env` property alone. Zero violations today: the API already reads the environment in `config/` only. The scan deliberately stops short of `tests/`, and the test says why in place: the reason for the ban is config caching (`php artisan config:cache` makes the helper return null for anything the cached config did not resolve, system-design 14.1 and ADR 017's env-only configuration posture), and no test process ever caches config. The two reads the harness does make (`Tests\Support\PostgresTestDatabase` building its PostgreSQL connection, and the Redis queue name in `tests/Feature/Support/Outbox/OutboxRedisDeliveryTest.php`) both happen before there is any config entry to read instead, so a literal repo-wide ban would have had to exempt them by name anyway. Scoping the scan to runtime code states the same rule without an exemption list that would read as two tolerated violations.
  - *Committed env examples carry placeholders rather than credentials*. Enumerates every `.env.example` in the repo live (all five: root, `apps/api`, `apps/storefront`, `apps/admin`, `apps/checkin`), so a sixth workspace added later is checked without touching this test, and asserts on each assignment two ways. Secret-shaped keys (`KEY`, `SECRET`, `TOKEN`, `PASSWORD`, `PASSPHRASE`, `CREDENTIAL`, `PRIVATE`, `SALT`, `SIGNATURE`, `DSN`) must be blank, `null`, boolean, or numeric, which lets `PASSPORT_ACCESS_TOKEN_TTL_MINUTES=15` and its siblings through without a per-key exemption, since a TTL carries a secret-shaped name but not a secret value. Separately, *every* value regardless of key name is failed if it is shaped like a real credential (a private key block, a JWT, `AKIA...`, `sk_live_...`, `ghp_...`, `xox...`, or any 32-plus-character opaque token), so a pasted secret cannot hide behind an innocent key name.
  - Two values are allowlisted by exact value, keyed `file:KEY` with the reason in place: the root `.env.example`'s `DB_PASSWORD=nodia` and `MINIO_ROOT_PASSWORD=nodia12345`. Both are the local Compose stack's own published defaults, carried inline in `infra/compose/docker-compose.yml` as `${DB_PASSWORD:-nodia}` and `${MINIO_ROOT_PASSWORD:-nodia12345}` (verified against that file, not assumed), so the stack stands up identically with the root `.env` absent and neither value is a secret. Pinning by exact value means swapping either for a real credential still fails the test.

Nothing was found to fix: the `env`-outside-`config` arm was already clean across the whole application, and no committed `.env.example` carries a credential. That is a weaker statement than task 16's "fix anything found" was meant to support, because the scanner that would have searched the rest of the repo and the history never ran.

Test evidence:

- Both cases pass green on the committed tree, and **both were proven against a seeded gap, witnessed**: appending `$seeded = env("SEEDED_GAP");` to `routes/api.php` failed the first case naming `api.php:27`; appending `SEEDED_API_TOKEN=sk_live_abc123def456ghi789` to the root `.env.example` and `SEEDED_INNOCENT=AKIAIOSFODNN7EXAMPLE` to `apps/checkin/.env.example` failed the second naming both files, the second of them through the key-name-independent credential-shape arm rather than the secret-shaped-key arm, which is the arm that would catch a real paste. All three seeds were reverted (`git checkout`, working tree confirmed clean before the commit).
- `php artisan test --testsuite=Architecture` - 45 tests, 45 passed, 106 assertions (43 before this task, plus these two).
- `./vendor/bin/pint --test tests/Architecture/SecretHandlingTest.php` - passes.
- No Data class changed, so `composer -d apps/api run types:generate` was not run. No other suite is affected: the change adds a test file and touches no production code.

Commit: `4224ed8` (`test(support): guard environment reads and committed env examples`).

Deviation from the plan, material and operator-directed: **the secret scanner arm of task 16 was dropped and no scan gate exists in CI.** The plan's Slice 6 calls for "a repo secret scanner (tool selected at slice start; gitleaks is the default candidate) runs in CI and fails on findings", and stage exit criterion 8 requires "the secret scan gate is required in CI". Work done before the drop, all of it discarded: gitleaks was selected against current documentation (CLI v8.30.1, MIT licensed, pinned by version and by the release tarball's SHA-256; the `gitleaks/gitleaks-action` wrapper was rejected because it carries a non-MIT license and requires a `GITLEAKS_LICENSE` key for organization-owned repositories, which would have made the gate depend on a third-party signup), and a `.github/workflows/security.yml` was written to run `gitleaks git .` over full history on every pull request with no paths filter, since a credential can be committed into any file and a secret committed then deleted still sits in the history. The operator declined the scanner before it was ever run locally and before `.gitleaks.toml` was written, so **the repository has never been scanned for secrets and this stage carries no evidence that its history is clean**. The workflow file was deleted, not committed. Consequences for whoever closes the stage: exit criterion 8 cannot be met as written, so either the scanner lands later (as gitleaks, as another tool such as trufflehog, or as GitHub's own push protection and secret scanning, which needs no third-party tool) or criterion 8 and the Slice 6 plan text must be amended to drop the requirement deliberately. T16 is ticked in the checklist above as partial with that qualification attached; it is not a clean completion.
