<?php

// Registered under the tenancy.storefront group (Host resolution, no
// staff identity) via EventCatalogServiceProvider, mounted under /v1
// (stage-05a plan, task breakdown item 2). Empty for now: task breakdown
// item 10 populates this file with the locale-negotiated storefront read
// endpoints (slice 5).
