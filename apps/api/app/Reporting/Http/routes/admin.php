<?php

// Registered under the tenancy.admin group (Passport staff bearer plus
// X-Tenant-Id membership validation) via ReportingServiceProvider,
// mounted under /v1 (stage-11 plan, task breakdown item 3). Empty for
// now: tasks 6, 9, 12, and 16 populate this file with the daily-sales,
// event-finance, attendance, and exports routes as each slice's
// controller and Data objects land, mirroring EventCatalog's own admin
// route file at the same scaffolding stage.
